# Corebridge X — Current Project State

## Purpose

Corebridge X is an internal PSA (project + team-ops) tool for a B2B outsourcing hub serving several partner brands. It is **internal-staff-only** — accounts are invite-only, there is no self-signup, and there is no client login of any kind. "Clients" are modeled as **Companies**; Client Contacts are reference-only data (name/title/email/phone), never accounts.

Operational focus: who is doing what, for which client, current status, time spent, and what's blocked — not a generic project-management tool and not an analytics/BI product.

Architecture is **frontend-first, provider-swap**: every feature has a mock provider (in-memory data, fully functional) and a Supabase provider (same interface, not yet implemented — see "Current backend status" below). Screens and hooks only ever talk to the provider interface, never to mock or Supabase specifics directly, so swapping the backend is meant to be a wiring change, not a rewrite.

Stack: Next.js (App Router) + TypeScript + Tailwind v4 + shadcn/ui (Base UI primitives). Supabase (Auth + Postgres) is the intended real backend, owned by a separate team.

## How to use this document

Source-of-truth order, most authoritative first:

1. **The current live repository/code** — what's actually implemented, always wins over any document.
2. **`docs/current-project-state.md`** (this file) — current verified status and handoff point.
3. **`docs/product-brief.md`** — detailed product rules, locked decisions, and full phase-by-phase history.
4. **`docs/route-map.md`** — routes and application structure.
5. **Git checkpoints** — recovery points; see "Latest verified checkpoint" below.
6. **AI chat transcripts** — historical context only. Never authoritative over what the repository and docs currently say.

**When starting a fresh AI/Claude Code session, first read, in this order:**
1. `docs/current-project-state.md`
2. `docs/product-brief.md`
3. `docs/route-map.md`

Then inspect the current repository (git status, the relevant source files) before assuming anything or changing code.

## Latest verified checkpoint

**Current:**
```
checkpoint: complete phase 11 workflow simplification
2026-08-25
Phase 11 in its entirety — 11A (Task Experience Redesign & Time
Simplification), 11B (Task Navigation & Quick View), 11C (Reporting
Simplification & Ownership), 11D (Role Surface Cleanup & Cross-App
Consistency, including Review Queue/Client Report Schedules UI removal,
Client Visits made temporarily Superadmin-only, legacy Internal Report
generation retired from the UI, and a repo-wide cross-app consistency
sweep) — manually accepted in full by the user. A "JWT issued at future"
runtime error was investigated pre-checkpoint and root-caused to
transient clock skew on Supabase's own hosted GoTrue/PostgREST
infrastructure, not application code or the local machine; confirmed
resolved by the user reloading the authenticated app. Phase 11 is
COMPLETE. See git log for the exact commit hash; pushed to GitHub.
```

**Previous:**
```
checkpoint: complete phase 10 one-level subtasks
2026-08-24
Phase 10 in its entirety — One-Level Subtasks (base slice), hierarchy-
authorization hardening (can_access_task_directly separation), migration
canonicalization (can_user_access_company/can_user_access_task Project-
access parity fix), and a post-implementation TaskDrawer infinite-
recursion fix (nested Subtask drawer now lazily mounted, only when
subtaskDrawerId != null) — manually accepted in full by the user against
the complete acceptance checklist (login/dashboard stability, Task/
Subtask creation, inherited context, stacked drawer, recursion-fix
regression check, one-level enforcement, independent statuses, parent
Done warning, checklist safety, independent time tracking + rollup,
list/Board/Planner presentation, hierarchy vs. direct-access permission
checks, Supervisor behavior, full Task/Subtask pages, final navigation
sweep). Phase 10 is COMPLETE. Pushed to GitHub.
```

**Earlier:**
```
1c45d688c94336c158cc00c60ad79030586d9c36
checkpoint: complete phase 9 reporting automation
Phase 9 in its entirety — 9A (Reporting Automation Audit), 9B (Reporting
Foundation + Client Report Correctness), 9C (Daily Update Automation), 9D
(Weekly Client Report Generation), 9E (Sparing Efficiency Review +
Finalization), 9F (Daily Visit Hours + Recurring Client Report Schedules,
including the locked Plan -> Complete Visit workflow correction and final
UX polish) — manually accepted in full by the user ("Phase 9 passed").
```

Before that: `67f8972d862e9a8ea57c280219a2f19cc5b45284` (Phase 10, complete); `a77785c57c3e5424fa63b4221ee9523a688ffae9` (Phase 8, complete); `299499948621f0d0d4742529d1e658d91b8eaf11` (Phase 9B), `a2f295f94a40e5c446e371a4b76cac9fcfb7dcdf` (Phase 9C), `39614fa660529c72d891c37ec573758599462559` (Phase 9D) — each a Phase 9 sub-checkpoint folded into the single final Phase 9 commit above; `ddc8da638205eb34436060dc003622e8a44b36c9` (Phase 8C), `77dbab9ed37766541aa94ddd053cae76a7e9dc40` (Phase 8B), `9b5a2d8789f9ea2909b2e37bbb7b368e656c68ee` (Phase 8A), `c63e86d03fafeb516846bb65759cfdacf19ae836` (Phase 7 — complete Supabase business backend).

**Baseline:**
```
76b8301d6cfc3c8b120d6529fd525d8faa896c77
checkpoint: working PSA frontend baseline before supervisor/subtask expansion
```

(There is one earlier commit, `0c47dccc11f9ad50dc793ac8d86f6a62f2f04002` — "Initial commit" — before the baseline above.)

## Current product hierarchy

```
Company
  → Project
      → Service / Workstream   (service container)
          → Activity           (optional single tag — see below)
          → Task                (executable work)
              → Subtask         (one level only — see Phase 10)
              → Checklist       (simple completion steps, inside a Task or Subtask)
```

- **Workstream** = one service delivered to one client (e.g. "Payroll 2026"). A lightweight container — name, service line, lead + team, status, optional start/renewal dates, optional recurrence. It carries **no** effort/estimate field of its own.
- **Activity** = a service-specific work category from the brand's Activity Catalog (Brand → Department → Activity). A Task may optionally carry a single `activityId` tag, scoped to the Task's own Workstream's service line when one is set. **This tag is optional, never required** — work is never blocked for lack of one.
- **Task** = the actual unit of executable work — title, status, priority, due date, expected time, assignees, checklist.
- **Subtask** = a full child Task, self-referencing its parent Task (`tasks.parent_task_id`) — exactly **one level deep** (a Subtask can never itself have children), inheriting its parent's Company/Project/Service/Activity context automatically, with its own independent status/assignees/time/checklist. **Built in Phase 10 — see that section below for the complete locked rules.**
- **Checklist** = plain completion steps inside a Task or Subtask (no sub-status, no assignee of its own).

**Locked data-model decision**: each Task belongs to exactly **one Workstream** (required — `Task.workstreamId`) and optionally **one Activity** (`Task.activityId: string | null`). `Task.companyId` is a denormalized copy of the Workstream's own company, kept in sync by the provider, never independently editable. **Do not** introduce `workstreamIds[]` or `activityIds[]` — if several Activities are ever needed at once, the intended direction is batch-creating separate Tasks, not one Task belonging to several Activities/Workstreams.

## Workstream / Activity rules

- Picking a Client scopes the Workstream picker to that Client's own Workstreams.
- Picking a Workstream scopes the Activity picker to that Workstream's own service line (e.g. a Payroll Workstream shows only Payroll Activities; File Management shows only File Management Activities). A Workstream with no service line set falls back to the whole brand's catalog.
- Changing the Client clears an incompatible Workstream/Activity selection; changing the Workstream clears an incompatible Activity selection.
- The inline "+ New Workstream" action from Task creation works, creates the Workstream under the selected Client without leaving the Task form, and auto-selects the new Workstream back on the Task.
- Workstream has **no** manual expected-time field. Its Time-vs-Budget figure is a roll-up: expected = sum of its own Tasks' `expectedMinutes`; actual = sum of completed time entries against those Tasks.

## Roles and access

Exactly **three roles**: `superadmin`, `supervisor`, `employee`. No configurable permission matrix.

- **Superadmin** — organization-wide visibility and management.
- **Supervisor** — self + direct-report team scope; is also a worker themselves, not purely a manager; manages/reviews the Tasks, Companies, and time of the people they directly supervise. **Does not** get organization-wide visibility.
- **Employee** — sees and acts on their own assigned work and assigned Companies only.

Key mechanics:
- `User.supervisorId` is the single flat field every "who manages whom" check (`managesUser`, `assignableStaffFor`, `visibleCompanyIds`, `canAccessTask`, `canAccessWorkstream`, `canViewTimeForUser`, `canCorrectTimeEntry`, …) is built on — a flat, single-supervisor hierarchy, not a configurable org chart.
- RBAC is enforced **inside the mock provider layer** (`src/lib/data/permissions.ts`, called from provider methods), not merely by hiding UI buttons — a direct call to a provider method from an unauthorized role/relationship is rejected the same way a hidden button would have prevented it.
- This provider-level enforcement is the mock backend's own discipline. It must be **mirrored in Supabase RLS** once that backend is real — RBAC today is not yet production security (see "Current backend status").

## Task assignment / execution

- Employees can self-add a Task; it goes live immediately (no approval) and notifies their supervisor + superadmin.
- An employee's own assignment is always forced to themselves (`resolveAssigneeIds`) — they can never assign someone else.
- A Supervisor can assign within their own team scope; a Superadmin has broad assignment scope.
- Ticking every checklist item on a Task auto-sets it to Done; unticking one on an already-Done Task reverts it to In Progress.
- `task-assigned` and `task-status-changed` notifications fire on genuine assignment/status changes (Phase 3.35) — never for the actor's own action on themselves, never duplicated by a no-op edit.
- Notification recipients are filtered through the same `canAccessTask` check the linked page itself uses, so a notification is never created whose link would then dead-end for its recipient (the "notification access protection" fix).

## Time tracking — verified current behavior

- Start / Pause / Resume / Stop a timer; manual "Log time" entry (time-range or duration).
- Exactly **one active (running) timer per user**, ever — starting a new one auto-pauses (not stops) whatever that same user had running elsewhere.
- A paused entry's elapsed time is excluded from further counting until resumed; resuming chains to the prior paused entry so the visible elapsed time is continuous.
- Every `TimeEntry` is owned by exactly one `userId` — there is no shared/team entry.

**Locked rule (Phase 3.36 refinement)**: **personal time logging requires being an explicit assignee of the Task.** Supervisor/Superadmin management visibility over a Task does **not**, by itself, grant permission to log their own time against it. If a Supervisor genuinely participates in the work, add them as a real assignee — their logged time then stays fully and separately attributed from the original assignee's. This is enforced in the provider (`canLogTime`, called from `startTimer`/`resumeTimer`/`createManualEntry`), not just a hidden button.

Also locked: a Supervisor/Superadmin may **review** an Employee's time (Team Time) without being assigned to the underlying Task — assignment gates *logging your own* time, not *reviewing someone else's*. Nobody may start/pause/resume/stop **another user's** live timer, regardless of role — this predates Phase 3.36 and was re-verified unchanged by it.

## Supervisor Time Review & Correction — Phase 3.36

**Core rule**: Estimated time is a planning/review benchmark. Actual/logged time is what really happened. Exceeding the estimate is a review *signal* only — it never automatically invalidates, reduces, or flags a time record. A **correction** is only for a genuinely inaccurate record (forgot to stop the timer, a duplicate entry, a typo'd manual duration).

Correction rules, all provider-enforced:
- Only a **completed** entry (`durationMinutes !== null`) can be corrected — never a running one.
- Supervisor → their own direct reports only. Superadmin → organization-wide. Employee → can never correct, including their own — there is no self-correction for any role in this phase.
- A **reason is always required**.
- Confirming a correction updates `TimeEntry.durationMinutes` to the corrected value in place (the "effective" duration every existing total/rollup reads) — `startTime`/`endTime` are untouched.
- Every correction is stored **separately and append-only** (`TimeEntryCorrection`, its own `db.timeEntryCorrections` table) — never overwritten. Correcting an already-corrected entry adds a new record whose "previous" value is the entry's value at that moment, so repeated corrections chain correctly and nothing is ever lost.
- The Employee whose time it is can always see the full correction history for their own entries (view-only — they cannot alter it). Visibility is a separate, broader gate (`canViewTimeForUser`) from correction ability (`canCorrectTimeEntry`).
- Correction duration is entered as plain **Hours / Minutes** fields (not the Task/Workstream expected-time unit control) — no unit conversion required from the Supervisor.
- A correction never grants any control over another user's live timer.

**Task estimate-vs-actual review signal**: always **Task `expectedMinutes` vs. the cumulative effective actual minutes across every completed entry logged against that Task** (any assignee, summed) — never one individual `TimeEntry` compared against the whole Task's estimate. Team Time shows this cumulative Task-level context beside (not instead of) each entry's own duration.

## Notifications

`task-assigned` and `task-status-changed` notify the relevant assignees/supervisor (never the actor about their own action, never duplicated). All notification links are filtered so they only ever point somewhere the recipient can actually open. Full rules live in `docs/product-brief.md`'s "Task Assignment & Status Notifications" section — not repeated here.

## Current backend status

- The user's local `.env.local` reads **`supabase-core`** as of this writing (the Phase 7 smoke-test mode) — see below for the full four-mode model.
- There are **14 provider groups** in `src/lib/data/providers/` (Auth, Companies, Workstreams, Tasks, TimeEntries, Notes, Notifications, Templates, TaskHandoffs, ActivityCatalog, AccomplishmentsReport, SavedViews, DailyUpdates, ClientReport).
- **`NEXT_PUBLIC_DATA_PROVIDER` has four valid values**, centralized in `src/lib/data/provider-mode.ts` (`providerMode`/`usesSupabaseAuth`/`usesSupabaseCoreData`/`usesSupabaseData` — everything else compares against these instead of raw strings): `mock` (every provider mock — the default and an unrecognized/invalid value both fail safe to this), `supabase-auth` (Auth is real Supabase, every other provider stays mock), `supabase-core` (Auth + Companies + Workstreams + Activity Catalog + Tasks + Time Entries + Notifications are real Supabase; Notes/Templates/Task Handoffs/Accomplishments Report/Saved Views/Daily Updates/Client Report stay mock — unchanged by Phase 7D/7E), **`supabase`** (every one of the 14 provider groups is now real Supabase — **as of Phase 7D/7E, this is a genuinely complete real backend; no provider selected under this mode still throws a `notImplemented` stub**).
- **All 14 real Supabase providers are now implemented** in `src/lib/data/providers/supabase/` — the original 6 (Companies/Workstreams/ActivityCatalog/Tasks/TimeEntries/Notifications, Phase 7A-C) plus the 7 completed this phase (Notes/TaskHandoffs/SavedViews/Templates/DailyUpdates/AccomplishmentsReport/ClientReport, Phase 7D/7E) plus Auth. All follow the same flat-fetch-then-JS-join pattern (mirroring the mock providers' own in-memory join style), all mapping database rows back to the exact existing `*WithRelations`/domain shapes so no component/hook changed. Every genuinely sequential/stateful mutation is a `SECURITY DEFINER` RPC rather than a raw table write — see "Supabase Phase 7" below for the full list.
- **Current RBAC is enforced in real Postgres RLS for every provider except the mock ones under `mock`/`supabase-auth`/`supabase-core`'s still-mock set.** RLS/grants, never JWT claims or a service-role client, is the real access-control boundary for every real provider.

**Existing Supabase target**: **Corebridgex** organization → **corebridgex-crm** project (ref `qxqxzuoaivyddwxqqoog` — not a secret, safe to record). No credentials, keys, or URLs for it are recorded in this document or should ever be committed to this repository.

**The repository is now linked to this project, and the Foundation A schema is live there**: all four migrations pushed and confirmed matching (`supabase migration list` shows identical local/remote versions), the reference/test seed applied (idempotently — see below), RLS confirmed enabled on all 7 tables, `handle_new_user()`'s trigger confirmed present on `auth.users`. `profiles`/`user_companies` are confirmed empty — no Auth users exist yet. This is strictly schema + fake/test reference data; no real client information, no Auth users, no secrets were ever placed there by this process.

**Supabase Foundation A (local scaffolding) is complete** — the following was built locally, then applied to the hosted project as described above:
- `@supabase/supabase-js`, `@supabase/ssr` installed; `supabase` CLI pinned as a devDependency (no global install).
- `supabase/` initialized locally (`supabase init`) — config only, no remote link.
- `src/lib/supabase/client.ts` (browser) and `server.ts` (server) — the sole Supabase-client construction points; not yet called from any provider.
- `src/proxy.ts` — **Next.js 16 uses this file, not `middleware.ts`** (Middleware was renamed to Proxy in Next 16; same runtime behavior). Scoped to session-cookie refresh only, never an authorization decision. No-ops cleanly when the Supabase env vars are absent (today's normal state) — verified the mock app still starts and serves pages correctly with this file present.
- First migration set written under `supabase/migrations/` (local files only, never run against the hosted project): `profiles` (+ `handle_new_user` trigger, `is_superadmin`/`is_supervisor`/`is_employee`/`manages_user` helpers, `admin_set_user_role`/`admin_set_supervisor`/`admin_set_active` RPCs), `brands`/`service_lines`, `companies`/`client_contacts`/`company_service_lines`/`user_companies` (+ `can_access_company` helper). RLS enabled and policies written for every table in this set, mirroring current provider behavior exactly (including the discovered `is_internal` company flag needed because the mock's `"company-internal"` string id can't be a `uuid` PK — see the migration file's own header comment).
- `supabase/seed.sql` — reference/company data only (brands, service lines, 3 fake test companies plus the internal one, 2 fake contacts). No `auth.users`/plaintext passwords, no real client information. Made idempotent (`ON CONFLICT DO NOTHING` / `NOT EXISTS` guards keyed to the schema's own uniqueness invariants) after review found the original version would hard-fail on a second `db push --include-seed`. Applied to the hosted project and verified by row count — safe to re-run.
- `.env.example` added (git-ignore has an explicit `!.env.example` exception) — variable **names** only (`NEXT_PUBLIC_DATA_PROVIDER`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`), no values. `.env.local` remains ignored and untouched.
- `src/proxy.ts` uses `supabase.auth.getClaims()` (not `getUser()`) — the current-recommended call: it refreshes the session first if the token is close to expiring, then verifies the JWT, typically locally via a cached JWKS endpoint rather than always round-tripping to the Auth server.

**Development strategy**: the local Docker-based Supabase stack is **not** being used. The user is new to Supabase; the hosted **corebridgex-crm** project itself is the development/test backend, connected directly (see above) rather than through a local Postgres.

Tables intentionally **not yet created**: workstreams, tasks, task_assignees, checklist_items, time_entries, time_entry_corrections, notes, notifications, task_handoffs, reports (either kind), daily_updates, templates, saved_views, documents. These are later migrations, once this slice is applied and re-tested.

A prior `initial_workops_schema.sql` reportedly exists from earlier planning and **predates the current role/hierarchy architecture** (Company → Workstream → Task, the flat `supervisorId` model, the current permission functions). It was not found in this repository — if it's supplied later, treat it as historical input to be reconciled against the *current* code, not applied as-is.

### First-superadmin bootstrap (documented, not yet performed)

Every new Supabase Auth user gets a `profiles` row defaulted to `role = 'employee'` via `handle_new_user()` — there is no signup path that can pick a different role (deliberate: role is never signup-controlled). This creates a one-time chicken-and-egg problem: someone has to become the first Superadmin before the superadmin-only `admin_set_user_role` RPC can be used on anyone, including that very first user. Planned resolution, to be performed interactively (not yet done):
1. Create the first real dev Auth user through the Supabase Dashboard's Authentication UI (email, in the `corebridgex-crm` project).
2. `handle_new_user()` fires automatically and creates their `profiles` row as `role = 'employee'`.
3. The **project owner**, using the Dashboard's SQL Editor (a privileged, human-in-the-loop context — not an app code path, not a migration file, not a public endpoint), runs a single one-time `update public.profiles set role = 'superadmin' where id = '<that user's id>';` for that specific row only.
4. From that point on, every further role/supervisor/active change goes through the normal `admin_set_*` RPCs, exercised by that first Superadmin — no further manual SQL needed.

Explicitly not done, and not planned: no signup-metadata role selection, no public "promote to superadmin" endpoint, no user IDs/emails/passwords ever placed in a migration file or committed anywhere.

**Status: complete and verified.** One development Auth user exists; `handle_new_user()` fired correctly (its `profiles` row was auto-created, matched by `id`, defaulted to `role = 'employee'`/`active = true`/`supervisor_id = null` — confirmed read-only before any bootstrap SQL ran); the project owner then ran the one-time bootstrap SQL in the Dashboard SQL Editor. Read-only re-verification afterward confirmed that same profile now has `role = 'superadmin'`, with `active = true` and `supervisor_id = null` unchanged. **One development Superadmin now exists on the hosted project.** No Supervisor or Employee test users have been created yet — that's deliberately deferred so the normal `admin_set_*` RPC path (exercised by this Superadmin) becomes the mechanism for creating them, rather than more one-off SQL Editor use.

### Internal Company representation (`companies.is_internal`)

Approved as the Supabase equivalent of the mock's `INTERNAL_COMPANY_ID` string sentinel. Rules preserved: at most one company may ever be flagged `is_internal` (enforced by a partial unique index, not just convention); it's visible to every authenticated staff member regardless of their own `user_companies` rows (via `can_access_company()`'s explicit `is_internal` check); being the internal company grants **no** access to any other, unrelated company — the check is scoped to that one row's id, never a blanket bypass. Any future real Supabase provider implementation should resolve "the" internal company by querying `is_internal = true`, never by matching a hardcoded id string.

## Current phase status

**Phase 3.36 — Supervisor Time Review & Correction: COMPLETE and manually verified.**

**Supabase Foundation A — Local Scaffolding + First Migration Set: schema and test seed are live on the hosted `corebridgex-crm` project.** The app still runs entirely on mock — nothing in the running application talks to Supabase yet.

**Supabase Foundation B — Hosted Seed + First Auth/Profile Verification: COMPLETE.** Remote schema/seed verified, `handle_new_user()` verified, first-superadmin bootstrap verified (see above). One development Superadmin exists on the hosted project; no Supervisor/Employee test users yet. The app still runs entirely on mock — `NEXT_PUBLIC_DATA_PROVIDER=mock`, no provider talks to Supabase yet.

**Supabase Foundation C — Explicit Grants + Function Execution Hardening: COMPLETE.** An audit of the hosted project found that Supabase's platform-level default ACLs (`pg_default_acl`, for role `postgres` in schema `public`) had been auto-granting every new table/function/sequence full privileges to `anon`/`authenticated`/`service_role` — broader than any migration's own `GRANT` statements intended (GRANT is additive, it never replaces a pre-existing broader default). One new migration (`20260813162744_harden_public_grants.sql`, applied on top of the existing Foundation A set — none of those four files were modified) now establishes an explicit least-privilege model:
- Table privileges are now exact per the real provider contracts: `anon` has zero privileges on any of the 7 public tables; `authenticated` gets only what current providers actually use (e.g. `profiles` is SELECT + column-scoped UPDATE on `full_name`/`email` only — `role`/`supervisor_id`/`active` stay unreachable by ordinary UPDATE; `companies`/`client_contacts` get no DELETE, since neither provider has a delete path; `company_service_lines`/`user_companies` get no UPDATE, since both are always fully replaced rather than row-edited); `service_role` gets ordinary CRUD only. No role retains TRUNCATE/REFERENCES/TRIGGER on any table.
- Function EXECUTE is now explicit: `PUBLIC`/`anon` have none of the 9 application functions; `authenticated`/`service_role` can call the 5 RLS helpers + 3 admin RPCs only (the admin RPCs keep their own internal `is_superadmin()` check — EXECUTE privilege was never the authorization boundary); `handle_new_user()` is not directly callable by any role and remains attached to the `on_auth_user_created` trigger on `auth.users` unchanged (trigger firing doesn't require the triggering role to hold EXECUTE on the trigger function).
- `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public` now excludes `anon`/`authenticated`/`service_role` from future tables/functions/sequences — a future migration must GRANT explicitly rather than inheriting broad access automatically. (Supabase's own `supabase_admin` role default ACL is untouched — out of scope, platform-managed.)
- No data was touched, no RLS policy was changed (all 13 pre-existing policies remain, same names/commands), RLS remains enabled on all 7 tables. Committed as `628021e0f8b254490241bf4fbac274823dd24d72` ("checkpoint: harden Supabase public privileges").

**Supabase Auth — Controlled Transitional Mode: COMPLETE and manually verified.** `NEXT_PUBLIC_DATA_PROVIDER` gained a third value, `supabase-auth` (real Auth, every other provider stays mock — see "Current backend status" above for the full three-mode model). `supabaseAuthProvider` is a real implementation of the existing `AuthProvider` contract (`getCurrentUser`/`login`/`logout`/`updateProfile`), mapping the signed-in session to the app's `User` type from `public.profiles` (role/active/supervisorId/email/fullName/createdAt) and `public.user_companies` (assignedCompanyIds) — never from JWT/`user_metadata`, never through a service-role key (none exists in this app). An Auth identity with no matching `profiles` row, or with `active = false`, is denied app access (signed out, not fabricated as a default Employee). Editing a profile's email to a different value is explicitly rejected for now (`"Changing your sign-in email isn't supported yet"`) rather than letting `public.profiles.email` diverge from the real Supabase Auth sign-in email — fullName-only edits still work, gated the same `canEditOwnProfile` (superadmin-only) way as mock. The login page's quick-demo account buttons only render in `mock` mode.

**Manually verified by the user in the running app** (real hosted `corebridgex-crm` project, real Superadmin credentials — no email/UUID/password recorded here): real Supabase Superadmin login succeeds; mock quick-login controls are hidden in `supabase-auth` mode; the authenticated dashboard loads with the Superadmin identity/role recognized correctly; a page refresh preserves the authenticated session (`getCurrentUser` restores it, no re-login needed); logout correctly clears the session and returns to `/login`; a wrong password produces a clear authentication error without creating a session, and a subsequent correct login still succeeds afterward; existing mock business-data pages (Companies, Tasks, etc.) remain fully usable while signed in as the real Auth user. **All Auth acceptance criteria passed — no regressions found.**

**Known transitional limitation (expected and accepted)**: the real Superadmin's Supabase UUID doesn't exist anywhere in the mock's own task/time/notification seed data, so in `supabase-auth` mode some self-specific mock views (My Day's own assignments, personal notifications/time history) can legitimately look empty for that user — organization-wide Superadmin visibility (where permission logic already short-circuits for the role) still works. This is expected and temporary; do not "fix" it by injecting the real UUID into mock seed data. Email-change support also remains intentionally deferred (see above) until a real confirmation flow is built.

**Boss Feedback Alignment — Phase 5, Final Domain Alignment: COMPLETE and manually accepted** (Implementation A + A.1 + A.2 + A.3, checkpointed together). The business owner clarified that a Workstream represents a client-specific instance of a *Service*, that Activities should be explicitly selected per-workstream, and that Tasks should be creatable directly under an Activity. Implemented, in mock only — no Supabase schema touched:
- **Workstream identity is now Service-first.** `service_lines` remains the internal Service Catalog, unchanged. The Workstream create/edit form's primary control is now "Service / Workstream" (a `Select` over `service_lines`); the old "Workstream name" field is now an optional, visually secondary "Reference / qualifier" input that can never substitute for a service. The `name` field is preserved internally (backward/recurrence/template compatible) but is now *derived* (`deriveWorkstreamName`/`splitWorkstreamQualifier`/`workstreamDisplayHeading` in `src/lib/data/workstream-name.ts`) from the service name plus an optional qualifier — display everywhere (detail page, overview card) now prioritizes the service name.
- **A normal client Workstream now REQUIRES a Service** — "None" isn't offered as a choice when creating one, and the form won't submit without a service picked. The one canonical exception is the Internal/Non-billable company (`company.id === INTERNAL_COMPANY_ID`, the same sentinel every other visibility check already keys off — no second internal-company concept was introduced), where "None" remains available, preserving "Internal Operations"'s existing null-service behavior exactly. Editing an already-existing legacy workstream that predates this rule (still `serviceLineId: null`, non-Internal company) also keeps "None" available so opening Edit and saving without touching Service never destroys it — but a brand-new workstream, or one that already has a real service, never gets that option.
- **A Workstream can now persist a selected subset of its service's Activities**, and that persisted set is now **always authoritative — zero rows means zero, never a fallback to the whole service catalog.** New `WorkstreamActivity` join (`workstreamId`, `activityId`) — mock `db.workstreamActivities`, resolved into `WorkstreamWithRelations.activities: Activity[]` the same way `team` resolves from `workstreamMembers`. `useWorkstreamActivities` (the single hook every Activity picker reads from — Task form, Quick Add, the workstream detail page) has no generic "empty means legacy" branch at all, so a genuinely-configured empty selection can never be confused with an unconfigured one. For a service with real catalog activities, the form now requires selecting **at least one** to save (create or edit) — a service with no catalog yet is exempt and can be saved with zero, showing an explicit "No activities are configured for this workstream" state rather than looking broken or silently expanding later.
- **The workstream detail page's Tasks card is Activity-oriented** (`WorkstreamActivityTasks`): each enabled Activity gets its own compact group of its own Tasks (still full Task rows — status/priority/checklist-progress, unchanged `TaskRow`) plus its own "+ Add Task" pre-filled with that workstream + activity; a "No activity tag" group preserves legacy/untagged tasks, never hidden.
- **Task creation**: a brand-new task on a workstream that has activities to choose from requires picking one (client-side, create-only — editing an existing legacy task never forces this). Server-side (`mock-tasks-provider.ts`), a submitted `activityId` must belong to the workstream's enabled set whenever that workstream has any persisted associations at all — a workstream with genuinely zero (a no-catalog service) still accepts any catalog activity for its service, unchanged. Any role that can access the workstream (including Employee) can add a Task under an Activity — self-assignment/team-assignment rules are completely unchanged.
- **Apply Template** passes `activityIds: []` — honestly zero, not expanded by any fallback; Templates still have no Activity concept, an explicitly preserved, undocumented-no-longer limitation, not redesigned this phase. **Generate Next Occurrence** carries forward the source workstream's exact configured `activities` (never expanding to the full service catalog) — if the source has zero, the new occurrence keeps zero.
- **Legacy seed data (`seed-workstream-activities.ts`) deterministically backfills every existing workstream's OLD effective scope**: for each seeded workstream with both a `serviceLineId` and a matching department for its own brand, every one of that department's activities is seeded as a real association (reproducing exactly what the old, pre-per-workstream-selection picker always implicitly showed — a strict superset of every activityId any of its tasks already reference). A workstream whose brand/service has no matching department (every brand except Sparing Consulting today) or no service line (Internal Operations) correctly gets zero rows — nothing to associate, not a gap.
- Regression-checked: `tsc`/`eslint`/`build` clean (mock and the current `supabase-auth` `.env.local`, plus an ephemeral mock-mode build) — same 2 pre-existing warnings only.

**Implementation A.2 — supabase-auth/mock Task actor compatibility fix.** Manual testing in `supabase-auth` mode (real Superadmin login, mock business data) surfaced a real bug: creating a Task via the new "+ Add Task under an Activity" flow threw `"references unknown creator"`, because `mock-tasks-provider.ts`'s Task-hydration always looked `createdById`/`statusChangedById` up in mock `db.users` — which the real, non-seeded Supabase viewer UUID is never in. Fixed narrowly: `toTaskWithRelations` now takes the current `viewer` and resolves an actor id via a small local helper (`resolveTaskActor`) that returns the already-known `viewer` object directly when the id matches the viewer's own, falling through to the existing mock `db.users` lookup otherwise — no seed-user injection, no ID aliasing/rewriting, no change to who a Task can be *assigned* to (assignee resolution/rules are completely untouched). Since a freshly-created Task's `createdById` is always the current viewer's own id, this closes the bug's root cause structurally in the create path (the throw condition is now unreachable there), so no additional rollback/atomicity scaffolding was needed. This compatibility is intentionally narrow to the current one-real-test-identity transitional setup — it is not a general multi-real-user identity model, and that need disappears once business providers migrate to real Supabase.

**Implementation A.3 — Activity-first Workstream UX + Task lifecycle polish.** Manual testing of A/A.1/A.2 found the Workstream detail page still *read* task-first even though the data model was already Activity-first, plus a real lifecycle gap (starting work never left a task at "Todo"). Fixed, still mock-only:
- The Workstream detail page's grouped section is now titled **"Activities"** (was "Tasks") — Task entities/pages elsewhere are unaffected, this is presentation-only on this one page.
- Each Activity's own "+ Add Task" is now the **primary** task-creation path; the generic top-level "+ Add task" button only renders when a workstream has **zero** configured activities (so a legacy/no-catalog workstream is never left without a way to create a task). "Add from activity" (Quick Add) remains available as a secondary action, unchanged, still scoped to only the workstream's own enabled activities.
- A Task form opened via an Activity's "+ Add Task" now shows that **Activity as fixed, read-only context** (not a re-editable picker) — `TaskFormDialog` gained `activityLocked = mode === "create" && Boolean(defaultActivityId)`; editing an existing task is never locked, so retagging/untagging still works there.
- **Role-aware Activity ordering** on the same page (`WorkstreamActivityTasks`): Employee sees "My Work" (activities with an open task assigned to them) before "Other Activities"; Supervisor sees "My Work" → "Team Work" (direct reports' open tasks) → "Other Activities," with an activity that has both shown once, in "My Work" only (never duplicated); Superadmin (no personal-work concept) sees "Active Activities" (has any tasks) → "Other Activities." Activities with zero tasks render in a visually compact/muted card (no complex accordion). Inside every activity, tasks sort by actionable status first (in-progress → blocked → waiting-on-client → todo → done), due date as the tiebreaker.
- **Start Timer on a Todo task now transitions it to In Progress** — `mockTimeEntriesProvider.startTimer` routes this through the real `tasksProvider.updateTaskStatus` (never a raw mutation), so `statusChangedAt`/`statusChangedById` are set correctly and resolve through A.2's current-viewer-aware actor resolution. Only Todo → In Progress is automatic; Blocked/Waiting on client/Done are never overridden. The timer entry is always committed first — a (currently undemonstrated) status-update failure is swallowed rather than rolling back or failing the Start Timer action itself. The existing checklist-driven Done/In-Progress-on-reopen automation is unchanged.
- Small copy fix: the Workstream Overview card's task-count progress bar no longer says "No checklist" for a workstream with zero tasks (a `ChecklistProgress` component reused from the per-Task checklist display) — it now takes an optional `emptyLabel` (default unchanged) and this one call site passes "No tasks yet."
- Regression-checked: `tsc`/`eslint`/`build` clean against the current `.env.local` and an ephemeral opposite-mode build — same 2 pre-existing warnings only.

**Status: Implementation A, A.1, A.2, and A.3 have all been manually accepted by the user together and are checkpointed as one commit** — see "Last updated" below for the exact hash.

**Mock refresh reminder (unchanged constraint, now explicit)**: while business providers remain mock, a full browser reload resets all temporary mock-data mutations (the mock "database" is just seed data re-materialized in memory) — this is expected, not a bug, and is unrelated to Auth (which stays real and persists normally). When manually verifying a temporary mock mutation, navigate away and back rather than doing a full reload. This limitation disappears once business providers migrate to real Supabase.

**Phase 6 — Employee & Supervisor Operational UX: IMPLEMENTED (mock/UI only), pending manual acceptance.** Closes the one confirmed gap from the earlier audit — the Supervisor's `/dashboard` landing page had no personal-work section, unlike My Day (which already had full Employee/Supervisor parity). Employee remains the operational baseline; Supervisor = Employee + direct-report management, never org-wide.
- **`SupervisorDashboard`** gained a new **"01 · My Work"** section (own open tasks via `useMyTasks()`, an "Add task" button, and a personal running-timer/weekly-hours card via `useMyTimeEntries()`) — the exact same personal-work pattern `EmployeeDashboard` already used, reused rather than duplicated. The existing team-oversight content is unchanged, just renumbered/relabeled: "Team" → **"02 · Team Attention"**, "Review & Activity" → **"03 · Review & Activity"**. The top KPI row stays team-level (unchanged) — it's still useful oversight context, not personal work.
- **Task cards gained Client/Service/Activity context.** `TaskGridCard` (My Day's primary card, previously title/status/priority/checklist/assignees/due-date only, with no idea which client or service a task belonged to) now shows a compact `{company} · {workstream} · {activity}` line (activity segment omitted when untagged). Both dashboards' "My Tasks" list subtitles (`EmployeeDashboard`, `SupervisorDashboard`) gained the same activity segment for consistency with the workstream detail page's own no-redundant-prefix convention.
- **Verified by code inspection, no changes needed** (already correct): Supervisor's own task creation/self-assignment, personal time-logging (`canLogTime`, assignee-only, role-agnostic), My Day (already had full Employee-equivalent parity plus the team attention strip), receiving own notifications, and appearing in their own Team Time roster — all confirmed still intact and unaffected by Phase 5. Sidebar navigation was re-verified unchanged and already correctly additive (Employee: Dashboard/My Day/Companies/Reports; Supervisor/Superadmin: + Tasks/Team Updates/Team Time) — no change made.
- Regression-checked: `tsc`/`eslint`/`build` clean against the current `.env.local` and an ephemeral opposite-mode build — same 2 pre-existing warnings only. **Manually accepted by the user and committed** as `cb2e7c5ff57dae46d298bcc69e515ef156ae27ae` ("checkpoint: employee and supervisor operational ux").

**Role connectivity readiness (Supabase Auth/profiles) — resolved.** Real development Auth identities now exist on the hosted `corebridgex-crm` project: 1 Superadmin, 1 Supervisor, 2 Employees, with the Supervisor/Employee `supervisor_id` relationships set. (Created interactively by the project owner via the Supabase Dashboard + a one-time SQL Editor `UPDATE`, the same mechanism as the first-Superadmin bootstrap — no in-app admin UI exists yet for this.) A read-only aggregate check confirmed exactly this shape (correct role counts, all active, no self-supervisor violations) — **no names/emails/UUIDs were ever printed or recorded**, by design; every seed/demo reference to these identities resolves them dynamically via `role`/`created_at`-ordered SQL subqueries, never a hardcoded id.

## Supabase Phase 7 — Core Operational Backend + Collaboration/Reports/Productivity (schema/providers live on hosted project, app wiring uncommitted)

**Status: schema, RLS, grants, and seed for the FULL feature set (7A-C core + the Phase 7A-C hotfix + 7D Collaboration + 7E Reports/Productivity) are live and verified on the hosted `corebridgex-crm` project. All 14 real Supabase providers are implemented; `supabase` mode is now a complete real backend with zero stub providers. `tsc`/`eslint`/`build` pass in all four modes (`mock`/`supabase-auth`/`supabase-core`/`supabase`). Deliberately uncommitted, pending one final full-Supabase manual smoke test before checkpointing.**

### Phase 7A-C hotfix (already applied, see its own migration below)

`20260814100000_hotfix_workstream_task_visibility.sql` — `can_access_workstream()` gained one additional branch: visible when a Task inside that Workstream is assigned to the current user AND they have company access to that task, mirroring `can_access_task`'s own compound condition exactly (never broader). Fixes a real gap where an Employee could read an assigned Task but not its parent Workstream (their `task_assignees` row was never mirrored into `workstream_members`). Also centralized 19 duplicate `initials(fullName)` functions into one `src/lib/initials.ts` `getInitials()` helper, and fixed a latent bug in the old duplicated logic (3+ word names now correctly use first-word + last-word, not the first two words). Added empty-id guards to `getCompany`/`getWorkstream`/`getTask` (a caller mid-render can pass `""` before its own dependency loads — now returns `null` instead of a Postgres `invalid input syntax for type uuid` error).

### Phase 7D — Collaboration (new migrations, all pushed)

- `20260814110000_notes.sql` — `notes` (exactly one of `company_id`/`task_id` set, enforced by a `num_nonnulls` CHECK). Append-only — no UPDATE/DELETE grant, no update/delete provider method, matching the mock exactly. RLS mirrors `can_access_task`/`can_access_company`.
- `20260814110001_task_handoffs.sql` — `task_handoffs` (no status enum; pending/acknowledged is derived from `acknowledged_at`). Two new **parameterized** access helpers, `can_user_access_company(candidate_id, ...)`/`can_user_access_task(candidate_id, ...)` — purely additive variants of the existing `auth.uid()`-based helpers that take an explicit candidate id, needed to validate a handoff recipient (not the caller) is a legitimate target. RPCs: `list_handoff_candidates` (mirrors `usersWhoCanReceiveHandoff`), `create_task_handoff` (validates the recipient, inserts the handoff and its `task-handoff` notification atomically), `acknowledge_task_handoff` (recipient-only, once).
- `20260814110002_saved_views.sql` — `saved_views` (jsonb `filters`, exact `SavedViewFilters` shape). Strictly owner-scoped RLS (full CRUD, `user_id = auth.uid()` throughout) — no sharing concept, matching the mock.
- `20260814110003_templates.sql` — `templates` + `template_tasks` + `template_checklist_items` (ungated reference-data SELECT, no in-app editor, matching current behavior). Added the real FK for the already-existing `tasks.template_id` column (now correctly pointed at `template_tasks`, not `templates` — it always held a `TemplateTask` id). New `apply_template` RPC replaces the old client-side "1 createWorkstream + N sequential createTask calls, no rollback" sequence with one atomic transaction — closes a real partial-apply risk. `TemplatesProvider` gained a new `applyTemplate` method (mock implementation added too, for parity); `apply-template-dialog.tsx` now calls it instead of orchestrating multiple provider calls itself.
- `20260814110004_daily_updates.sql` — `daily_updates` (real `UNIQUE (user_id, date)`, closing a gap the mock only upheld via find-or-create lookup logic). The entry-computation/merge logic (`computeFreshEntries`/`mergeEntries`) stays in TypeScript in the provider (flat-fetch-then-JS-join, exactly mirroring the mock), operating on real Tasks/TimeEntries/TaskHandoffs — SQL's only job is 3 RPCs: `upsert_my_daily_update_draft` (create-or-refresh, never touches a confirmed row), `confirm_my_daily_update`, `reopen_my_daily_update`.

### Phase 7E — Reports + Productivity (new migrations, all pushed)

- `20260814110005_accomplishments_reports.sql` — `accomplishments_reports` (polymorphic subject: `subject_user_id`/`subject_company_id`, exactly one set per `kind`, same pattern as Notes) + `accomplishments_report_comments` (a real table, not jsonb, since commenting has a different actor set than the report's own owner-only edits). `brand_sections`/`history` are jsonb — genuine point-in-time snapshots the mock never re-derives after generation. `generateReport`'s evidence-gathering (checklist/time-entry/note evidence, brand/department/activity grouping) stays in TypeScript; every other transition (`updateDraft`/`finalize`/`reopen`/`addComment`/`trash`/`restore`/`permanentlyDelete`) is an RPC, since each has a different owner/state precondition.
- `20260814110006_client_reports.sql` — `client_reports` + `client_report_comments`, same shape/rationale as Accomplishments Reports. **Locked business rule preserved exactly as the mock implements it**: client-facing line items never carry a name (task title + duration, or the person's own reviewed Daily Update prose only); `generated_by_name`/comments/history stay on the row for internal audit use but must never be surfaced by whatever eventually renders a client-facing print/export view — the mock never enforced this at the data-shape level either, and this migration doesn't change that division of responsibility.
- `20260814110007_notifications_extension.sql` — adds the real FKs for `notifications.related_report_id`/`related_client_report_id` (bare uuid columns from Phase 7A-C, since those two tables didn't exist yet). No RLS/grant change — still no direct INSERT for `authenticated`, every notification (including the two new comment types) is RPC-written.
- `20260814110008_assignee_scope_hardening.sql` — resolves the Phase 7A-C-disclosed limitation ("individual Task assignee ids aren't independently revalidated against the actor's role"). One small trigger, `enforce_task_assignee_scope()`, reusing the already-existing `manages_user()` helper — turns out `manages_user(new.user_id)` alone exactly reproduces `assignableStaffFor`'s scope for all three roles at once (self for Employee, self+direct-reports for Supervisor, anyone active for Superadmin). **This limitation is now resolved, not just documented.**
- `20260814110009_phase7de_demo_seed.sql` — idempotent, fake-data-only: 6 Notes, 2 Task Handoffs (1 acknowledged, 1 pending), 3 Daily Updates (2 confirmed, 1 draft, dated yesterday so a real "today" row isn't immediately overwritten), 1 Template (2 template tasks, 2 checklist items), 2 Saved Views, 1 Accomplishments Report (finalized, 1 comment), 1 Client Report (draft). All resolved dynamically by role/created_at — no identifiers hardcoded or printed. Verified by aggregate row count.

**New migrations (all pushed, `supabase migration list` confirms Local ↔ Remote match)**:
- `20260814090000_workstreams.sql` — `workstreams` + `workstream_members`; `enforce_workstream_service_requirement()` trigger (normal client requires a `service_line_id`, Internal company exempt); `can_access_workstream()` RLS helper mirroring `canAccessWorkstream` exactly.
- `20260814090001_activity_catalog.sql` — `departments` + `activities` (ungated reference-data SELECT, same treatment as brands/service lines) + `workstream_activities` (a Workstream's enabled Activity set — **zero rows always means zero, no DB-level fallback**, matching the locked A.1 rule); `enforce_workstream_activity_service_match()` trigger.
- `20260814090002_tasks.sql` — `tasks` + `task_assignees` + `checklist_items` + `notifications`; `can_access_task`/`can_edit_task`/`can_progress_task` helpers; `enforce_task_invariants()` trigger (forces `tasks.company_id` to always match its Workstream's own company — closes a data-integrity gap the mock only trusted client-side); RPCs `update_task_status`, `toggle_checklist_item`, `notify_task_created`, `notify_task_assignment_changed`, `mark_all_notifications_read`. `notifications` has **no direct INSERT grant** for `authenticated` at all — every row is written by one of these RPCs.
- `20260814090003_time_entries.sql` — `time_entries` (partial unique index enforcing one running timer per user at the DB level) + `time_entry_corrections`; `can_log_time_on_task`/`can_correct_time_entry` helpers; RPCs `start_timer` (auto-pauses any other running entry, and — the A.3 lifecycle rule — flips a Todo task to In Progress via `update_task_status`, never a raw mutation), `stop_timer`/`pause_timer` (ownership-only, no re-check of task assignment, per the locked rule), `resume_timer` (re-checks assignment), `create_manual_time_entry`, `correct_time_entry` (Phase 3.36 rules preserved exactly: never self, never on a running entry, supervisor direct-reports-only, superadmin org-wide, positive duration, required reason). **No direct INSERT/UPDATE grant** on either table for `authenticated` — every mutation is RPC-only, mirroring the `admin_set_*` precedent.
- `20260814090004_harden_phase7_function_grants.sql` — a discovered-and-fixed gap: Foundation C's `ALTER DEFAULT PRIVILEGES` does **not**, on its own, prevent a newly-created function from getting an implicit PUBLIC EXECUTE grant (verified via raw `proacl` inspection — confirmed via re-check that all 20 new functions are now correctly `anon`/`public`-false). **Lesson for all future function-creating migrations: always explicitly `REVOKE EXECUTE ... FROM public, anon` per function — never rely on the schema-level default alone.**
- `20260814090005_phase7_demo_seed.sql` — idempotent, fake-data-only demo seed (departments/activities/workstreams/tasks/checklist items/`user_companies`), linking to the real Supervisor/Employee identities purely via `role`-keyed SQL subqueries. Verified by aggregate row count after applying.

**RLS/grants verification performed (7A-C + hotfix + 7D/7E, cumulative)**: RLS enabled on all tables (11 from 7A-C + 11 new from 7D/7E: `notes`, `task_handoffs`, `saved_views`, `templates`/`template_tasks`/`template_checklist_items`, `daily_updates`, `accomplishments_reports`/`accomplishments_report_comments`, `client_reports`/`client_report_comments`); `anon` has zero table grants and zero function EXECUTE anywhere (re-verified read-only after every migration batch); every new/replaced function in 7D/7E explicitly `REVOKE EXECUTE ... FROM public, anon` then grants only `authenticated`/`service_role` — the Phase 7A-C-discovered "implicit PUBLIC EXECUTE" lesson was applied inline this time, not as a separate follow-up hardening migration. **Not yet tested with a real end-user JWT** (no password was requested or used) — see the final report's manual smoke-test steps for the precise UI-based verification the user can run themselves.

**Provider-mode strategy**: `supabase-core` is unchanged by Phase 7D/7E, exactly as instructed — it still means Auth + the original 6 core providers real, everything else mock. Full `supabase` mode is the new milestone: with all 14 providers now real, it's a genuinely complete backend, not a placeholder.

**Assignee-hardening limitation — resolved, not just documented.** The Phase 7A-C-disclosed gap (task_assignees INSERT only checked "can this actor edit the task," never "is this specific assignee a legitimate target for the actor's role") is now closed by a single small trigger (`enforce_task_assignee_scope`, `20260814110008`) — see the migration list above.

**Real profile `full_name` data gap — still present, still not something this process can fix.** A read-only aggregate check earlier this phase confirmed 3 of the 4 real development profiles have an empty `full_name` (Supabase's `handle_new_user()` defaults it to `''` when `raw_user_meta_data.full_name` wasn't set at account creation, and the SQL Editor bootstrap only ever set `role`/`supervisor_id`). The `getInitials()` code itself is correct and centralized; this is a **data** gap, not a code defect, and this process does not know the real names and will not fabricate or guess them. **Manual action remaining for the project owner**: set the 3 profiles' `full_name` via the same one-time SQL Editor mechanism already used for the role/supervisor bootstrap, whenever real display names are wanted for a presentation.

**Full `supabase` mode readiness**: all 14 provider groups now resolve to real Supabase implementations under `NEXT_PUBLIC_DATA_PROVIDER=supabase` (verified by reading `src/lib/data/providers/index.ts` — every export uses `usesSupabaseCoreData`/`usesSupabaseData`, both true under `supabase`, and no `src/lib/data/providers/supabase/*` file still throws `notImplemented`). An ephemeral `supabase` build was run and passed clean. **Not yet manually smoke-tested end-to-end by a real user in the browser** — that's the one remaining step before checkpointing all of Phase 7 (7A through 7E plus the hotfix) together.

### Phase 7 final-acceptance hotfix

Two runtime errors under full `NEXT_PUBLIC_DATA_PROVIDER=supabase` ("Task references unknown creator", "Workstream references unknown lead") were root-caused (read-only, no identifiers printed) to `profiles_select` RLS being too narrow for relation hydration: it only ever exposes a viewer's own row or their own direct reports', with no "upward" (my own manager) or "lateral" (a co-assignee, a task's creator who isn't my manager) visibility — so an Employee legitimately viewing a Task/Workstream their Supervisor created/leads could never resolve that Supervisor's profile. Fixed with one new additive migration, `20260814120000_profile_directory.sql`: a `resolve_profile_directory(target_ids)` SECURITY DEFINER RPC returning only `id`/`full_name`/`role`/`supervisor_id` (never email/active/created_at — confirmed by codebase search that no UI reads those off these relation objects) for ids the caller has a legitimate reason to see (self, managed, or co-occurring as creator/status-changer/assignee/lead/member/note-author/handoff-participant on a Task/Workstream/Note/Handoff they can already access). `profiles_select` itself is unchanged — this is additive, not a broadening of the sensitive full-row grant. Centralized in `src/lib/data/providers/supabase/profile-directory.ts` and now used by `supabase-tasks-provider.ts`, `supabase-workstreams-provider.ts`, `supabase-notes-provider.ts`, and `supabase-task-handoffs-provider.ts` (Time Entries was inspected and found not to have this gap — its own RLS is already exactly as narrow as `profiles_select`).

New boss permission rule, implemented in `20260814120001_employee_workstream_creation.sql` plus `permissions.ts`'s new `canCreateWorkstream`: **an Employee may now create a Workstream for a Company they can already access** (Service still required unless Internal; zero-Activities-selected still means zero, never a full-catalog fallback), **choose its Activities, and create their own Tasks under them** (already worked — `tasks_insert`/`task_assignees_write` had no role restriction and the existing assignee-scope trigger already limits an Employee to self-assignment). The only real gap was `workstreams_insert` excluding Employee entirely. The fix requires an Employee-created workstream to name the Employee as its own `lead_user_id` (enforced in the RLS `WITH CHECK`, mirrored in the mock provider) — this is what makes it visible to them afterward without touching `workstream_members` at all. Team/staff assignment (`workstream_members_write`) and later full-record editing (`workstreams_update`) are **unchanged** — still Supervisor/Superadmin only — so an Employee cannot assign other staff or edit an existing workstream's core fields merely because they could create one. **Company/contact administration rights are entirely untouched** — an Employee gains no new Company-edit, contact-edit, or organization-wide Company visibility. Supervisor/Superadmin abilities are unchanged (Supervisor = Employee + team; Superadmin = org-wide).

**This hotfix does NOT implement any UI surface for Employee workstream creation** — no new button/entry point was added to the Company page or elsewhere; that's deliberately deferred to the next roadmap phase (a Zoho-inspired Project/Task UX realignment that will also rework the Employee-facing Company view into a more operational, less admin-styled workspace). This phase only establishes the correct backend permission foundation.

**Phase 7 — ACCEPTED AND CHECKPOINTED.** All of Phase 7A through 7E, the Phase 7A-C hotfix, and the final-acceptance hotfix were manually verified end-to-end by the user (full `supabase` mode, legitimate creators/leads resolve, all three roles work, Employee Workstream-creation permission in place) and committed as **`c63e86d03fafeb516846bb65759cfdacf19ae836`** ("checkpoint: complete Supabase business backend"). Working tree was clean after that checkpoint.

## Phase 8 — Project & Task Management UX Alignment (Zoho-inspired, Sparing-tailored)

An architecture/design audit (no implementation) preceded this phase, recommending a durable hierarchy of `Company (permanent administrative record) → Project (annual client engagement) → Workstream (=Service) → selected Activities → Task → future Subtask → Checklist`, with "Project" as the fixed UI term (never "Engagement" — that word was this repository's own retired name for what is now Workstream).

### Phase 8A — Project / Annual Contract Foundation (backend + read-only surface)

**Status: manually accepted and checkpointed** as `9b5a2d8789f9ea2909b2e37bbb7b368e656c68ee` ("checkpoint: project annual contract foundation").

New migrations, all pushed and verified (Local ↔ Remote aligned):
- `20260815090000_projects.sql` — `projects` (`company_id`, `name`, `owner_id`, `status`, `contract_start_date`, `contract_months` default 12, `contract_end_date` — always stored, never derived, with a `contract_end_date >= contract_start_date` check when both are set) + `project_members` (`project_id`, `user_id`); `can_access_project()` mirroring `can_access_workstream`'s exact shape (superadmin/Internal-always-visible/owner-or-member via `manages_user`); RLS with **conservative writes this slice — Superadmin only** (Project creation/edit UX is explicitly deferred; Employee's new Workstream/Activity/Task creation rights are unrelated and unaffected); `workstreams.project_id` added as a **nullable** transitional FK — `workstreams.company_id` is untouched.
- `20260815090001_projects_backfill.sql` — one Project per existing Company (including Internal/Non-billable), **never fabricating a contract date**: `contract_start_date`/`contract_end_date` are copied directly from `companies.contract_start_date`/`renewal_date` (null stays null — never computed as start + 12 months), and the Internal Company's Project gets null dates unconditionally. Every existing Workstream's `project_id` backfilled from its own Company's new Project, with a safety assertion that fails the migration if any Workstream is left without one. `project_members` seeded from **six** current relationships (not just `user_companies`): Workstream lead, workstream members, task assignees, task creator, task status-changer, and `user_companies` itself — deduplicated, no arbitrary staff added. Verified read-only afterward: every Company got exactly one Project, all Workstreams linked, member counts match real legitimate relationships (one backfilled Project even correctly picked up a Superadmin who had genuinely touched a task there during earlier manual testing — the multi-relationship backfill design was validated by this, not just assumed).
- `20260815090002_profile_directory_projects.sql` — extends `resolve_profile_directory` (the Phase 7 final-acceptance hotfix's RPC) with Project owner/member branches, pre-empting the same "references unknown X" bug class for Project relations before it could ever surface.

**Provider/UI**: `ProjectsProvider` (`listProjects`/`getProject` only — read-only this slice) implemented for both `mockProjectsProvider` and `supabaseProjectsProvider`, wired into `providers/index.ts` the same way as every Phase 7D/7E provider (mock under `supabase-core`, real only under full `supabase`). New read-only routes `/dashboard/projects` (list: search, contract period, owner, status, progress, open/overdue task counts — no Board view, no creation, no complex filtering) and `/dashboard/projects/[id]` (minimal: name/company/contract period/owner/status/team count/Service count/Task summary — no tabs, no Contacts, no Company admin data). **Not yet linked from the sidebar** — reached directly for manual testing ahead of the Phase 8B navigation migration.

**Known, deliberate scope boundaries for 8A** (not gaps, decisions): `can_access_project` does not (yet) grant access via incidental task assignment the way `can_access_workstream` does — Project membership is meant to be the real, explicit relationship, and the 6-relationship backfill was specifically designed to avoid needing that broader branch on day one. The Project provider's workstream/task rollups join through `workstreams.project_id` in the real provider and through `companyId` equivalence in the mock (safe today since every Company maps to exactly one Project — the mock's `Workstream` type deliberately was not given a `projectId` field this slice, to avoid rippling a change through every Workstream call site for a read-only surface). Company pages/provider/data are completely unchanged.

**Next planned slice: Phase 8B** — Project workspace (tabbed) + role-aware navigation (Projects in the sidebar for Employee/Supervisor, Companies staying Superadmin-only) + the Employee "+ Add Service" UI + Task Center Employee access.

### Phase 8B — Employee-First Project Workspace

**Status: manually tested and accepted by the user, checkpointed** as `77dbab9ed37766541aa94ddd053cae76a7e9dc40` ("checkpoint: employee-first project workspace"). Not pushed to GitHub — the user intends to push only once the full Phase 8 series (8A–8E) is complete and accepted.

**Locked principle**: Corebridge X is Employee-first. Every nav/permission decision defaults to "what does an Employee need to do their own work"; Supervisor is Employee + direct-report/team privileges, never a separate manager-only experience; Superadmin stays primarily administrative but can still act operationally where existing rules already allow it.

- **Navigation**: sidebar now shows Projects to every role. Companies is Superadmin-only (Employee/Supervisor never see the link). Tasks is open to every role (no more Supervisor/Superadmin gate). `/dashboard/companies` and `/dashboard/companies/[id]` redirect Employee/Supervisor to `/dashboard/projects` at the route level (a `useEffect` + `router.replace`, mirroring the existing `dashboard/layout.tsx` pattern) — visiting the URL directly is blocked, not just the sidebar link.
- **Project workspace** (`/dashboard/projects/[id]`): a real 4-tab workspace (Overview / Tasks / Services / Team) built as a local button-group tab-switcher (no generic `Tabs` primitive exists yet in this design system). Overview shows operational info only (name, client, status, contract period, owner, team count, task progress, open/overdue) — no Contacts/Company-admin/edit controls. Services lists every Workstream under the Project (employee-facing term "Services") with a Superadmin/Supervisor/Employee-gated "+ Add Service" button. Tasks reuses `TaskRowList`. Team is read-only, listing Project members via the same `resolveProfileDirectory` pattern used elsewhere. Time/Reports/History tabs deliberately omitted — no real data to show yet.
- **Employee "+ Add Service"**: reuses the existing `WorkstreamFormDialog` (no duplicate form) with a new `projectId` prop. `canCreateWorkstreamInProject` (new, mirrors `canCreateWorkstream` but Project-based) gates it: Supervisor/Superadmin unconditional, Employee requires `canAccessProject`. An Employee-created Service always self-leads (server-enforced, not just hidden UI).
- **Workstream → Project linkage**: `Workstream.projectId: string | null` added to the shared type/provider contracts. A new forward-only migration (`20260815100000_workstream_project_link.sql`) adds an `enforce_workstream_project_link` BEFORE INSERT trigger on `workstreams`: when a `project_id` is given it forces `company_id` to match (closing "pass an unrelated company_id" for every role); when omitted it auto-resolves from `company_id` only when that Company has exactly one Project (true for every Company today), raising otherwise rather than guessing. The mock provider mirrors this exactly (`resolveProject` in `mock-workstreams-provider.ts`). Every creation call site was audited: the Project workspace's own "+ Add Service" now always passes `projectId`; the legacy Company-page "Add Workstream", Apply Template, and Generate-Next-Occurrence flows all continue to omit it and rely on the trigger/mock's auto-resolution — Generate-Next-Occurrence was additionally updated to carry the source Workstream's own `projectId` forward explicitly (strictly more correct than re-deriving it, and future-proof once a Company can have more than one Project).
- **Activity + Add Task / global Add Task**: the existing per-Activity "+ Add Task" on the Workstream detail page (`workstream-activity-tasks.tsx`) was already correctly self-assign-scoped for Employees before this phase (verified by reading `mockTasksProvider.createTask`'s `resolveAssigneeIds`, unchanged). The global `TaskFormDialog` (used by `/dashboard/tasks`, both dashboards, and My Day) had its "Client → Workstream" picker replaced with "Project → Service", scoped through `useProjects()`/`useWorkstreams({ projectId })` — Employees only ever see Projects/Services they can access, matching the same real access boundary (RLS / `canAccessWorkstream` / `canCreateTask`'s self-assign enforcement) that already existed.
- **Company read dependency (disclosed, not closed this slice)**: `can_access_company` was extended with two new EXISTS branches (Project ownership, Project membership) so an Employee/Supervisor who is a legitimate Project member — but has no `user_companies` row — can still read their own Project's Company name (needed by the Services tab and "+ Add Service"). This is a real, intentional transitional dependency: Task/Workstream/Project hydration still reads Company identity data directly rather than through a Project-only view. Mock's `canAccessCompany` was **deliberately not extended to match** — doing so would require changing its fixed 3-argument signature used at 6+ call sites, and mock's `seedProjectMembers` was derived inclusively from `assignedCompanyIds` among other relationships, so the gap it would close doesn't actually manifest in mock seed data today. This is a disclosed mock/real fidelity gap, not an oversight.
- **Supervisor**: retains the complete Employee experience (own Projects, own Tasks, own Add Service/Add Task) plus Team Updates, Team Time, and team-scoped Task/Project visibility — never forked into a manager-only view.

**Phase 8B acceptance hotfix — Service/Task creation RLS bootstrap bug (`20260816090000`, `20260816100000`).** Manual testing under real `supabase` mode found Employee/Supervisor Service and Task creation both failing with a generic RLS violation. Root-caused (read-only, role-simulated, rolled back) to a Postgres command-visibility gap: `workstreams_insert`/`tasks_insert`'s own WITH CHECK predicates were never wrong, but the client's `.insert({...}).select("*").single()` pattern also implicitly re-SELECTs the brand-new row for RETURNING, and a self-referential RLS branch (querying the very table/row the command is still inserting) can never see that row within the same command — Superadmin only "worked" because `is_superadmin()` short-circuits before ever reaching that branch. Fixed with two new atomic `SECURITY DEFINER` RPCs, `create_workstream`/`create_task`, replacing the client's direct insert + follow-up join-table-write sequence — the whole create happens inside the function (bypassing the problematic RETURNING-time SELECT check entirely), never leaving a partially-created record. Also closed in the same pass: `client_contacts_select` narrowed to Supervisor-or-Superadmin (it had inherited Phase 8B's Project-membership `can_access_company` broadening, newly letting an Employee Project member read real Contacts nothing in the UI ever displays to them), the Activity picker got dynamic "Available {Service} Activities" wording, the qualifier field became visibly a real input, and Employee-facing Service terminology ("New Service"/"Create service"/"This service recurs") was finished in `WorkstreamFormDialog`.

**Phase 8B final hardening pass (`20260817090000`, `20260817100000`, `20260817110000`).** Three more issues found and closed before manual re-acceptance, none of them regressing any working flow:
- `create_workstream` had grouped `is_supervisor() OR is_superadmin()` together, trusting `lead_user_id`/`team_user_ids`/`project_id`/`company_id` completely as sent for a Supervisor caller — meaning Supervisor (not just Superadmin) could name any org member as lead/team or target a Project outside their own scope. Now Supervisor gets its own branch: `project_id` required, `can_access_project` enforced, lead/team restricted to self-or-active-direct-reports only, company always derived from the given Project (never trusted from the browser) — Superadmin remains organization-wide, including the legacy no-project Company-page flow.
- `create_task` checked "allow unassigned" before the Employee branch — a direct (non-UI) RPC call from an Employee session could have produced a genuinely unassigned self-added Task. The Employee branch is now checked first and unconditionally forces self-assignment regardless of what's requested; Supervisor/Superadmin's legitimate "allow unassigned" bulk-creation flows (Quick Add from Activity, Generate Next Occurrence) are unaffected.
- `client_contacts_select`/`_write` narrowed again, from Supervisor-or-Superadmin to **Superadmin-only** — Contacts are Company *administrative* data, and Supervisor's extra privileges over Employee are team/operational privileges, never Company administration.
- A genuinely pre-existing, previously-undiscovered bug was also found and fixed while rollback-safe-simulating these paths: `enforce_workstream_project_link`'s project-omitted auto-resolve branch used `min(id)` over a `uuid` column — Postgres has no built-in MIN aggregate for `uuid`, so that branch had been raising `function min(uuid) does not exist` for its entire life (uncaught because every Project-aware flow built since always supplies `project_id` explicitly). Replaced with a plain `order by id limit 1`.
- The real Supabase Activity Catalog was expanded from the mock source of truth (`seed-activities.ts`/`seed-departments.ts`), additively and idempotently (case-insensitive per-department name matching so e.g. the existing real "Payroll Processing" is recognized as the mock's "Payroll processing" and never duplicated): Human Resources 2→18, Payroll 2→19, Accounting 2→22 activities; Compliance and File Management departments (0→13, 0→10 activities) created fresh, since they didn't exist in the real schema at all before. Tax/IT-Digital/Consulting have no mock departments/activities to mirror at all — nothing was invented for them. `workstream_activities` (every existing Service's selected subset) is completely untouched — zero rows still means zero, nothing new was auto-attached to any existing Service.
- All three RPC/RLS changes were verified via a full rollback-safe role-simulated probe suite (Employee/Supervisor/Superadmin creation scope, Contacts read/write) before pushing — every probe passed, no UUIDs/tokens ever printed, no rows left behind.

### Phase 8C — Employee-First Task Center Polish + Task Drawer

**Status: manually tested and accepted by the user, checkpointed** as `ddc8da638205eb34436060dc003622e8a44b36c9` ("checkpoint: employee task center and drawer"). Not pushed to GitHub.

- **Task Center default/header**: `/dashboard/tasks` keeps its existing role-scoped query (Employee's own, Supervisor's own+team, Superadmin org-wide — unchanged access boundary) but gains a compact quick-status pill row (All/Todo/In Progress/Blocked/Waiting/Done, plus independently-toggleable **Running**/**Overdue** chips) with a visibly stronger selected state, reusing the same `color-mix` tint language `StatusBucketButton` (My Day) already established. Running/Overdue are derived (active-timer lookup, due-date comparison) — never new stored `TaskStatus` values, matching the locked rule. List/Board toggle, Group By, and "+ New Task" stayed in the header; filters/Saved Views stayed in their own row below — no new Popover primitive was introduced (would have been the larger, riskier option for a "polish" pass).
- **Filters/grouping extended**: `TaskFilters`/`SavedViewFilters` gained `projectId`/`activityId` (optional on the saved shape, so a pre-8C saved view still round-trips as "all"); `TaskGroupBy` gained `"project"`. Search now also matches Project/Client/Service/Activity name, not just the Task title. Employee's Assignee filter/groupBy option is hidden (no team of their own to filter by); Supervisor/Superadmin keep it.
- **List view**: reused, not rewritten — its own `<Table>` gained a combined "Project → Service → Activity" column (replacing separate Company/Workstream columns) and an overdue-styled due-date cell; a Running task shows a small inline play icon by its title.
- **Board view**: unchanged 5-status columns; `TaskCard` gained due date + activity + a "Running" cue, `TaskBoard` gained optional `runningTaskId`/`onOpenTask` props (default behavior for any other future caller is unchanged).
- **Task Drawer** (the phase's primary feature): `TaskDetailContent` was extracted from the full `/dashboard/tasks/[id]` page (Details/Status/Checklist, Assignees, Time Tracking, Handoffs, Notes — every section already backed by a real provider, no fake tabs) so the new `TaskDrawer` and the full page render the exact same body, never diverging. The drawer is a wide (`sm:max-w-3xl`) Sheet — the same primitive every other dialog in this app uses — opened via a `?task=<id>` query param the Task Center's own row/card clicks set through `router.push`; closing calls `router.back()` when this page instance pushed that entry, or strips the param via `router.replace()` for a direct/shared deep link. This was chosen over a heavier parallel-routes/intercepting-routes setup as the smallest robust option: a refresh just re-derives the same drawer state from the URL, the full route is never bypassed, and Back closes the drawer predictably in the common case. `TaskRow`/`TaskGridCard` gained an optional `onOpen` prop (opt-in — every other embedding of these shared components across dashboards/Company/Project/Workstream pages is completely unchanged, still Link-navigating to the full page).
- **Global "+ New Task" — contextual Activity extension (Sections 25-33)**: an Employee/Supervisor Service lead can now pick a catalog Activity that isn't yet configured for the selected Service directly from the Task form ("+ Add another Activity to this service"), scoped to that Service's own service line (never another Service's activities). The choice is staged client-side only ("will be added to this service") — nothing is written until "Create" is pressed.
- **Atomic enable + create**: `create_task` (`20260818090000_task_activity_extension.sql`) now checks, when the requested Activity isn't yet enabled for the Workstream, whether the caller may extend that Service's Activities (Employee: Service lead only; Supervisor: lead is self-or-direct-report AND Project in their own scope; Superadmin: org-wide — the exact same rule as the hardened `workstream_activities_write` policy below), enables it, then creates the Task — all inside the one `SECURITY DEFINER` function call. A forced mid-function failure (invalid `status`) was used to prove the enable is rolled back with the rest of the call — verified via a rollback-safe probe, zero orphan `workstream_activities` rows survive a failed create. The mock provider (`resolveActivityForTaskCreation` in `mock-tasks-provider.ts`) mirrors this exactly for frontend-only testing. Once enabled, the Activity is real — it shows under Project → Services → Service → Activities and in every future Task creation immediately, no reload required (same hooks/provider refresh already in place).
- **`workstream_activities_write` hardened** in the same migration — it had unconditionally allowed any Supervisor to reconfigure any Workstream's Activities organization-wide since Phase 7/8B; now scoped to Supervisor's own team (Service lead is self-or-direct-report) and own Project access, mirroring `create_task`'s own internal check. Employee (lead-only) and Superadmin (org-wide) were already correct and are unchanged.
- **No multi-Activity Task**: the extension only ever sets the Task's existing single `activityId` — no `activityIds[]` was introduced, matching the locked one-Task-one-Activity rule.
- Verified via a full rollback-safe role-simulated probe suite (13 checks: Employee lead/non-lead, cross-service rejection, self-assign-only, Supervisor own/direct-report/unrelated scope, Superadmin org-wide, and the forced-failure atomicity proof) before pushing — every probe passed, no UUIDs ever printed, no rows left behind.

**Phase 8C running-filter hotfix.** Manual testing found the Running quick filter showed nothing after starting a timer from the Task Drawer, even though the Task correctly flipped Todo → In Progress. Root-caused to two real bugs, both purely client-side (no database/RLS involved):
- **Stale running-timer state**: `TasksPage` and `TaskTimeTracking` each hold their own independent `useRunningTimer()` instance — starting a timer only refreshed `TaskTimeTracking`'s own local copy; nothing told `TasksPage`'s copy (the one `runningTaskId`/the Running badges/the Running filter actually read) to refresh, so it stayed whatever it was when the page first mounted (typically `null`). Fixed by threading a new optional `onTimerChanged` callback: `TaskTimeTracking` → `TaskDetailContent` → `TaskDrawer` → `TasksPage`'s own `useRunningTimer().refresh`, fired after every successful Start/Pause/Resume/Stop (previously only Start called back at all, and only for the unrelated `onTaskChanged`/status-refresh purpose). The full `/dashboard/tasks/[id]` route has no separate Running view to invalidate, so it simply omits the now-optional prop — nothing there changed.
- **Filter-order/semantics gap**: the Running toggle was fully independent of the stored status pill, so a task moving between statuses in the background (exactly what starting a timer does) could still fail a lingering non-"all" status filter before the Running filter ever ran. Fixed by making the two mutually exclusive by construction rather than by filter-ordering: picking any stored status pill (or "All") now always turns Running off first; turning Running on always resets the status filter to "All" first; applying a Saved View that carries a non-"all" status does the same. Overdue remains independently combinable with any status, unchanged, since a task can genuinely be overdue in any status.
- Verified end-to-end (real `supabase` mode reasoning + code trace, since this is pure client state — no server round-trip to role-simulate): Start/Pause/Resume/Stop from the Drawer now update the Task Center's Running count/badges/filter immediately in both List and Board, with no reload; the full Task page's own timer controls are unaffected; mock mode needs no separate handling (the fix is entirely in the React hook/prop layer, not the provider). No new migration — this was correctly a client-state bug, not a database issue.

### Phase 8D — Planner

**Status: manually accepted, checkpointed as part of "checkpoint: complete phase 8 project operations."**

- **A dedicated `/dashboard/planner` route**, distinct from both the Task Center ("what work exists") and My Day ("what do I personally need today") — Planner answers "when is work planned, across time." Added to the sidebar for every role, right after Tasks.
- **Four views** (Day/Week/Month/Group), a local button-group tab switch (the same pattern the Project workspace's own tabs and the Task Center's List/Board toggle already use — no new generic `Tabs` primitive). View, selected date, week/month anchor, and Group-By dimension are all local page state, so switching views mid-session never resets position.
- **Date source**: `Task.dueDate` only — no new "planner date" field was added. A Task with no due date is never hidden; it appears in a calm, collapsed-by-default "Unscheduled" panel shared by Day/Week/Month (Group view already shows every Task, unscheduled included, inside its own groups).
- **Timezone-safe date arithmetic** (`src/lib/planner-dates.ts`): every date-only `YYYY-MM-DD` value is parsed/formatted through `Date`'s *local* year/month/day constructor and getters only — never `new Date(string)` or `.toISOString()`, both of which interpret/convert through UTC and can silently shift a date-only value onto the previous or next day depending on the browser's timezone offset.
- **Own-work-first scope**: Employee's list is already exactly their own (unchanged provider scoping); Supervisor's Planner defaults to "My work" (their own assigned Tasks) with an explicit "Team" toggle for their legitimate direct-report scope — never the reverse; Superadmin sees the organization-wide list with no toggle needed. Assignee filtering/grouping is hidden entirely for Employee (no team to filter by).
- **Filters**: a smaller, planning-oriented set reusing the existing `TaskFilters`/`filterTasks`/`TaskFilterBar` infrastructure — Project/Service/Activity/Priority/Assignee(role-aware)/search. No separate filtering backend, no Status quick-filter row (Planner is deliberately calmer than the Task Center), though Running/Overdue still appear as inline cues on each Task item.
- **Task interaction**: every view's Task item opens the exact same `TaskDrawer` Phase 8C already built (no second Task detail surface) — clicking a Week/Month day header/cell instead jumps to Day view for that date.
- No new database migration — Planner is a pure read/reshape of Tasks the provider already returns.

### Phase 8E — Dashboard Drill-Down + Project/Annual Contract Polish (final Phase 8 slice)

**Status: manually accepted in full (after four hotfix rounds), checkpointed as part of "checkpoint: complete phase 8 project operations."**

- **Dashboard card interaction rule fixed**: `StatCard` (every KPI tile on all three dashboards) previously made its *entire* body a single expand/collapse toggle, with no direct navigation at all. It now separates the two: the main card body navigates straight to `viewAllHref` when one is provided (a plain non-interactive card when it isn't — never a dead-end), while a small separate corner button (a sibling of the navigating `Link`, not nested inside it — nesting a `<button>` inside an `<a>` is invalid HTML) independently toggles the expand preview. This one shared-component fix applies to every dashboard's KPI row at once.
- **Newly-linked "dead click" cards** (real underlying data, previously no navigation at all): `TaskStatusDonut`'s legend rows now link to `/dashboard/tasks?status=<status>` (reusing the exact deep-link param Phase 8C already established); `TeamWorkloadCard`'s per-member rows now link to `/dashboard/tasks?assignee=<id>` (a new, small, one-line seeding addition to the Tasks page's existing "seed filters from URL params on mount" pattern); `BrandSnapshotCard`'s per-brand rows now link to `/dashboard/companies?brand=<id>` (the Companies list already had brand-filter state, just never a URL-seeded default — same treatment as its existing `?health=` param); `ClientHealthOverviewCard` now also lists/links "Needs attention" companies, matching the "At risk" bucket's own pre-existing treatment exactly. Superadmin's "Total staff" KPI intentionally stays non-navigable — no staff/admin list page exists yet to send it to, and inventing one wasn't in scope for this pass.
- **Known, disclosed, not-fixed-this-phase gap**: `ClientHealthOverviewCard`'s company links point to `/dashboard/companies/${id}`, which redirects Supervisor away (Company admin pages are Superadmin-only per Phase 8B) — this pre-existed before this pass (the "At risk" bucket already had it) and is unchanged by this pass; resolving it would need a Company→Project id mapping this card doesn't currently have in scope.
- **Project list Service summary** (`/dashboard/projects`): `ProjectWithRelations` gained `services: {id, name}[]` (both providers) — the list now shows up to 2 Service name badges per Project plus a "+N more" overflow, never the full Activity hierarchy per row.
- **Superadmin-only Project creation** (`+ New Project` on the list page) and **Project editing** (`Edit Project` on the workspace page) — `ProjectFormDialog` mirrors `WorkstreamFormDialog`'s Sheet/form/FormSection shell exactly. Employee/Supervisor still cannot create or edit a Project, even though Supervisor already manages Companies operationally elsewhere — Project administration is narrower than `canManageCompanies`, gated by a new `canManageProjects` (Superadmin-only).
- **Annual contract defaults**: Start/Duration(months, default 12)/End are all shown as genuinely separate, independently-editable fields — `contract_months` only *suggests* an end date (`start + months`, with a one-click "Use suggested" hint), it never silently overrides a manually-set `contract_end_date`, which remains what it always was: a plain stored column, never derived/generated. Project name auto-suggests `"{Company} {startYear}-{endYear}"` (mirroring the exact convention the Phase 8A backfill already established) until the user types their own name; Internal/Non-billable Projects are unaffected (no start date exists to suggest a name or a period from).
- **Superadmin-only Project renewal** (`Renew Project` on the workspace page, `ProjectRenewalDialog`): an explicit review/preview screen, never a blind duplication — new name/dates/owner/members are all pre-filled-but-editable, and every one of the source Project's current Services is an individually-uncheckable (pre-checked) carry-forward candidate. New migration `20260819090000_project_admin_rpcs.sql` adds two `SECURITY DEFINER` RPCs:
  - **`create_project`** — Project row + initial members in one atomic call (the same atomicity rationale as `create_workstream`/`create_task`: a partial "Project exists but has none of its intended members" state must never be reachable from the client).
  - **`renew_project`** — creates a brand-new Project under the *same* Company; the source Project is never mutated. Only the explicitly selected Services are carried forward, each as a genuinely new `workstreams` row: name/description/service_line_id/brand_id/selected `workstream_activities` copy; lead carries forward only if still active (else falls back to the new Project's owner); team members copy only the still-active ones; `start_date` resets to the *new* contract's own start (never the old year's stale start), `end_date` is left blank for review (never silently carrying a stale prior-year end date), `recurrence_frequency`/`recurrence_custom_interval_days` carry forward as genuine ongoing Service configuration but `recurrence_anchor_date` resets to the new start date rather than keeping a stale historical anchor; `previous_occurrence_workstream_id` is deliberately left null (that field is reserved for the existing intra-Project "Generate Next Occurrence" recurrence-chain feature — a different concept from cross-Project annual renewal); every new Service's `status` always starts `'active'` regardless of the source Service's own status. **Never copied, by construction** (no code path touches them): Tasks, checklist completion, time entries, Notes, Handoffs, status-change history, Reports, notifications, correction history — the source Project remains the sole historical record. A Service id that doesn't genuinely belong to the source Project is silently excluded from the carry-forward set, never processed.
  - Both RPCs independently re-verify `is_superadmin()` internally (`SECURITY DEFINER` bypasses table RLS for their own writes — the function body is the real authorization boundary here); `projects_insert`/`projects_update`/`project_members_write` RLS was already Superadmin-only since Phase 8A and needed no change.
  - Ordinary Project *editing* (no new Services involved) deliberately stayed a plain `.update()` + members resync, not an RPC — matching the existing `updateWorkstream` provider pattern; a superadmin's own `is_superadmin()` RLS short-circuit never re-queries the row being written, so this doesn't hit the RETURNING-time RLS-visibility bug class `create_workstream`/`create_task` were built to avoid, and an RPC here would be pure ceremony.
- **Verified via 29 rollback-safe role-simulated probes** before pushing: Employee/Supervisor correctly rejected from both `create_project`/`renew_project`; Superadmin's legitimate creation/renewal succeeds; a renewed Project carries forward only its explicitly-selected Services and their selected Activities; zero Tasks/time entries/Notes copied (verified against a renewal source Service with real historical data attached); the source Project and its Services are provably unchanged after renewal; a Service belonging to an unrelated Project is silently excluded from carry-forward (cross-project isolation); and an explicit mid-renewal forced failure (injected into a test-only double of `renew_project`, after the *first* Service had already been successfully copied) left zero trace — no new Project row, no orphan copied Service — proving the whole call is genuinely one atomic transaction. No UUIDs/tokens ever printed, no rows left behind.

**Phase 8E hotfix — Dashboard + Project list final polish (still uncommitted, on top of the above).** Phase 8D (Planner) was manually accepted as-is, no changes. Phase 8E's own manual acceptance pass found everything above already correct and left only four final UI polish issues, now fixed — no database/RLS change involved in any of them:
- **KPI drill-down retargeted to the Task Center**: Employee's "My open tasks"/"Due today"/"Overdue" cards previously all pointed at `/dashboard/my-day`; they now point at `/dashboard/tasks?active=1` / `?due=today` / `?overdue=1` respectively (Supervisor/Superadmin's KPI hrefs were already correct and unchanged). `due=today` is a brand-new client-side-only Task Center quick filter (a `dueTodayOnly` boolean mirroring the existing `overdueOnly` pattern, plus a matching "Due today" pill in `TaskStatusQuickFilters`) — not a stored status, no schema change. My Day is untouched and remains the personal "what do I need today" surface, never the generic KPI destination.
- **Inline KPI expansion replaced with a large detail drawer**: `StatCard`'s old `preview`-driven inline-expanding panel is gone; the main card body now only navigates (to `viewAllHref`), and a separate corner Expand button opens the new shared `DashboardDetailDrawer` (a right-side Sheet, ~50vw capped at `max-w-2xl` on desktop, full width on mobile) showing the KPI's real underlying rows. Task-based KPIs render their rows via the new `TaskKpiDetail`/`TaskSummaryItem` (the same row component Planner already uses, relocated from `planner-task-item.tsx` to the shared `src/components/tasks/task-summary-item.tsx` and extended with an optional `showDueDate`); clicking a Task row closes the KPI's own drawer first, then opens the existing page-level `TaskDrawer` — never two overlays stacked. Non-Task KPIs (client/staff lists) reuse the existing `KpiPreviewList` unsliced. Every dashboard now also owns its own `TaskDrawer` instance for this.
- **Bounded dashboard widgets**: `TeamWorkloadCard`, `BrandSnapshotCard`, and `UpcomingDeadlinesCard` previously rendered every row with no cap; all three (plus `RecentNotificationsCard`, `ReportsAwaitingReviewCard`, `RecurringWorkDueCard`, `TeamActivityCard`, and `ClientHealthOverviewCard`, which were already internally bounded but had no way to see past that bound) now cap their card-body preview at ~5-6 rows with a "+N more" affordance and a header Expand icon, both opening the same `DashboardDetailDrawer` with the fuller (still real, already-loaded, never newly-fetched) list. No widget can grow the dashboard unboundedly again.
- **Project list Service badge truncation fixed**: root cause was `Badge`'s own base classes (`w-fit shrink-0 justify-center overflow-hidden`) clipping symmetrically from both ends once a `max-w`+`truncate` was applied directly to the Badge itself, with nothing shrinkable inside it. Fixed by moving the label into an inner `min-w-0 truncate` span (now the Badge just caps at `max-w-40`), giving proper end-only ellipsis; each badge now also shows a compact `"Accounting · Monthly Accounting"` form (new `workstreamCompactLabel` helper in `workstream-name.ts`, swapping the stored `" — "` qualifier separator for `" · "` — presentation-only, no data change) and exposes the full original name via `Tooltip`. Still max 2 Service badges + "+N more" overflow, unchanged.

**Phase 8E second hotfix, corrected — per-widget Focus View, NOT section-level (still uncommitted, on top of the first hotfix; its KPI drawers/`due=today`/bounded widgets/Service-badge fix are all retained unchanged).** An initial pass at this had misread the request and put the Expand control on the numbered *section headings themselves* (e.g. "01 · MY WORKSTREAMS [expand]"), with one large Focus View per section. That was wrong and has been fully reverted: `SectionBreak` is back to a plain, non-expandable organizational divider (no `onExpand` prop at all), and `DashboardSectionFocusView` has been deleted. The correct, now-implemented model:
- **Numbered section headings** ("01 My Workstreams", "02 Today", etc., and their Employee/Supervisor/Superadmin equivalents) are purely organizational labels — never expandable, no control of any kind next to them.
- **`DashboardWidgetFocusDialog`** (new, replaces the deleted `DashboardSectionFocusView`) — the ONE Expand destination for an individual bounded widget: a near-full-screen `Dialog`, `~94vw/90vh` on desktop (capped `1680px`×`980px`), near-full-screen on mobile, Dashboard dimmed/mounted behind it. Deliberately much larger than the KPI cards' own `DashboardDetailDrawer`, which is unchanged and untouched — the two serve different purposes (KPI = a metric's underlying rows; widget Focus = a whole card's fuller real data) and are never confused.
- **The 8 already-self-contained list widgets** (`TeamWorkloadCard`, `BrandSnapshotCard`, `UpcomingDeadlinesCard`, `RecentNotificationsCard`, `ReportsAwaitingReviewCard`, `RecurringWorkDueCard`, `TeamActivityCard`, `ClientHealthOverviewCard`) each already had their own bounded preview + header Expand + drawer from the first hotfix — repointed in place to open `DashboardWidgetFocusDialog` instead of the small Sheet-based `DashboardDetailDrawer`. One Expand icon each, same as before, just a bigger destination. No dashboard file needed any change for these — each manages its own expand state internally.
- **Newly given their own bounded-preview + header Expand + `DashboardWidgetFocusDialog`** (previously unbounded, inline dashboard JSX with no expand at all): Employee's "My Workstreams" grid (capped 6, `WorkstreamOverviewCard` unchanged) and "My Tasks" card (capped 6, now wired to the real `TaskDrawer` via `onOpen` instead of Link-navigating); Employee's and Supervisor's "Time this week" card (previously fixed-content only, its Focus View adds a real week-entries breakdown from already-loaded time-entry data); Supervisor's own "My Tasks" card (capped 6, `TaskDrawer`-wired). Superadmin has no personal task/workstream section, so nothing new was needed there.
- **Nested-overlay sequencing preserved**: a Task row inside any widget's Focus View (My Tasks, Time this week) closes that Focus View before opening the real `TaskDrawer` — same pattern the KPI drawers already use. Workstream/Company/report rows still just navigate away directly.
- No new backend infrastructure, no configurable "Add Widget"/Clone/Delete affordances, no database/RLS change.

**Phase 8E third hotfix — widget Expand made permanent, not overflow-dependent (still uncommitted, on top of both prior hotfixes; every KPI-drawer/`due=today`/routing/Project-renewal/Planner/role-scope/`DashboardWidgetFocusDialog`-size behavior above is retained unchanged).** Manual testing found every widget's header Expand button was still gated on `rows.length > MAX_ROWS` / `overflow > 0` — so a widget with only 1-2 records (e.g. a 2-person team's "Team Workload") showed no Expand at all, even though the Focus View itself worked correctly once visible. Root cause: the same conditional that correctly gates "+N more" (overflow-only) had also been wrongly wrapped around the header `CardExpandButton` in all 8 shared list widgets plus the dashboard-inline "My Workstreams"/"My Tasks" widgets. Fixed by unconditionally rendering the header Expand on every expandable widget, in every one of: `TeamWorkloadCard`, `BrandSnapshotCard`, `UpcomingDeadlinesCard`, `RecentNotificationsCard`, `ReportsAwaitingReviewCard`, `RecurringWorkDueCard`, `TeamActivityCard`, `ClientHealthOverviewCard`, Employee's "My Workstreams" (now wrapped in a proper `Card`/`CardHeader` so it reads as a coherent widget, matching the others, instead of a bare header row above a grid), Employee's and Supervisor's "My Tasks". "+N more" itself is unaffected — still shown only when there's real overflow. Also added Expand (previously entirely absent) to Employee's "My Tasks by Status" and Supervisor's "Team Tasks by Status", each opening a new shared `TaskStatusFocusContent` (donut + the same viewer-scoped tasks grouped by status, reusing already-loaded data, no new fetch). Every widget's Focus View now also renders a proper empty state (e.g. "Nothing due for regeneration right now.") when it has zero rows, so Expand is meaningful even with no data. Audited: Superadmin has no per-dashboard-inline widgets beyond the 8 shared components, so no separate Superadmin-specific fix was needed there.

**Phase 8E fourth (final) hotfix — `DashboardWidgetFocusDialog` made genuinely near-full-screen.** The one remaining manual-acceptance blocker: the widget Focus View, while functionally correct, still visually read as "a medium modal," not the intended "temporary full-page workspace." Root cause was the fixed desktop caps chosen in the second hotfix (`max-w-[1680px] max-h-[980px]`) — deliberately conservative at the time, but genuinely too small on an ordinary 1920px+ monitor — compounded by a latent, never-triggered risk in the base `DialogContent` primitive: its own `sm:max-w-sm` sits in a *different* Tailwind modifier group than an unprefixed `max-w-[…]` override, so simply passing a new unprefixed `max-w-*` (as the second hotfix did) does not by itself neutralize that `sm:`-scoped default — `cn`/twMerge only dedupes classes within the *same* modifier group. Fixed by dropping both fixed caps entirely and sizing purely off the viewport — `w-[calc(100vw-1rem)] h-[calc(100dvh-1rem)]` (mobile) and `sm:w-[calc(100vw-2rem)] sm:h-[calc(100dvh-2rem)]` (desktop/laptop, ~16px margin per side) — with explicit `max-w-none!`/`sm:max-w-none!` overrides in both modifier groups so the base primitive's defaults can never silently reassert themselves regardless of Tailwind's internal class-generation order. Header (title + close X) and optional "View all" footer stay `shrink-0`; only the content region scrolls — unchanged from before. No other Focus View content, widget bounding, or Expand-visibility logic was touched by this pass.

Phase 8D and Phase 8E (including all four hotfixes) have both now been **manually accepted in full** — Phase 8 is complete, checkpointed as `a77785c57c3e5424fa63b4221ee9523a688ffae9`, and pushed to GitHub by the user.

## Phase 9 — Reporting Automation

### Phase 9A — Reporting Automation Audit & Architecture

**Status: COMPLETE. Read-only — no code, migrations, or docs beyond this record were touched during the audit itself.** Inventoried the two existing, easily-confused report systems (the internal, fully-attributed **Accomplishments Report** at `/dashboard/reports` — `accomplishments_reports` table, `kind: "person" | "client"` — vs. the true, name-free **Client Report** at `/dashboard/reports/client` — its own `client_reports` table, Supervisor/Superadmin-only, Company-scoped, no `project_id`), Daily Update's real field set and auto-draft engine, Task/TimeEntry completion-timestamp and correction reliability, PDF/CSV export mechanics, and the full report permissions/RLS matrix. Diagnosed the reported `accomplishments_reports` RLS failure as almost certainly a feature-identity mix-up — the reproduction steps ("Report for: Client") match the Accomplishments Report's own dialog, not the true Client Report's (which has no such toggle and hard-blocks Employees at the page level) — while separately flagging both report types' raw `.insert().select().single()` generation pattern as a real, pre-existing architectural inconsistency worth fixing regardless. Produced a full Phase 9 slice plan (9B–9F) and a 40-question answer set. Full audit report delivered to the user for review; no implementation occurred in this pass.

### Phase 9B — Reporting Foundation + Client Report Correctness

**Status: COMPLETE — manually accepted by the user.** All Phase 9B acceptance checks passed, including Employee Client Report generation, Internal-vs-Client-Report terminology, Client → Project → range generation, Project scoping, Employee/Supervisor/Superadmin detail access (the earlier detail-page 404 resolved — see below), legitimate Supervisor commenting, generator-only Trash/Restore, Superadmin-only Permanent Delete, legitimate-vs-unrelated Supervisor finalization, Employee finalize rejection, finalized-report immutability (no Reopen/edit, still exports/views), intentional Personal/Internal Report Reopen/edit retention, Project A/B isolation, and PDF/CSV export.

- **Client Report is now Project-scoped, not merely Company-scoped.** `ClientReport` gained `projectId: string | null` (`client_reports.project_id`, new migration `20260820090000_client_reports_project_scope.sql`) — a Company can have several annual Projects (e.g. "Alderleaf Manufacturing 2025-2026"/"...2026-2027"), and Task/time evidence for a report is now gathered strictly through a Project's own Workstreams (`workstream.project_id = report.project_id`), never through the whole Company — closing the "two annual contracts' work gets silently mixed" risk. The one real legacy row (the seeded Alderleaf Manufacturing demo report) was backfilled **deterministically** — only because its Company had exactly one Project at migration time — never guessed; a Company with zero or several Projects would have been left `null` rather than fabricating an association. Legacy rows with a null `projectId` continue to load/view/export/trash unaffected.
- **Generation moved from a raw INSERT onto SECURITY DEFINER RPCs**, for both report types (`generate_client_report`, `generate_accomplishments_report` — new, `20260820090000_client_reports_project_scope.sql` and `20260820100000_accomplishments_report_generation_rpc.sql`), closing the known raw-insert/RETURNING-vs-RLS-visibility inconsistency the same way `create_workstream`/`create_task`/`create_project` already did in Phase 8. `generate_client_report` independently validates Project accessibility (`can_access_project`) and derives Company/brand from the Project **server-side** — a browser can no longer submit a mismatched `project_id`+`company_id` pair, because `company_id` is no longer a client-supplied input at all. Accomplishments Report's own generation logic/permissions are otherwise unchanged (still no role restriction on `kind:"client"` — a deliberate, disclosed carry-over pending a future taxonomy decision, not an oversight).
- **Employees may now generate a true Client Report draft**, scoped to a Project they can access — the locked business rule. `canGenerateClientReport` is now Project-scoped (reuses the existing `canAccessProject`, which already gives Employee "their own Projects," Supervisor "+ their team's," Superadmin "everything" — no per-role branching needed). A real, pre-existing bug was also fixed while doing this: `canViewClientReport` checked `isEmployee(viewer) → return false` **before** checking ownership, so an Employee could never see even their own generated report; ownership is now checked first.
- **"Generate draft" and "finalize" are now two separate permissions.** `canEditOwnClientDraft` (owner + still-draft, replaces `canEditClientReportEntries`) governs Save/"+ Add section" — any owner, Employee included. `canFinalizeClientReport` (new) is deliberately conservative and **not owner-based at all** — an Employee can never finalize their own report. **Supervisor finalization is legitimate-team/report-scoped, never organization-wide**: a Supervisor may finalize a Client Report only if they can already legitimately view it (owner, or they manage the report's own generator — reusing the existing `can_view_client_report` helper), plus a Project-dimension defense-in-depth check for Project-scoped reports (`can_access_project`, skipped for legacy `project_id IS NULL` reports). Superadmin remains unconditional/organization-wide. (Two same-slice rollback-safe-probe-driven hotfixes were required to reach this: first, the SQL RPC accidentally also allowed `generated_by = auth.uid()` to satisfy the check, which would have let an Employee-owner finalize their own draft — fixed in `20260820110000_client_report_finalize_fix.sql`. Then a second, more serious review finding: that same fix's replacement predicate was simply `is_supervisor() OR is_superadmin()`, with no report-specific scoping at all — since the function is SECURITY DEFINER, this was an unconditional, organization-wide finalize bypass for **any** Supervisor, not just their own team's. Fixed in `20260820120000_client_report_finalize_scope.sql`, verified by rollback-safe probes using structurally-guaranteed-unrelated temporary fixtures so the negative cases cannot silently skip.) This intentionally does **not** yet build a full "Sparing Efficiency reviewer" concept — no fourth role, no new permission/department attribute — a future reporting slice will replace/refine this interim rule with an explicit, least-privilege Sparing Efficiency review/finalization capability.
- **A finalized true Client Report can no longer be reopened — ever.** `reopen_client_report`'s EXECUTE privilege was revoked from `authenticated` (kept for `service_role` only) via the new migration; the Reopen button/handler and `reopenReport` method were removed entirely from the UI and both providers (mock + Supabase). Trash/Restore/View/Export are unaffected — a finalized report can still be trashed, restored, viewed, and re-exported, it just can never become editable again. Accomplishments Report's own reopen/re-finalize cycle is completely untouched.
- **UX disambiguation**: the Accomplishments Report's confusing "Report for: Person / Client" toggle is now labeled "Personal" / "Internal Client Summary" with explicit copy pointing to the real Client Reports feature for the client-facing document; the page itself is now titled "Internal Reports"; the shared `ReportTypeTabs` now reads "Internal Reports / Client Reports" and is shown to every role (previously self-hid for Employees). No database rename, no table merge — presentation-only.
- **Verified via rollback-safe role-simulated SQL probes** against the hosted database (temp-table result collection, explicit `ROLLBACK` at the end — nothing created/mutated persists, no identifiers ever printed): Employee generate succeeds on an accessible Project and is rejected on an unrelated one; Employee cannot finalize their own draft; a Supervisor *can* finalize their own genuine direct report's draft; a finalized report rejects both a direct edit and a reopen attempt; Superadmin generates and finalizes organization-wide; Supervisor generates within legitimate scope; and a temporary two-Project-under-one-Company fixture proved zero cross-Project task leakage. `reopen_client_report`'s EXECUTE-revoke was independently confirmed via `has_function_privilege`. The finalize-scope hotfix (`20260820120000`) was verified with a second probe pass using **structurally-guaranteed-unrelated temporary fixtures** (a Project owned by Superadmin with zero `project_members` rows, and a report generated on it) specifically so the two negative cases — Supervisor generating on an unrelated Project, and Supervisor finalizing an unrelated report — cannot silently pass by skipping; both executed and were rejected.
- `tsc`/`eslint`/`build` clean in all four provider modes. Both new/replaced RPCs follow the established SECURITY DEFINER hardening convention (`set search_path = ''`, EXECUTE revoked from `public`/`anon`, granted only to `authenticated`/`service_role`).
- **Client Report detail-page 404 (manual repro, since resolved) — root cause was NOT RLS/permissions.** A Supervisor/Superadmin clicking a legitimately-visible Client Report row got Next.js's own framework "404: This page could not be found," not this app's "doesn't exist" message. Extensive rollback-safe SQL probing (using a genuine `set local role authenticated` + JWT-claim simulation, matching exactly what PostgREST enforces per request) proved the database allows the exact real-world case (a Supervisor opening their genuine direct report's report) at every layer the frontend uses — list query, single-row query, and the comments query — with zero denials. The actual cause was a **stale/corrupted Turbopack dev-server route cache**: the long-running local `next dev` process had been sharing its `.next` output directory with several interleaved `next build` (production) runs, which desynced its serving state specifically for the nested `/dashboard/reports/client/[id]` and `/dashboard/reports/client/trash` routes (confirmed by comparing HTTP status codes directly against the running dev server, unauthenticated, before and after a clean `.next` wipe + restart — 404 before, 200 after, for every affected route). No application code, RLS policy, or permission function needed to change for this; do not "fix" it again by broadening `client_reports_select` RLS or `can_view_client_report`. If it recurs, restart the dev server (clear `.next` if a restart alone doesn't clear it) rather than suspecting the permission model.
- **Client Report destructive-action scope hardened** (new migration `20260820130000_client_report_destructive_action_scope.sql`): `trash_client_report`/`restore_client_report`/`permanently_delete_client_report` previously authorized purely via `can_view_client_report` — the same "can I see this" gate used for view/comment/finalize — which meant a Supervisor who could merely *view* a direct report's Client Report could also trash or restore it. Now: **Trash/Restore** — the report's own generator, or Superadmin, only (view/comment/finalize access alone is no longer sufficient); **Permanent delete** — Superadmin-only, always, never the generator regardless of role. New TS permission helpers `canTrashClientReport`/`canRestoreClientReport`/`canPermanentlyDeleteClientReport` mirror this exactly and now gate the Delete/Restore/Delete-forever controls on both the detail page and the Trash list page (previously shown unconditionally to any viewer). Mock provider mirrors the same rule. Verified via a second rollback-safe probe suite: Employee trash/restore own → PASS, permanently delete own → REJECT; Supervisor trash/restore/permanently-delete a genuine direct report's report → all REJECT (view/comment still PASS); Supervisor trash/restore their *own* generated report → PASS; Superadmin trash/restore/permanently-delete any → PASS; the full finalize regression (legitimate Supervisor PASS, Employee REJECT, unrelated Supervisor REJECT, Superadmin PASS) and finalized-immutability guarantees (edit rejected, reopen unavailable, restoring a finalized report leaves it `finalized`, not reverted to draft) all re-confirmed intact.

- **Clean development practice going forward**: the detail-page 404 above was caused by running `next build` while `next dev` shared the same `.next` directory. Stop `next dev` before running any production build against this repo, and restart `next dev` afterward — do not run a dev server and a production build concurrently.

**Not built this slice (deliberately deferred)**: Daily Update Automation, Daily Visit Hours, scheduled/recurring report generation, the multi-day summary-plus-detail-line report structure, and any real "Sparing Efficiency reviewer" capability — all per the explicit Phase 9B scope boundary; each is a later Phase 9 slice (9C onward).

**Phase 9B is COMPLETE and manually accepted.**

### Phase 9C — Daily Update Automation

**Status: COMPLETE — manually accepted by the user ("Phase 9C passed").** Evolves the existing Daily Update foundation (Phase 3.23/3.24, reused as-is — data model shape, RLS, the six-method provider contract, Team Updates' visibility scoping) rather than rebuilding it, per the explicit "do not rewrite from scratch" instruction. Goal: an Employee/Supervisor reviews today's already-tracked work instead of re-typing it.

- **Task-centric aggregation, not one-row-per-event.** `computeFreshEntries` (mock + Supabase providers) now produces **at most one entry per Task per person per day**, regardless of how many Time Entries, status changes, or Handoffs touched it that day — previously a Task with both logged time and a same-day Handoff could produce two separate rows. Actual Time is the sum of that day's legitimate (non-running, `durationMinutes != null`) Time Entries for that Task; a status change or a Handoff alone still surfaces the Task with Actual = 0, since it represents real context that day even with nothing logged. A Handoff never produces its own row anymore — its context (`handoffIds`, and its text folded into `details` when nothing else seeded it) lives on the Task's own combined entry. The merge key for a Task-backed entry is now simply `task:{taskId}` (previously a composite including source/handoff id), directly satisfying "stable merge key should be Task-centric." A legacy `handoff-sent`/`handoff-received`-sourced entry from before this change is dropped as subsumed once a fresh combined entry exists for the same Task (never kept forever as a stale duplicate), and still renders correctly if genuinely orphaned.
- **Full Phase 8 hierarchy captured per entry.** `DailyUpdateEntry` gained (all optional, for legacy compatibility — see below) `projectId`/`projectLabel`, `workstreamId`/`workstreamLabel` (Task → Workstream → Project, resolved the same way Client Report generation already does), and `taskLabel` (the Task's own title, its own field now rather than being baked into `details` — so the person is never asked to retype it). `null` wherever the Task's own Workstream genuinely has no Project (internal/non-client work) — never guessed. This is what lets a future weekly report safely use a Daily Update entry's narrative as Project-scoped supplementary context without risk of mixing two annual contracts, per the locked Phase 9C requirement.
- **Actual Time renamed to `actualMinutes` (canonical), `minutesLogged` kept read-only for legacy rows.** A new `getEntryActualMinutes(entry)` helper (`src/lib/data/types/daily-update.ts`) reads whichever field is present — every call site (both providers, both report generators that consume confirmed Daily Update entries) uses it rather than either field directly, so old stored JSON missing the new field still renders/aggregates correctly.
- **Scheduled Time — deliberately never auto-derived.** `DailyUpdateEntry.scheduledMinutes` is a new, purely user-editable field. `Task.expectedMinutes` is a whole-Task estimate, not a per-day plan, and Planner schedules dates, not per-day effort — there is no trustworthy per-day planned-effort source in this codebase today, so auto-created rows begin blank/null rather than guessing (dividing `expectedMinutes` across days, or otherwise). Once set, it survives every draft refresh exactly like `details` does (the merge logic explicitly carries both forward from the prior entry, never overwriting human input with a fresh recompute) — verified end-to-end (see below).
- **Progress stays the real Task status vocabulary** (`TaskStatusBadge`'s own `STATUS_META` labels, not an ad-hoc humanized string) — no second free-text status system invented.
- **My Day / Team Updates UI redesign** (`daily-update-entries.tsx`): each entry row now shows Task/Work, Client/Project, Service (Workstream)/Activity, Scheduled (editable while draft, via a compact minutes-only inline input — the full `ExpectedTimeInput` value+unit+stepper control is too wide for a row cell), Actual (read-only, computed; a small pen icon marks a manual entry's Actual as a fallback value, never a real Time Entry), Progress, and Details — as a responsive grid that reads as a compact table on desktop and stacks naturally on narrow widths, reusing the app's existing card-row aesthetic rather than a literal `<table>` (which doesn't play well with an inline textarea/input per row). Column labels shown once, desktop-only. Every field but Details/Scheduled remains genuinely read-only regardless of role — if a Task-backed row's Client/Project/Service/Activity/Task/Actual/Progress is wrong, the source Task/Time Entry is what needs correcting, not this screen.
- **Manual entry gained Project + Scheduled Time.** `AddManualDailyUpdateEntryInput` now includes `projectId` (the Project select only appears once a Client is picked and has ≥1 accessible Project — genuine internal/non-client work is never forced to pick one) and `scheduledMinutes`; its Actual field is explicitly labeled "A fallback value — not a real Time Entry" and is never merged into a real Time Entry or silently converted into one.
- **Team Lead review — one marker per submitted snapshot, not a new status.** `daily_updates` gained `reviewed_at`/`reviewed_by`/`reviewed_by_name` (the last denormalized at review time, same rationale as `client_reports.generated_by_name` — a Supervisor viewing their own direct report's update has no RLS visibility into a Superadmin's profile if the Superadmin reviewed it organization-wide, so the name can't be resolved client-side from the viewer's own scoped roster). RPC `review_daily_update` (SECURITY DEFINER, `set search_path = ''`, EXECUTE revoked from `public`/`anon`): rejects a still-draft update, **rejects reviewing an already-reviewed submission a second time** (hotfix — the first version of this RPC allowed a second reviewer, or the same reviewer again, to silently overwrite `reviewed_at`/`reviewed_by`/`reviewed_by_name`; fixed with an explicit `existing.reviewed_at is not null` guard, `'This Daily Update has already been reviewed.'`), rejects self-review (including a Supervisor reviewing their own submission — **a Supervisor remains Employee-first for their own Daily Update**, never exempt), permits Superadmin organization-wide, permits a Supervisor only for a genuine direct report (`is_supervisor() AND manages_user(target)` — the same precedent this project's earlier Client Report finalize-scope hotfix established, deliberately not a broader `is_supervisor() OR ...`). `reopen_my_daily_update` (forward `create or replace`, original file untouched) clears `reviewed_at`/`reviewed_by`/`reviewed_by_name` — the submitted snapshot that was reviewed no longer exists once it's back in draft, so a re-submit must be reviewed again, enforced server-side. TS permission `canReviewDailyUpdate` mirrors the RPC's predicate exactly, including the `reviewedAt === null` guard (hotfix) — not `canViewDailyUpdate`'s broader shape, so "can I see it" can never drift into "can I review it," and the UI's "Mark reviewed" button genuinely disappears (not just visually — the same permission gate the RPC enforces) once a review exists. Team Updates' detail pane shows "Mark reviewed" only while reviewable, and "Reviewed by {name} · {time}" / "Not yet reviewed" once submitted, never both. No fourth role, no approval/rejection workflow, no comment thread — a marker only, per the explicit scope boundary.
- **Migrations**: two new forward-only files — `20260821090000_daily_update_review.sql` (the original review marker + RPCs) and `20260821100000_daily_update_review_guard.sql` (this hotfix's `create or replace` adding the already-reviewed guard). Neither edits `20260814110004_daily_updates.sql`. Confirmed the generic `upsert_my_daily_update_draft` path cannot forge or clear review metadata (it has no parameter for those columns at all).
- **User-facing terminology is Submit/Submitted; the database enum/RPC names are unchanged.** "Confirm" → "Submit Daily Update" (button), "Confirmed" → "Submitted" (status badge, Team Updates stat chip, "Submitted at {time}" everywhere it's shown) — internally still `status = 'confirmed'`, `confirm_my_daily_update`, `confirmedAt`; renaming those would have been needless churn for a copy-only change.
- **Missing Scheduled Time — non-blocking Draft attention, not a submit blocker.** While a Daily Update is a Draft, a small warning line appears above the Submit button whenever ≥1 Task-backed row has `scheduledMinutes == null` (an explicit `0` — the person really did schedule zero minutes — is never treated as "missing"; manual/fallback entries are never counted, since they were never going to have a per-day plan behind them). Submission remains fully allowed regardless; the line simply disappears once every Task-backed row has a value.
- **Work-date/timezone handling — fixed (hotfix).** Daily Update "today," and every Time Entry/status-change/Handoff date comparison, now use the viewer's LOCAL calendar date via two new shared utilities in `src/lib/planner-dates.ts` — `dateKeyFromTimestamp(isoTimestamp)` (classifies an absolute timestamp by local calendar day, replacing every `timestamp.slice(0, 10)` in both providers) and `localDayBoundsUtc(dateKey)` (turns a selected local day into a half-open UTC instant range — `startUtc <= timestamp < endUtc` — for the Supabase provider's queries, replacing the old `${date}T00:00:00.000Z`..`23:59:59.999Z` UTC-midnight approximation). Team Updates' `date-stepper.tsx` now uses the same `parseDateOnly`/`formatDateOnly`/`addDays` Phase 8D already established, instead of its own separate UTC arithmetic. Time Entry/Task/Handoff timestamp *storage* is completely untouched — still real absolute `timestamptz`/ISO instants; only how Daily Update classifies them into a calendar work-date changed. Both new utilities build on `addDays`'s existing `setDate`-based (not millisecond) arithmetic, so a DST transition day still produces its correct 23- or 25-hour window rather than a naive fixed 24h one.
- **Verified three ways**, matching what each layer can actually prove: (1) the pure-TypeScript aggregation/merge/review-authorization algorithm — not reachable from SQL at all — was re-verified end-to-end against the mock provider after every hotfix change (27/27 checks passed, run under both `TZ=America/Toronto` and `TZ=UTC` via a bootstrap script that sets `process.env.TZ` before any Date-touching module loads, since this sandbox's shell doesn't propagate an inline `TZ=` prefix to child processes); (2) the local-date fix itself was proven with fixed timestamps under both timezones — `2026-08-21T00:30:00Z` correctly classifies as `2026-08-20` in Toronto (the exact "UTC date ≠ local date" example) and as `2026-08-21` under UTC, `localDayBoundsUtc` produced the exact expected UTC instants for both zones, the half-open contract was independently confirmed (a timestamp exactly at `endUtc` falls on the next local day, one millisecond earlier still falls on the selected day), and the 2026-03-08 Toronto DST spring-forward day correctly produced a 23-hour window; (3) the `review_daily_update`/`reopen_my_daily_update` RPCs' server-side authorization boundary — including the new already-reviewed guard — was verified via rollback-safe role-simulated SQL probes against the hosted database (9/9 checks passed): draft rejected, self-review rejected, a genuinely unrelated Supervisor rejected on relationship grounds *before* any review exists (hosted seed data has only one real Supervisor account, so this used the same structurally-guaranteed-unrelated temporary role-flip technique as the prior finalize-scope hotfix), the legitimate Supervisor's first review succeeds, the same Supervisor reviewing the same snapshot again is rejected by the new guard, Superadmin attempting to overwrite that same reviewed snapshot is rejected by the same guard, owner reopen clears the marker, re-submit is reviewable again, and `anon` EXECUTE remains revoked.
- Client Reports/Internal Reports remain unaffected beyond the one mechanical fix from the base Phase 9C implementation (both report generators' Daily-Update-entry evidence path reads Actual Time via `getEntryActualMinutes(entry)` instead of the renamed `minutesLogged` field directly) — this hotfix touches neither report generator's file at all.
- `tsc`/`eslint`/`build` clean in all four provider modes.

**Not built this slice (deliberately deferred)**: Weekly Client Report generation redesign, the Sparing Efficiency reporting-review capability, scheduled/recurring Client Reports, Daily Visit Hours, and Subtasks — all per the explicit Phase 9C scope boundary; each is a later Phase 9 slice (9D onward).

**Phase 9C is COMPLETE and manually accepted (checkpoint `a2f295f94a40e5c446e371a4b76cac9fcfb7dcdf`), including the final polish/hardening hotfix — local-calendar dates, Submit/Submitted terminology, non-blocking missing-Scheduled-Time attention, and the one-review-marker-per-submission guard.**

### Phase 9D — Weekly Client Report Generation + Multi-day Task Lines + Client Report calculation/export hardening

**Status: COMPLETE, manually accepted (base slice + real-Supabase hotfix).** Extends the Phase 9B/9C true Client Report — every locked Phase 9B guarantee (Project scoping, Employee/Supervisor/Superadmin generation and finalize scope, Draft/Finalized snapshot lifecycle, immutable Finalized state, comments/history, Trash/Restore, name-free exports, the `generate_client_report` SECURITY DEFINER RPC and `window.print()`/CSV mechanics) is reused as-is; only the report *content algorithm* changed, replacing the prior contributor/day-based heuristic with the new locked weekly business rule.

- **Locked qualifying-Task rule**: a Task qualifies for the selected reporting period when `status === "done"` AND its `statusChangedAt`'s LOCAL work date (via Phase 9C's `dateKeyFromTimestamp`) falls within the range — never `updatedAt`/`dueDate`/`createdAt`. Project scoping is unchanged from Phase 9B: only Tasks whose Workstream belongs to the report's own Project ever qualify.
- **Actual duration**: the sum of every legitimate (`durationMinutes != null`, i.e. non-running), in-range Time Entry for that Task on that exact local work date (from `TimeEntry.startTime`) — **every contributor combined, never attributed by name** (multi-employee aggregation: two Employees' time on the same Task/day becomes one client-facing row, never two named rows). A corrected Time Entry's current (already-corrected) duration is used as-is; time outside the selected range never contributes even if the Task itself completed inside it.
- **Multi-day Task lines**: a Task worked across more than one date gets one presentation-only "Weekly summary" row (Task title + total) plus its individual dated detail rows below; a single-date Task shows just that one row with its title as a small caption. **Anti-double-counting is enforced by construction, not convention**: the weekly summary has no stored/independently-editable duration anywhere — `groupLineItemsByTask` (`src/lib/data/client-report-weekly.ts`) computes its total as `sum(items)` live, every time, from the same flat `ClientReportLineItem[]` Service/Activity/report totals already summed from (`client-report-totals.ts`, unchanged). Editing a detail line's minutes therefore immediately and correctly changes the summary, the Service Total, and Total Week Hours — there is no second number to keep in sync.
- **Narrative precedence**: (A) a confirmed Daily Update's Task-backed entry for the same Task + local work date, screened against every known staff full name first (Phase 9C Daily Update narrative can contain human-entered text) — a match is discarded and never used, falling through to (B) the Task's own `description` if genuinely present, else (C) the Task's own `title`. A missing/unsubmitted Daily Update never blocks generation — it only means (A) has nothing to offer that day, not a failure. Task Notes (the internal, casual `TimeEntry.notes`/never a Task's own note field — this app doesn't have one) are never read for narrative, matching the pre-9D algorithm's own rule.
- **Zero-time completed Task**: a `done`, in-range Task with no legitimate tracked time anywhere in range is **omitted from duration-bearing lines** (never a fabricated `0` or dash) and recorded as an internal-only generation warning — persisted as a `"generation-warning"` `ClientReportHistoryEvent` (additive `history` field, no migration) and rendered on the Draft's own History card (`print:hidden`, never CSV/client-facing) so a human reviewer actually sees it, rather than only a `console.warn`.
- **Service/Activity grouping is unchanged and fully dynamic** — still buckets by the Task's own `activityId` → the activity's `departmentId`, from whichever catalog the selected Project's Company actually has (never a hardcoded department list); a Task with no Activity still falls into the existing "Other / Untagged work" fallback.
- **Data shape — additive, no migration.** `ClientReportLineItem` gained optional `taskId`/`taskLabel` (a legacy pre-9D line, and any manually-added line, simply has neither, and renders exactly as before — ungrouped, standalone). No new table, no new column, no `departments` JSON restructuring — the existing snapshot-jsonb architecture represents the weekly output cleanly, so `generate_client_report`'s RPC signature and the `client_reports` table are both completely untouched.
- **New pure module**: `src/lib/data/client-report-weekly.ts` — `computeWeeklyReportSections` (the qualifying-Task/aggregation/narrative-precedence algorithm, zero DB/mock/Supabase dependency) and `groupLineItemsByTask` (the index-preserving view-layer grouping `ClientReportView` uses for editable rows). Both Client Report providers gather their own already-real Tasks/TimeEntries/confirmed-Daily-Update-entries/catalog data (same established flat-fetch-then-JS-join pattern each already used) and hand it to this one shared algorithm unchanged — mock/Supabase parity by construction, not by separately-maintained duplicate logic.
- **Screen/PDF/CSV**: the detail page now shows the Project's own name in its header (a live lookup via the existing `useProject` hook, not a new stored/snapshotted field — deliberately avoiding a migration for a presentation-only need); `ClientReportView`'s per-activity table renders the new weekly-summary-plus-detail-rows structure with `print:break-inside-avoid`, so PDF (still `window.print()`, no new dependency) matches the screen exactly; the report-end total is now explicitly labeled "Total Week Hours" (still `sumAllDepartments`, detail rows only); Finalize confirmation copy now states "later Time Entry corrections will not change this finalized report." CSV gained a `Task` column (`Service, Activity, Task, Date, Duration, Details`) and remains **one row per actual dated detail row** — the weekly summary is never exported as its own duration-bearing row, so a spreadsheet `SUM` over Duration can never double-count.
- **Verified two ways** (base slice), matching what each layer can prove: (1) the pure aggregation/anti-double-counting algorithm — qualifying/non-qualifying Tasks, in-range/out-of-range time and completion dates, multi-Time-Entry summing, multi-Employee combining, multi-day grouping with the summary always equal to the sum of its own detail rows, running-entry exclusion, corrected-duration handling, staff-name-narrative fallback (and the safe-narrative-is-used case), missing-Daily-Update fallback, and untagged-Task "Other" bucketing — was exercised with 27 plain-object unit checks against `computeWeeklyReportSections`/`groupLineItemsByTask` directly (27/27 passed); (2) Project isolation (Project A's report never includes Project B's Task, and vice versa) was verified at the mock-provider level with two temporary Projects/Workstreams/Tasks (4/4 checks passed).
- Daily Update (Phase 9C) is read-only here — Weekly Client Report reads submitted/confirmed Daily Update narrative as supplementary context only and never mutates a Daily Update from report generation.
- `tsc`/`eslint`/`build` clean in all four provider modes.

#### Phase 9D hotfix — real-Supabase multi-contributor evidence + RLS-scoped reporting evidence RPC

**Root issue found on review**: the base slice's "verified two ways" note above was accurate for the pure algorithm and for the *mock* provider, but wrong for the *real Supabase* provider — it assumed the weekly-aggregation algorithm's own evidence-gathering was reachable only client-side, missing that the Supabase provider's evidence-gathering was itself an ordinary authenticated SELECT against `time_entries`/`daily_updates`, whose RLS (`user_id = auth.uid() OR manages_user(user_id)`) is deliberately owner/team-scoped, not Project-scoped. An Employee generating a report for a Project they legitimately access would therefore only ever see their OWN Time Entries on a Task a coworker also logged time against, silently under-counting the coworker's legitimate minutes on that same Task/date — the pure function's own multi-employee-combining guarantee was correct, but the *evidence it was fed* was already incomplete by the time it got there.

- **Fix — new SECURITY DEFINER RPC, `get_client_report_weekly_evidence(p_project_id, p_range_start, p_range_end, p_timezone)`** (migration `20260821110000_client_report_weekly_evidence.sql`): requires `auth.uid()`, re-checks `can_access_project` independently (same check `generate_client_report` already uses), validates the date range and the caller-supplied IANA timezone, then returns only: (a) the Project's own Tasks (id/title/description/status/statusChangedAt/activityId — no Task Notes field exists to leak), (b) Time Entry minutes pre-summed in SQL to **one row per (Task, local work date) across every legitimate contributor** (`group by task_id, (start_time at time zone p_timezone)::date` — contributor identity is structurally absent from the result shape, not merely omitted from a SELECT list; no `user_id`, no notes, no correction history), (c) confirmed Daily Update narrative isolated to one row per (Task, local work date) — no author identity, no Scheduled Time, no review metadata, no entries for any other Task/Project. **Base `time_entries_select`/`daily_updates_select` policies are byte-for-byte unchanged** — this is a narrow, independently-authorized exception, not a broadening.
- **Local-calendar-safe bucketing preserved, not regressed to naive UTC slicing**: Postgres has no notion of "the browser's timezone," so the browser passes its own detected `Intl.DateTimeFormat().resolvedOptions().timeZone` explicitly; the RPC buckets via `AT TIME ZONE`, the SQL-side equivalent of the existing `dateKeyFromTimestamp` rule. All contributors bucket under the *generating user's own* timezone (same "whichever machine runs the aggregation" behavior the mock/pure algorithm already had).
- **`generate_client_report` gained one optional parameter, `p_history` (default `'[]'::jsonb`)**, so generation warnings persist atomically with the report row instead of via a separate follow-up write; the old 5-argument overload was dropped (`create or replace` can't widen a signature in place) so there is exactly one generation path again.
- **Pure module (`client-report-weekly.ts`) evidence-shape refactor**: `ComputeWeeklyReportInput.timeEntries` (raw, per-entry) → `.timeEvidence` (pre-aggregated `{taskId, date, minutes}`, per the new `WeeklyReportTimeEvidenceInput`) — the Supabase provider now gets this straight from the RPC; the mock provider aggregates its own in-memory Time Entries the same way (`dateKeyFromTimestamp`-bucketed sum) before calling in, so both providers converge on an identical evidence shape by construction. The pure function's own qualifying-Task rule, narrative precedence, and anti-double-counting contract are unchanged.
- **Source-metadata truthfulness fix**: `resolveNarrative` now returns `{details, source}` as one atomic decision instead of computing `source` from "did a Daily Update candidate exist" (which couldn't distinguish "used" from "existed but was rejected for naming a staff member, then fell back to the Task description/title") — a rejected candidate is now correctly recorded as `source: "raw"`, never `"daily-update"`.
- **Name-scanner audit**: `resolveNarrative` already calls the exact same `textMentionsStaffName` the existing Client Report finalize scan uses — the Phase 9D automatic narrative path and the finalize-time scan share identical semantics already; no change was needed. Documented precisely (not overclaimed): it matches the full name AND each individual space-separated fragment (first/middle/last) as a case-insensitive whole word (regex word boundary), skipping fragments under 3 characters to avoid noisy false positives on initials. It does not do fuzzy/nickname matching and is a substring/word-boundary check, not context-aware — the mandatory edit-before-finalize review remains the real backstop, as documented since Phase 9B.
- **Verified against the hosted database** (rollback-safe fixtures, no lasting rows — see the Phase 9D FINAL REPORT for the full transcript): direct SELECT of another Employee's `time_entries`/`daily_updates` still returns zero rows (base RLS confirmed unchanged); the evidence RPC combined a real 60m + 120m Time Entry from two unrelated Employees into 180m for the same Task/date with zero contributor identity in the JSON; an unrelated non-internal Project was rejected; a Supervisor's legitimate Project succeeded (180m) and an unrelated Project was rejected (Supervisor scope not broadened); a Superadmin's org-wide call succeeded; a confirmed Daily Update's safe narrative surfaced via the RPC while a direct generic SELECT for it stayed blocked; a staff-name-mentioning narrative, fed through the real production `computeWeeklyReportSections`, correctly fell back to the Task description with `source: "raw"`; and a full `generate_client_report` call as the real Employee produced a 180m, zero-name-leaked draft, then was rolled back with nothing left behind.
- `tsc`/`eslint`/all four provider-mode builds clean after the hotfix; the original 26 pure-function regression checks re-passed plus one new check for the source-metadata fix.
- Migration pushed and confirmed local↔remote aligned (`supabase migration list`).

**Not built this slice (deliberately deferred)**: the Sparing Efficiency explicit reviewer capability (Phase 9E), scheduled/recurring generation and any report-cutoff configuration (Phase 9F), Daily Visit Hours/`visit_entries` (Phase 9F — no fake Daily Visit row or Grand-Total-including-Visit-Hours row exists; the report-end total is Total Week Hours only), Subtasks.

**Phase 9D (base slice + hotfix) is COMPLETE and MANUALLY ACCEPTED by the user, checkpointed as `39614fa660529c72d891c37ec573758599462559`** ("checkpoint: complete phase 9d weekly client reporting"), not yet pushed.

### Phase 9E — Sparing Efficiency Review + Finalization

**Status: COMPLETE, MANUALLY ACCEPTED** (part of the final Phase 9 checkpoint — "Phase 9 passed"). Replaces the Phase 9B interim "Supervisor may finalize their team's Client Reports" rule with a real, orthogonal reviewer capability — Corebridge still has exactly Employee/Supervisor/Superadmin, no fourth role.

- **Capability**: `profiles.reporting_review_access boolean not null default false` (`User.reportingReviewAccess` in TS). `hasReportingReviewAccess(user)` = Superadmin always (administrative override) OR the explicit flag — never inferred from role/title/department/email/name. Only a Superadmin may grant/revoke it, via `set_reporting_review_access(target_user_id, enabled)` (SECURITY DEFINER, `set search_path=''`, `is_superadmin()` re-checked internally, PUBLIC/anon revoked) — mirrors the existing `admin_set_role`/`admin_set_active` pattern exactly. Never touches `role`/`supervisor_id`/`active`. A small toggle list was added to Settings → Workspace ("Client reporting reviewers" card, Superadmin-only, reusing the existing `listAssignableStaff` staff list) — no new admin page.
- **Finalization** (`canFinalizeClientReport`, `finalize_client_report`): capability-based only — Superadmin always, or anyone with `reportingReviewAccess`, regardless of role. A Supervisor without it can no longer finalize even a direct report's own draft; an Employee/Supervisor WITH it may finalize org-wide, same as Superadmin.
- **View** (`canViewClientReport`, `can_view_client_report`): additive — a reviewer sees every Client Report org-wide (needed for the Review Queue), on top of the existing owner/managed-generator rule. Does NOT expand `time_entries`/`daily_updates`/`tasks`/`contacts` RLS at all.
- **Draft-edit scope split**: the existing owner-only, full-tree `update_client_report_draft`/`canEditOwnClientDraft` is untouched (an Employee's own generated draft is still theirs to fully refine). A NEW narrower lane, `update_client_report_draft_wording`/`canEditClientReportWording`, lets a reviewer who is NOT the owner correct only a line item's `details` text (matched by id) — the RPC walks the stored `departments` JSON server-side and replaces nothing else, so Task identity/Service/Activity/work date/Actual Duration/every total are structurally impossible for a reviewer to rewrite through this path. `ClientReportView` gained a `wordingEditable` mode (Details-only Textarea, no add/remove controls) alongside the existing full `editable` mode.
- **Review Queue**: a third tab on `/dashboard/reports/client` (My Reports / Team's-or-All / **Review Queue**), reviewer/Superadmin-only, showing every Draft `listReports` already returns to a reviewer — a pure status filter, not a new fetch/endpoint.
- **Comments**: `canCommentOnClientReport` now lets a reviewer (including an Employee-role one) comment org-wide, bypassing the `isEmployee` block that would otherwise apply — never the report's own owner commenting on their own work.
- **History/name-safety unchanged**: finalize still appends a `"finalized"` history event; a finalized report is still permanently immutable (update/wording RPCs both reject it); the mandatory name-scan before finalize is untouched; reviewer identity stays internal-only (never in print/PDF/CSV body).
- **Verified against the hosted database** (rollback-safe, real seed users — Alicia/Priya/Jordan): Employee/Supervisor without capability both rejected from self-enabling and from finalizing; a temporarily-granted Employee gained org-wide view + could finalize + could edit wording (proven to touch ONLY `details`, byte-identical everywhere else) on a report she didn't own; capability removal took effect immediately (view/finalize both re-rejected in the same transaction); finalized-report immutability held for both edit RPCs even for a capability holder; Superadmin finalize org-wide unconditional. Everything rolled back — no lasting rows/flags.
- Mock parity: `seed-users.ts` seeds Dana Osei (Employee) with `reportingReviewAccess: true` specifically to demonstrate the capability is orthogonal to role without a manual grant step; `mockClientReportProvider` gained `updateDraftWording` mirroring the RPC's "only `details` changes" contract exactly.
- Migration: `20260821120000_reporting_review_capability.sql`.
- `tsc`/`eslint`/all four provider-mode builds clean.

**Not built this slice**: any new report status beyond Draft/Finalized (no Approved/Rejected/Needs Changes workflow — Section 16's own explicit boundary).

### Phase 9F — Daily Visit Hours + Recurring Client Report Schedules

**Status: COMPLETE, MANUALLY ACCEPTED** (part of the final Phase 9 checkpoint — "Phase 9 passed").

**Daily Visit Hours** — a dedicated `visit_entries` table (never a fake Task/Time Entry): `user_id`, `project_id`, `visit_date` (local calendar date, derived server-side), `start_at`/`end_at` (real timestamptz), `duration_minutes` (a `generated always as` column — never freely entered), `agenda`, `timezone`. Mutated only via `create_visit_entry`/`update_visit_entry`/`delete_visit_entry` RPCs (no direct client INSERT/UPDATE/DELETE grant, same convention as `time_entries`). Server-enforced rules: must be a real, non-internal, accessible Client Project; start < end; ≤16h; must stay within one local calendar day (crossing midnight is rejected, asking the user to split it); **real timestamp-interval overlap against the same user's other Visit Entries AND their Time Entries is rejected server-side** (`visit_entry_overlaps`, a still-running Time Entry treated as occupying through "now") — never a UI-only warning, never auto-mutating the conflicting entry. Authorization: Employee/Supervisor both self-service-only for create/edit (Supervisor is never manager-only here); delete is owner OR Superadmin. My Day gained a compact "Daily Visit Hours" card (Employee and Supervisor) — Today's Visit Total, an "Add Visit" dialog (Project/Start/End/Agenda + live duration), no giant form.

**Client Report evidence + Grand Total** — `get_client_report_weekly_evidence` (additive `create or replace`, same jsonb return) gained `visitEvidence`: one row per local `visit_date`, summed across every legitimate contributor for the Project — no `user_id`, no Agenda text. `client_reports` gained a nullable `daily_visit_minutes` snapshot column; `generate_client_report` gained a matching optional `p_daily_visit_minutes` parameter. The pure `computeWeeklyReportSections` gained an optional `visitEvidence` input and returns `dailyVisitMinutes: number | null` — `null` only when the caller never computed Visit evidence at all (a true pre-9F legacy report — never retroactively invented as 0); a real 0 means Visit Hours WERE computed and there simply weren't any. `ClientReportView`'s footer shows "Total Week Hours" only when `null` (unchanged legacy rendering), or "Total Week Hours / Daily Visit Hours / Grand Total" when a real number exists — Task minutes and Visit minutes are never summed into one number anywhere except that one final display line. CSV gains a `Record Type` column (`Task`/`Visit`) and one extra Visit total row when applicable — deliberately no numeric "Grand Total" row, so a spreadsheet `SUM(Duration)` can never double-count; Agenda is never exported.

**Recurring schedules (V1: weekly only)** — `client_report_schedules` (`project_id`, `created_by`, `active`, `weekday` 0-6/`local_time`/`timezone`, `next_run_at`, `last_run_at`, `last_report_id`), management via `create_client_report_schedule`/`update_client_report_schedule`/`delete_client_report_schedule`/`run_client_report_schedule_now` RPCs — reporting-reviewer/Superadmin-only, org-wide (`canManageClientReportSchedules`), on top of real Project access at creation time. Weekly period is locked: each run covers the previous **seven completed local calendar days**, ending the local day immediately before the run — computed via `compute_next_client_report_run`'s `AT TIME ZONE`-based (DST-safe, not fixed-24h) next-occurrence logic, verified live across a real fall-DST transition (EDT→EST offset shift confirmed correct). Duplicate prevention is a hard unique index (`schedule_id, range_start, range_end`) plus an idempotent "return the existing report" check, not merely an application-level guard — verified live: running the exact same generation twice never creates a second Draft.

**Automatic background execution is REAL**, not a settings-only UI: pg_cron was verified available and enabled on the linked hosted project; a single cron job (`client-report-schedule-runner`, every 15 minutes, idempotently created — unschedule-then-reschedule by fixed job name so repeated migrations never duplicate it) calls `run_due_client_report_schedules()`, which processes every active schedule whose `next_run_at` has arrived (one runner, not one job per Project). **Scheduled generation deliberately does not reuse the browser TypeScript algorithm** (a cron tick has no session to run JS in) — `run_one_client_report_schedule` reimplements the same qualifying-Task/aggregation/Activity-Department-grouping rules directly in SQL against the source tables, and per Section 36's own explicit, deliberate simplification for AUTOMATED generation, uses **only Task description, then title** for narrative — never confirmed Daily Update narrative, never Task Notes — since unattended generation has no interactive staff-name scanner available; the mandatory review/finalize name-scan is completely unchanged and still runs before any Draft (scheduled or manual) can be finalized. `run_client_report_schedule_now` ("Run Now" in the UI) calls this exact same function — never a second, divergent "test" algorithm. A scheduled Draft is Project-scoped, contributor-identity-free, includes Daily Visit Hours, and lands in the ordinary Review Queue exactly like a manual one; it is never auto-finalized. Deleting a schedule detaches (never deletes) its historical Drafts (`schedule_id` FK is `on delete set null`); pausing stops future runs without losing the schedule itself.

A compact reviewer/Superadmin-only "Schedules" tab was added next to Review Queue on `/dashboard/reports/client` — Project, weekday/time/timezone, Active/Paused, Next run, Last run, Run now/Edit/Pause-Resume/Delete. No frequency picker beyond weekly; no ADP-style setup wizard.

- **Verified against the hosted database** (rollback-safe, real seed data/Project): the full `run_one_client_report_schedule` pipeline end-to-end (qualifying Task + two-contributor time correctly summed, Visit Hours correctly summed into `daily_visit_minutes`, Daily-Update-narrative-with-a-real-staff-name correctly ignored in favor of the Task's own description, zero-time completed Task correctly recorded as a `generation-warning`, idempotent on a second call); the full schedule CRUD authorization matrix (Employee/Supervisor without capability rejected from create/delete; Superadmin create/manage succeeds); the due-runner's full lifecycle (paused → 0 drafts, reactivated → exactly 1, a second due-run tick → still 1, delete → historical report survives with `schedule_id` NULL); `compute_next_client_report_run` proven correct across ordinary weekday selection, "already passed today → next week not tomorrow," and a real DST fall-back transition. Visit overlap enforcement verified in the mock provider (Task-time overlap rejected, Visit-Visit overlap rejected, non-overlapping succeeds, midnight-crossing rejected, internal-Project rejected, multi-contributor sum correct) — mirroring the hosted SQL trigger-equivalent logic. Everything rolled back — no lasting rows.
- **Phase 9D regression re-confirmed** after all 9E/9F migrations: direct SELECT of another Employee's `time_entries`/`daily_updates` still returns zero rows; the weekly evidence RPC's 60m+120m=180m multi-contributor behavior is unchanged.
- Mock parity: `visitEntriesProvider`/`clientReportSchedulesProvider` both implemented for mock, sharing the exact same `gatherWeeklyEvidenceForProject` + `computeWeeklyReportSections` path the manual generator uses (schedule path passes an empty `dailyUpdateEntries` array, achieving Section 36's narrative rule for free rather than via a second code path); `processDueSchedulesForTesting` exposes the due-runner equivalent for mock so schedule behavior is testable through the same provider contract even though background cron doesn't literally run in mock.
- Migrations: `20260821130000_visit_entries.sql`, `20260821140000_visit_reporting_integration.sql`, `20260821150000_client_report_schedules.sql`.
- `tsc`/`eslint`/all four provider-mode builds clean.

**Not built this slice**: daily/monthly/quarterly schedule frequencies (V1 is weekly-only, per Section 31); Subtasks (Phase 10).

#### Phase 9E + 9F final integrity hotfix

**Status: COMPLETE, MANUALLY ACCEPTED** (part of the final Phase 9 checkpoint — "Phase 9 passed"). A narrow review pass before acceptance found four concrete integrity/security issues plus one capability-scope mismatch; all four are closed here, forward-only.

- **Bidirectional Visit/Time anti-double-counting**: the Visit-creation path already rejected a conflict with existing Time Entries; the reciprocal direction was missing. `start_timer`/`stop_timer`/`pause_timer`/`resume_timer`/`create_manual_time_entry` all now reject a conflict with an existing Visit too, checked BEFORE any mutation (never partially pause one timer and then fail to start/resume the next). `correct_time_entry` was audited and deliberately left unchanged — it only ever updates `duration_minutes`, never `start_time`/`end_time`, so it cannot create a new timestamp overlap.
- **Running-timer-open-ended semantics**: a genuinely running Time Entry (`duration_minutes is null` — the table's own established "running" indicator) is now treated as open-ended into the future (`'infinity'`) when checked against a proposed Visit, never as ending "now" — a 09:00 timer that later runs through 10:30 can no longer be silently invalidated by a 10:00 Visit that was wrongly allowed to exist beside it. A real correctness bug was caught by this migration's own hosted probe and fixed before being finalized: the fix must key off `duration_minutes is null`, NOT `end_time is null` — a completed duration-only manual entry also has a null `end_time` while representing a real, finished span (`start_time + duration_minutes`), and must never be treated as open-ended. Fixed identically in both the hosted SQL (`visit_entry_overlaps`) and the mock TS mirror.
- **Internal-helper privilege hardening**: `visit_entry_overlaps` and the new `time_interval_overlaps_visit` are both revoked from `authenticated` (already revoked from public/anon) — an ordinary browser session has no legitimate reason to ask "does user X have a Visit/Time conflict right now," a cross-user activity oracle. Verified live: `authenticated` cannot execute either helper directly, while every outer Visit/Time RPC (which calls them internally as its own SECURITY DEFINER owner, unaffected by the caller's own revoked grant) still works normally.
- **Local-date "Today" fix**: `DailyVisitHoursCard` used `new Date().toISOString().slice(0, 10)` (a UTC date) to filter "today's" Visits, even though `VisitEntry.visitDate` is the user's own local calendar date. Replaced with the shared `todayDateOnly()` from `planner-dates.ts` (already used elsewhere for the identical local-vs-UTC problem) — both sides of the comparison now use the browser's own local `Date` getters consistently. Audited the mock provider's own `visitDate` derivation (`dateKeyFromTimestamp`) — already local-safe, no change needed there.
- **Reporting-reviewer schedule scope, made genuinely orthogonal**: `create_client_report_schedule` no longer requires ordinary `can_access_project()` — schedule management is an organization-wide Client Reporting capability, not operational Project access. It now requires `has_reporting_review_access()` plus "this is a real, non-internal Client Project," nothing more. A new narrow, capability-gated `list_client_report_schedule_projects()` RPC (no Task/Time/member/contact data, base `projects` SELECT RLS untouched) backs the Schedules UI's Project picker instead of the operationally-scoped `useProjects()`. Verified live: a reviewer with zero operational access to a real Project could list it via the narrow directory, create/pause/edit/Run Now/delete its schedule, while still being unable to ordinarily SELECT unrelated Tasks/Time Entries/Daily Updates/Contacts (all confirmed 0 rows).
- **Canonical migration for the scheduled-generator live fix**: `20260821150000`'s `run_one_client_report_schedule` had a `zero_time_tasks` CTE-scoping bug, found by that migration's own hosted probe and corrected live via an ad-hoc `create or replace` before it was ever accepted. This hotfix's migration re-asserts that exact corrected body via `create or replace`, so migration history alone (not an unrecorded live edit) reproduces the current correct state. `20260821150000` itself was left byte-for-byte untouched.
- Migration: `20260821160000_phase9_final_integrity_hardening.sql`.
- **Verified against the hosted database** (rollback-safe, real seed users/Projects): all six bidirectional overlap scenarios (Task-time-first blocks Visit, Visit-first blocks manual Time, Visit-around-now blocks `start_timer`, running-timer-first blocks a later Visit, non-overlapping succeeds, Visit-Visit overlap rejected); cross-contributor Visit aggregation (Alicia 120m + Sam 60m = 180m, zero contributor ids/names/Agenda text in the evidence); the exact 300m Task-time + 180m Visit = 480m Grand Total arithmetic, proven through the real production `computeWeeklyReportSections`; the full new schedule-authorization matrix (Employee/Supervisor without capability rejected; a capability holder with NO operational Project access could list/create/edit/Run Now/delete a schedule while remaining unable to browse unrelated Tasks/Time/Daily Updates/Contacts; Superadmin org-wide; idempotency; historical-report survival on delete); pg_cron confirmed still exactly one job; the Phase 9D 60m+120m=180m evidence regression and base `time_entries`/`daily_updates` RLS re-confirmed unchanged. Everything rolled back — zero leftover fixture rows.
- Mock parity: `visit-time-overlap.ts` (shared by both mock Visit/Time providers) mirrors the exact same running-timer/duration-only-entry distinction; `mock-client-report-schedules-provider.ts` gained `listSchedulableProjects` and dropped its own `canAccessProject` check; `ScheduleFormDialog`/`ClientReportSchedulesPanel` both switched from `useProjects()` to the new narrow `useSchedulableProjects()` hook.
- `tsc`/`eslint`/all four provider-mode builds clean.

#### Phase 9E + 9F migration canonicalization

**Status: COMPLETE, MANUALLY ACCEPTED** (part of the final Phase 9 checkpoint — "Phase 9 passed"). A narrow follow-up: `20260821160000`'s `visit_entry_overlaps` had received a post-push correction applied ad hoc (live `create or replace`, not a migration) after the hosted probe found it incorrectly treated a completed duration-only manual entry (`end_time is null` AND `duration_minutes is not null`) the same as a genuinely running timer (`duration_minutes is null`) — both would be flagged open-ended, when only the latter should be.

- `20260821160000_phase9_final_integrity_hardening.sql`'s local file was restored to accurately represent the SQL that was actually applied when that migration version first ran remotely (the simpler, since-superseded `coalesce(te.end_time, 'infinity'::timestamptz)` form) — no already-applied migration is edited to reflect a later fix; migration history now matches remote history exactly.
- The correction itself lives forward-only in a new migration, `20260821170000_visit_time_overlap_canonical_fix.sql`: for each Time Entry, effective end = `end_time` if set, else `'infinity'` only when `duration_minutes is null` (a genuinely running timer), else `start_time + duration_minutes` (a completed duration-only entry's real, implied end). Visit-vs-Visit behavior unchanged. Privileges re-asserted: `authenticated`/anon/public still cannot call it directly; `create_visit_entry`/`update_visit_entry` (which call it internally as their own SECURITY DEFINER owner) remain unaffected and callable by `authenticated`.
- **Verified against the hosted database**, rollback-safe: the live function definition confirmed to contain the three-way logic; privilege grants confirmed (`authenticated`/anon → false, `service_role` → true, outer RPCs still `authenticated`-callable); `stop_timer`/`pause_timer`/`resume_timer` each independently proven to reject when their relevant interval overlaps a real Visit; a normal Start→Pause and Start→Stop with no Visit anywhere near both still succeed; the exact duration-only regression that motivated this fix (60-minute duration-only entry, implied end 15:00, does NOT overlap a 16:00–17:00 Visit; a 150-minute one, implied end 16:30, DOES overlap it) reproduced correctly against the final canonical function.
- **Local-date boundary determinism re-confirmed**: `dateKeyFromTimestamp` (the same local-date core `todayDateOnly()` builds on) maps the UTC instant `2026-08-21T00:30:00Z` to `2026-08-20` under `TZ=America/Toronto` and to `2026-08-21` under `TZ=UTC` — the exact cross-midnight local-vs-UTC case `DailyVisitHoursCard`'s fix depends on.
- No TypeScript/React source changed in this pass — migration SQL and documentation only; `tsc --noEmit` re-run clean as a lightweight sanity check (full four-build matrix not re-run, per instruction, since no source changed).

#### Phase 9E + 9F manual-acceptance UI integration fixes

**Status: COMPLETE, MANUALLY ACCEPTED** (part of the final Phase 9 checkpoint — "Phase 9 passed"). Two real bugs surfaced during final manual acceptance click-through; both were frontend integration defects, no migration required.

- **Schedule/Visit Project `Select` controlled-value bug**: `schedule-form-dialog.tsx` and `add-visit-dialog.tsx` both passed `value={projectId || undefined}` to the shared `Select` — an empty string became `undefined` on first render (uncontrolled), then a real string once a Project was picked (controlled), triggering Base UI's "changing an uncontrolled component to controlled" warning. Fixed by passing the plain string state directly (`value={projectId}`), matching the established repo convention already used correctly in `generate-client-report-dialog.tsx` (`value={form.companyId}`, initialized to `""`) — one controlled empty-state representation (`""`) for the component's entire lifetime, never alternating with `undefined`.
- **Run Now Draft not appearing in Review Queue without a page reload**: the underlying data layer was already correct (re-verified live: `run_client_report_schedule_now` creates a real `status='draft'` row, correct `project_id`/`schedule_id`, reviewer-viewable, idempotent on a second call) — the bug was purely a stale-frontend-state integration defect. `ClientReportSchedulesPanel`'s Run Now handler only refreshed its own `useClientReportSchedules()` state (Next/Last run); the Review Queue tab reads from a completely separate `useClientReports()` hook instance on the parent page, which was never told a new Draft existed. Fixed by threading the page's own `useClientReports().refresh` down as a new `onReportGenerated` prop, called (alongside the existing schedules refresh) after a successful Run Now — no `window.location.reload()`, same refresh-callback convention every other create/update action in this codebase already uses. Confirmed the Review Queue's own filter (`status === "draft"`) never excluded scheduled Drafts — that logic was already correct.
- **Verified against the hosted database** (rollback-safe, real seed data): a capability-holder with zero operational access to a real Project could create a schedule, Run Now, and the resulting Draft existed, was `status='draft'`, carried the correct `project_id`/`schedule_id`, was visible under `can_view_client_report`, satisfied the Review Queue's `status='draft'` predicate, and was idempotent on a repeat call (same report, no duplicate). One hosted verification pass did not roll back as expected on first attempt (an isolated tool/connection anomaly, not reproduced on a follow-up retest of the identical statements) — the leaked schedule/report/capability-flag rows were identified and fully removed immediately, confirmed clean by direct row-count queries afterward.
- No migration needed — both bugs were purely frontend integration defects; the RPC/RLS layer was already correct.
- `tsc`/`eslint`/all four provider-mode builds clean.

#### Phase 9E + 9F business-semantics correction — locked Plan → Complete Client Visit workflow

**Status: COMPLETE, MANUALLY ACCEPTED** (part of the final Phase 9 checkpoint — "Phase 9 passed"). A business-semantics correction, not a bug fix: the original Phase 9F Visit Entry model conflated "planning a client visit" with "having actually visited," letting a single Start/End be entered up front for something that hadn't happened yet. The corrected, locked model:

- A Visit is **PLANNED** first — Project, intended local visit date, and the Agenda/questions the Employee is taking into the meeting. No actual time exists yet, so none is asked for, and **a Planned Visit reserves nothing and contributes zero reportable minutes** (no overlap check at planning time; excluded from all Client Report evidence).
- Only after the visit happens does the Employee **COMPLETE** it — recording the real Start/End. Duration is always server-derived, never entered directly. Only at this point does the full anti-double-counting overlap check (against the same user's Time Entries, running timer, and other Completed Visits) apply, and only a Completed Visit's minutes are ever reportable.
- `visit_entries` gained an explicit `status` column (`'planned' | 'completed'`), backed by a structural CHECK constraint tying `status` to `start_at`/`end_at`/`duration_minutes` nullability — all three null together while planned, all three set together once completed, never partially. `duration_minutes`'s generated-column expression was rewritten with an explicit `CASE WHEN start_at IS NULL OR end_at IS NULL THEN NULL ELSE ...` guard (a bare `GREATEST(0, ...)` would otherwise silently compute 0, not NULL, for a Planned Visit — `GREATEST` ignores NULL arguments). A pre-existing real Visit row (real Start/End from before this correction) backfills straight to `status='completed'` with its historical hours unchanged.
- New RPCs: `create_visit_entry(project, visit_date, agenda, timezone)` (plans only — today or a future local date, no actual-time inputs, no overlap check), `update_visit_plan(id, visit_date, agenda)` (owner-only, Planned rows only — rejects with a pointer to `complete_visit_entry` if already completed), `complete_visit_entry(id, start_at, end_at)` (owner-only; validates start<end, ≤16h, same local day, AND that the actual interval's local date matches the Visit's own already-chosen `visit_date` — rejecting rather than silently moving the date on a mismatch; runs the full overlap check; works identically whether transitioning Planned→Completed for the first time or correcting an already-Completed Visit's hours, so no second correction/audit RPC was needed). `delete_visit_entry` unchanged.
- **The `tstzrange(NULL, NULL)`-is-infinite trap**: both `visit_entry_overlaps` and `time_interval_overlaps_visit` gained an explicit `and v.status = 'completed'` filter — without it, a Planned Visit's null Start/End would be read by Postgres as an *infinite, unbounded* range (not "no interval"), silently blocking that user's Task Time forever. Designed in from the start of this fix based on the known trap, not discovered via a failed probe.
- Client Report evidence parity preserved exactly: `get_client_report_weekly_evidence` (manual) and `run_one_client_report_schedule` (scheduled) both gained the identical `and v.status = 'completed'` filter on their visit-aggregation subqueries, so the two generation paths continue to agree on Visit totals by construction.
- My Day's Visit card was redesigned around Planned vs. Completed: "Today's Visit Hours" now counts completed-and-today-dated minutes only (`todayDateOnly()`); Planned Visits show their Agenda with a "Record hours" action, Completed ones show their actual time range/duration with an "Edit hours" action; "+ Plan Client Visit" replaced "+ Add Visit" as the primary action. The former single Add Visit dialog is now Plan-only (Project/Date/Agenda, no Start/End); a new Record Visit Hours dialog handles both the first-time completion and later corrections.
- **Account Manager is a job responsibility described in the business requirement, not a new application role** — Corebridge still has exactly three roles (Employee/Supervisor/Superadmin); any Employee/Supervisor with real Project access may plan/complete their own Client Visits, same as before.
- Migration: `20260821180000_planned_client_visit_workflow.sql` (forward-only — `20260821130000`/`140000`/`150000`/`160000`/`170000` all left byte-for-byte untouched).
- **Verified against the hosted database** (rollback-safe, real seed users/Project): all 12 required scenarios passed — plan succeeds with no actual-time fields set; a Planned visit contributes 0 to evidence; a Planned visit never blocks Task Time start/stop (direct proof that the `status='completed'` filter, not date-based luck, is what prevents the infinite-range trap); completing a Visit sets `status='completed'`/duration=120m correctly; a completed Visit is reportable; completion overlapping existing Task Time is rejected; a running timer (open-ended) blocks a Visit-completion attempt; Completed-vs-Completed Visit overlap is rejected; cross-contributor evidence sums correctly (120m+60m=180m) with zero ids/names/Agenda in the returned evidence shape; a Planned visit present alongside a Completed one does not inflate the total; the Grand Total components (300m Task + 180m Completed Visit = 480m) are each individually correct; the one pre-existing real legacy Visit row remains `status='completed'` with its original hours intact and still reportable. Everything rolled back — zero leftover fixture rows confirmed by direct follow-up count queries.
- Mock parity: `mock-visit-entries-provider.ts` rewritten for the same Plan/Complete contract (including the same local-date-match and no-overlap-while-planned rules); `visit-time-overlap.ts`'s two shared helpers gained the same `status !== "completed"` exclusion; `gatherWeeklyEvidenceForProject` (shared by both the manual and scheduled mock generators) filters visit aggregation to completed-only. A standalone mock-mode regression script reproduced all 12 hosted scenarios (10 independently-checkable assertions; two hosted-only PII-shape/cleanup checks don't apply to an in-memory store) and passed identically.
- `tsc`/`eslint`/all four provider-mode builds clean.

#### Phase 9E + 9F Visit UX polish — no architecture/schema change

**Status: COMPLETE, MANUALLY ACCEPTED** (part of the final Phase 9 checkpoint — "Phase 9 passed"). Three small frontend-only gaps closed on top of the Plan → Complete Visit workflow above; no migration, RPC, RLS, or reporting change.

- **Edit for a Planned Visit was unreachable**: the already-implemented `updateVisitPlan` provider method/`update_visit_plan` RPC had no UI entry point. `AddVisitDialog` was refactored into one shared create/edit form (an optional `editVisit` prop switches its mode) rather than duplicating the Plan form — editing shows Project as fixed read-only context (never a picker, matching the provider's own immutable-Project contract) and only allows changing Visit date / Agenda. `DailyVisitHoursCard` now shows `[Record hours] [Edit]` for every Planned Visit.
- **First-time Record Hours no longer invents times**: `RecordVisitHoursDialog` previously defaulted a fresh Planned Visit's Actual Start/End to 09:00–10:00 — a real hour could be saved without the user ever entering one. Both fields now start blank for a first-time completion; Save is disabled until Start and End are both entered and End is after Start. Opening "Edit hours" on an already-Completed Visit is unchanged — it still pre-fills the Visit's real stored Start/End, which is legitimate correction context rather than an invented default.
- **Agenda line breaks were being collapsed visually**: Agenda is a deliberate multi-line list of questions/items, not a paragraph. `whitespace-pre-wrap` was added everywhere Agenda renders as static text — the Planned and Completed Visit cards and the Record Visit Hours dialog's Agenda preview (the Edit Planned Visit form's own `Textarea` already preserved line breaks natively, no change needed there). Agenda remains internal-only — still absent from the Client Report body, PDF, and CSV.
- `tsc`/`eslint` clean (0 errors, same 2 pre-existing unrelated warnings); all four provider-mode builds (`supabase`, `supabase-core`, `supabase-auth`, `mock`) re-run clean since this touched React/UI source.

**Phase 9E — Sparing Efficiency Review + Finalization is COMPLETE and MANUALLY ACCEPTED. Phase 9F — Daily Visit Hours + Recurring Client Report Schedules (base + final integrity hotfix + migration canonicalization + manual-acceptance UI fixes + Plan → Complete Visit business-semantics correction + Visit UX polish) is COMPLETE and MANUALLY ACCEPTED. The user has confirmed "Phase 9 passed" — Phase 9 in its entirety (9A through 9F) is COMPLETE.**

## Roadmap history

1. ~~Supabase ground-truth/schema reconciliation~~ — done (audit approved).
2. ~~Supabase Foundation A (local scaffolding + Auth/Companies migration set)~~ — done, reviewed, checkpointed.
3. ~~Connect to the hosted `corebridgex-crm` project, apply the migration set + test seed~~ — done, verified.
4. ~~Create the first development Auth user, verify `handle_new_user()`, run the first-superadmin bootstrap SQL~~ — done, verified. One development Superadmin exists.
5. ~~Supabase Foundation C — explicit least-privilege table/function grants + safer default privileges for future objects~~ — done, verified read-only against the actual effective privileges, committed `628021e`.
6. ~~Supabase Auth — Controlled Transitional Mode (real `supabaseAuthProvider`, `supabase-auth` provider mode)~~ — done, manually verified end-to-end by the user (real login, refresh, logout, wrong-password handling, mock pages still usable).
7. ~~Phase 5 — Boss Feedback Alignment / Final Domain Alignment (Workstream = Service, selected Activities, Activity-first Task creation, role-aware ordering, Todo→timer→In Progress)~~ — done, manually accepted, checkpointed (mock business data only).
8. ~~Phase 6 — Employee & Supervisor Operational UX~~ — done, manually accepted, checkpointed as `cb2e7c5`.
9. ~~Real Supervisor/Employee Auth identities created~~ — done, verified read-only (aggregate counts only, no identifiers recorded).
10. ~~Phase 7A-C — Core Operational Supabase Backend~~ (Companies/Workstreams/Activity Catalog/Tasks/Time Entries/Notifications real; new `supabase-core` provider mode) — done, schema/RLS/grants/seed live, providers implemented.
11. ~~Phase 7A-C hotfix~~ (empty-id provider guards, Employee assigned-Task → parent Workstream visibility, centralized `getInitials()`) — done.
12. ~~Phase 7D — Collaboration~~ (Notes/Task Handoffs/Saved Views/Templates/Daily Updates real) — done.
13. ~~Phase 7E — Reports + Productivity~~ (Accomplishments Reports/Client Reports real; assignee-scope hardening resolved) — done. Full `supabase` mode selects a real implementation for all 14 provider groups.
14. ~~Phase 7 accepted and checkpointed~~ — `c63e86d03fafeb516846bb65759cfdacf19ae836`. Real Auth/Postgres/RLS backend is functionally complete across every pre-Phase-8 feature.
15. ~~Phase 8A — Project / Annual Contract Foundation~~ — done, manually accepted, checkpointed as `9b5a2d8`.
16. ~~Phase 8B — Employee-First Project Workspace~~ — done, manually accepted, checkpointed as `77dbab9`.
17. ~~Phase 8C — Employee-First Task Center Polish + Task Drawer~~ — done, manually accepted, checkpointed as `ddc8da6`.
18. ~~Phase 8D — Planner~~ — done, manually accepted, checkpointed. Day/Week/Month/Group views, own-work-first Supervisor scope, timezone-safe date handling, Task Drawer reuse.
19. ~~Phase 8E — Dashboard drill-down + Project/annual contract polish (final Phase 8 slice)~~ — done, manually accepted, checkpointed. StatCard interaction fix, newly-linked dead-click cards, Project list Service summary, Superadmin-only Project create/edit/renew (two new atomic RPCs, 29 passing security/atomicity probes), KPI drill-down retargeted to the Task Center (incl. `due=today`), permanent per-widget Expand into a near-full-screen `DashboardWidgetFocusDialog`, Project list Service badge truncation fixed.
20. ~~Phase 8 — COMPLETE.~~ All of 8A–8E manually accepted, checkpointed as `a77785c57c3e5424fa63b4221ee9523a688ffae9`, and pushed to GitHub by the user.
21. ~~Phase 9A — Reporting Automation Audit & Architecture~~ — done, read-only, delivered for review.
22. ~~Phase 9B — Reporting Foundation + Client Report Correctness~~ — COMPLETE, manually accepted, checkpointed as `299499948621f0d0d4742529d1e658d91b8eaf11`. Client Report now Project-scoped (`project_id`, deterministic-only legacy backfill), generation moved off raw INSERT onto SECURITY DEFINER RPCs for both report types, Employee Client-draft generation, generate/finalize permissions split (finalize is never owner-based; Supervisor finalization is legitimate-team/report-scoped via `can_view_client_report` + `can_access_project`, Superadmin org-wide — hardened in a same-slice security hotfix, `20260820120000_client_report_finalize_scope.sql`, after review caught that an earlier draft's `is_supervisor() OR is_superadmin()` predicate was an unconditional org-wide bypass), finalized Client Report reopen retired, Accomplishments-vs-Client-Report UX terminology disambiguated. A manually-reported detail-page 404 was root-caused to a stale local dev-server route cache (not RLS/code — see the Phase 9B write-up above), and destructive actions (trash/restore/permanently-delete) were separated from view/comment/finalize into their own narrower, generator-or-Superadmin-only (Superadmin-only for permanent delete) scope via `20260820130000_client_report_destructive_action_scope.sql`.
23. **Phase 9C — Daily Update Automation** — COMPLETE, manually accepted ("Phase 9C passed"). Tasks + Time Entries-backed auto-draft, one Task-backed row per user/date/Task, Actual Time from legitimate completed Time Entries, user-owned/persistent Scheduled Time (never auto-derived), full Company → Project → Workstream → Activity hierarchy per entry, Handoffs folded into their Task's own row, Supervisor Employee-first own-update behavior, submitted frozen snapshots, one Team Lead review marker per submission (reopen clears it, re-submit requires a fresh review), local-calendar-safe Daily Update dates with DST-safe day bounds, Submit/Submitted user-facing terminology (internal `confirmed` enum unchanged), non-blocking missing-Scheduled-Time attention. Two migrations: `20260821090000_daily_update_review.sql`, `20260821100000_daily_update_review_guard.sql`.
24. **Phase 9D — Weekly Client Report Generation + Multi-day Task Lines + Client Report calculation/export hardening** — COMPLETE, manually accepted (base slice + real-Supabase evidence hotfix). See its own section above.
25. **Phase 9E (Sparing Efficiency explicit review/finalization capability) + Phase 9F (Daily Visit Hours + scheduled/recurring report generation + the locked Plan → Complete Visit workflow correction + final Visit UX polish + final reporting/RLS/export hardening)** — implemented together as a combined slice, COMPLETE and MANUALLY ACCEPTED ("Phase 9 passed"); see their own sections above.
26. **Phase 9 — COMPLETE.** All of 9A–9F manually accepted, checkpointed as a single final commit ("checkpoint: complete phase 9 reporting automation" — see git log for the exact hash), pushed to GitHub.
27. **Phase 10 — One-Level Subtasks (base slice + hierarchy-authorization hardening + migration canonicalization + TaskDrawer infinite-recursion fix)** — COMPLETE, manually accepted in full against the complete acceptance checklist; see their own sections above. Checkpointed as a single final commit ("checkpoint: complete phase 10 one-level subtasks" — see git log for the exact hash), pushed to GitHub.
28. **Phase 11A — Task Experience Redesign & Time Simplification**, **Phase 11B — Task Navigation & Quick View**, and **Phase 11C — Reporting Simplification & Ownership** — all **MANUALLY ACCEPTED** by the user's final verification passes (an empty-UUID runtime bug, a timer-placement correction, and an Edit-button layout polish were found and fixed along the way). **Phase 11D — Role Surface Cleanup & Cross-App Consistency** — IMPLEMENTED, uncommitted, MANUAL ACCEPTANCE PENDING (Review Queue/Schedules UI removal, Client Visits temporarily Superadmin-only, cross-app consistency sweep). See their own sections above.

## Phase 10 — One-Level Subtasks

**Status: COMPLETE and MANUALLY ACCEPTED.** The user ran the complete manual acceptance checklist (login/dashboard stability, Tasks page stability, top-level Task creation, Add Subtask, inherited Company/Project/Service/Activity context, parent → Subtask stacked drawer, regression test for the TaskDrawer recursion fix, one-level-only Subtask hierarchy, independent parent/Subtask statuses, parent Done warning with open Subtasks, checklist safety, independent Subtask time tracking, parent own-time + Subtask time rollup, Tasks list presentation, Board, Planner, hierarchy-only permission checks, reverse hierarchy permission checks, Supervisor behavior, full Task/Subtask pages, final navigation/stability sweep) and confirmed it passes. A once-observed `TypeError: Failed to fetch` / `resolve_profile_directory` error did not reproduce after refresh/retesting and is treated as a transient transport failure — no speculative fix or migration was made for it.

Locked hierarchy:

```
Company
  → Project
      → Service
          → Activity
              → Task
                  → Subtask
                      → Checklist
```

A Subtask is a **full Task** self-referencing another Task — never a checklist item, never a separate lightweight object, never arbitrary-depth. `tasks.parent_task_id uuid null references tasks(id)`: `null` = top-level; non-null = a Subtask of that Task, exactly one level (a Task whose own `parent_task_id` is set can never itself be a parent — enforced server-side, not just by hiding a UI button).

**Data model / migration** — `20260821190000_one_level_subtasks.sql` (forward-only; no prior migration edited):
- `tasks.parent_task_id` (nullable, FK `on delete restrict`, indexed). No backfill needed — every existing Task already has `parent_task_id = null` by definition of adding a new nullable column; confirmed live (8/8 existing Tasks stayed top-level).
- `enforce_task_invariants()` (the pre-existing BEFORE INSERT/UPDATE trigger) extended with: parent-immutability (`parent_task_id` can never change after a row exists — no re-parenting, no promoting a Subtask to top-level, no converting an existing top-level Task into a Subtask), self-parent rejection, "parent must itself be top-level" rejection (no sub-subtasks), and context-integrity rejection (a Subtask's `workstream_id`/`activity_id`/`company_id` must exactly match its parent's — `IS DISTINCT FROM` used throughout for null-safe Activity comparison) — reject, never silently coerce. Also extended to block a top-level Task with live Subtasks from changing its own Workstream/Activity (Section 8's safe V1 rule — no cascading reassignment).
- `can_access_task(uuid)` extended with two new, strictly one-hop (never recursive) OR branches: a viewer assigned to a row's parent may read that row, and a viewer assigned to one of a row's children may read that row. **Read visibility only** — `can_edit_task`/`can_progress_task`/`can_log_time_on_task` are untouched, independent predicates, so seeing a coworker's Subtask (or its parent) for context never grants edit/status/time-logging rights over it.
- New `create_subtask(parent_task_id, title, description, assignee_ids, allow_unassigned, status, priority, due_date, expected_minutes, checklist_items)` RPC — a separate hardened path from `create_task` (never modified) that derives Company/Workstream/Activity from the parent **server-side only** (no such params exist to inject from the browser); rejects an inaccessible or already-a-Subtask parent; reuses `create_task`'s exact assignee-resolution logic verbatim (Employee forced to self, Superadmin any active user, Supervisor self+direct-reports, each falling back to self) so the two creation paths can't drift.
- New `get_task_time_rollup(task_id)` RPC — the smallest safe aggregate for "parent inclusive effort": two integer minute sums (`own_minutes`, `subtasks_minutes`), never raw `time_entries` rows/notes/user ids. Lets a parent-Task viewer see combined effort even when a Subtask is assigned to a coworker they have no independent raw Time Entry visibility into — `time_entries_select` RLS (`user_id = auth.uid() or manages_user(user_id)`) is completely untouched.
- `toggle_checklist_item` extended: the checklist-driven auto-Done branch now also requires zero open Subtasks (`not exists (select 1 from tasks where parent_task_id = target and status <> 'done')`) before auto-completing a parent — the auto-revert-to-in-progress branch (unchecking an item on an already-done Task) is unaffected.

**Assignment / authorization** — no new role (still exactly Employee/Supervisor/Superadmin). Parent and Subtask may have completely different assignees (reuses the existing multi-assignee `task_assignees` join, not a new system). An Employee can create a Subtask only under a Task they already legitimately access (assignee-only, same as today); Supervisor gets the same plus their existing team scope; Superadmin is org-wide — all via the same, unmodified `can_access_task`/role checks `create_task` already used, no Subtask-specific permission function invented.

**UI** (`src/components/tasks/`): a new "Subtasks" section (`task-subtasks-section.tsx`) inside a top-level Task's drawer/detail page — "X / Y done" derived progress (never stored), a `+ Add Subtask` action opening a new lightweight `add-subtask-dialog.tsx` (Title/Description/Assignees/Status/Priority/Due date/Expected time/Checklist — deliberately **no** Client/Project/Service/Activity fields, since those are inherited automatically). A Subtask's own drawer shows a `SUBTASK` badge + clickable "Subtask of `<parent>`" breadcrumb, and never shows `+ Add Subtask` itself (enforced server-side by `enforce_task_invariants`, not just hidden). Manually marking a parent Task Done while Subtasks remain open shows a new generic `ConfirmDialog` warning (`src/components/ui/confirm-dialog.tsx` — this codebase had no prior AlertDialog/confirm pattern; built on the existing `Dialog` primitives) in both the Task drawer's status Select and the Kanban board's drag-to-Done — a warning, never a hard block, matching status independence (completing one/all Subtasks never auto-changes the parent, and vice versa). `TaskRow`/`TaskCard`/`TaskGridCard`/`TaskSummaryItem` (the shared row components behind Task Center list, Kanban board, My Day, Planner, Dashboard KPI drill-downs, and Project workspace) each gained a small `SUBTASK` badge + "Subtask of `<parent>`"/"Parent: `<title>`" context line, so a Subtask is real, actionable, visible work everywhere a Task already appeared — no new page, no tree component, no recursive expander. **Known, disclosed scope gap**: the Done-warning was not wired into My Day's own "quick mark done" checkbox (`TaskGridCard`'s `onMarkDone`) in this pass, to avoid destabilizing its existing optimistic check-then-exit-animation flow — that path still fires immediately today, unchanged from before Phase 10; the drawer and board paths (the two most-used status-mutation surfaces) are covered.

**Reporting — no double-counting.** Subtask Time Entries stay attached to the Subtask's own `task_id`, exactly like any other Task; `get_client_report_weekly_evidence`/`run_one_client_report_schedule` were **not touched** — they already aggregate `time_entries` by `task_id`/date with zero special-casing, so a parent and its Subtasks simply appear as separate, independent rows summing to their real total. The "parent inclusive" roll-up (`get_task_time_rollup`) is a completely separate, UI-only RPC never called by any report generator — structurally impossible for it to double-count into report arithmetic. Proved live (30m parent + 60m + 120m Subtask = 210m factual total, three distinct task rows, never an inflated 390/480m figure).

**Mock parity** (`mock-tasks-provider.ts`, `permissions.ts`): `canAccessTask` gained an optional `hierarchyAssigneeIds` field (additive, backward-compatible — every existing call site omitting it keeps its exact prior behavior) mirroring the SQL function's two new one-hop branches; `updateTask` gained the same context-lock guards (Subtask context read-only, parent-with-children context frozen) as the SQL trigger; `toggleChecklistItem` gained the identical open-Subtasks guard; new `listSubtasks`/`createSubtask`/`getTaskTimeRollup` methods mirror the RPCs exactly, including `createSubtask` reusing the shared `resolveAssigneeIds` helper (no logic duplication, unlike the SQL side which required copying `create_task`'s assignee block verbatim since Postgres has no easy code-sharing across differently-shaped RPCs).

**Verified against the hosted database** (rollback-safe, real seed users — Alicia/Sam/Priya/Jordan): all 18 required scenarios (A–R) plus the time-roll-up and reporting-arithmetic probes passed — existing Tasks stayed top-level; a Subtask inherits exact parent context with no re-selection possible; mismatched-context/sub-subtask/self-parent/re-parent-an-existing-Subtask/unrelated-Employee-create/parent-hard-delete-with-live-children were all rejected server-side; a parent assignee can see (but not log time on) a Subtask assigned to someone else and vice versa; a Supervisor managing both assignees sees the full hierarchy; a Superadmin can create a Subtask under any parent; a Subtask's own Start/Stop timer is assignment-only; an ordinary Subtask's checklist auto-Done still fires while a parent's does NOT while a Subtask remains open, and does NOT auto-fire just because every Subtask finished; `statusChangedAt` updates correctly on a Subtask. Two probe-methodology lessons surfaced and were corrected before accepting results: (1) raw `SELECT`s against `tasks` on the probe's own elevated connection bypass RLS entirely and prove nothing about visibility — fixed by calling `can_access_task()`/`can_log_time_on_task()` directly; (2) the first-chosen real seed Task happened to already be `status='done'` with an already-fully-ticked checklist from unrelated prior testing, making "does NOT auto-complete" untestable against it — fixed by using a fresh, known-`in-progress` fixture Task for those specific assertions. Everything rolled back — zero leftover fixture rows confirmed by direct follow-up queries (two initially-alarming "leaks" during verification — a `project_members` row and 6 recent `time_entries` — were both confirmed to be genuine, pre-existing, unrelated data from earlier legitimate testing in this same hosted project, not actual leaks).

**Mock-mode regression**: a standalone script reproduced the equivalent 14 independently-checkable scenarios and passed identically.

- `tsc`/`eslint` clean (0 errors, same 2 pre-existing unrelated warnings); all four provider-mode builds (`supabase`, `supabase-core`, `supabase-auth`, `mock`) clean.
- Files: migration `20260821190000_one_level_subtasks.sql`; `src/lib/data/types/task.ts` (`parentTaskId`), `src/lib/data/providers/tasks-provider.ts` (`TaskWithRelations.parentTask`, `SubtaskInput`, `TaskTimeRollup`, `listSubtasks`/`createSubtask`/`getTaskTimeRollup`), `src/lib/data/permissions.ts` (`canAccessTask` hierarchy branch), `src/lib/data/hooks/use-tasks.ts` (`useSubtasks`), `src/lib/data/providers/mock/mock-tasks-provider.ts`, `src/lib/data/providers/supabase/supabase-tasks-provider.ts`; new `src/components/tasks/add-subtask-dialog.tsx`, `src/components/tasks/task-subtasks-section.tsx`, `src/components/ui/confirm-dialog.tsx`; updated `task-drawer.tsx`, `task-detail-content.tsx`, `task-row.tsx`, `task-card.tsx`, `task-grid-card.tsx`, `task-summary-item.tsx`, `task-board.tsx`, `src/app/dashboard/tasks/[id]/page.tsx`.

**Not built this slice (deliberately deferred, per explicit instruction)**: re-parenting/promoting/demoting a Subtask after creation; a Subtask-specific role; deep/multi-level nesting; wiring the Done-warning into My Day's quick-complete checkbox (see above); a dedicated Subtask search/filter UI beyond what Tasks' existing filters already cover for free (a Subtask is a real `TaskWithRelations` row, so search/status/assignee/Service/Activity filters and saved views already include it).

#### Phase 10 hierarchy-authorization hardening

**Status: COMPLETE and MANUALLY ACCEPTED (folded into Phase 10).**

A narrow, targeted review pass before acceptance found that Phase 10's `can_access_task` hierarchy-READ widening (parent assignee ↔ child assignee) had silently been inherited by several MUTATION/side-effect paths as if it were operating authority — a viewer who could only see a Task through a parent/child relationship could act on it as though directly authorized. Root cause example: Parent P assigned only to Alicia, Child C (parent_task_id = P) assigned only to Sam — Sam legitimately gets `can_access_task(P) = true` through hierarchy, and every write path gated on that alone would have let him act on P.

- **New migration**: `20260821200000_subtask_hierarchy_authorization_hardening.sql` (forward-only; `20260821190000` untouched). Adds `can_access_task_directly(task_id)` — a self-contained reproduction of `can_access_task`'s ORIGINAL pre-Phase-10 body (Superadmin org-wide; Supervisor via direct-report/self assignee match or unassigned+Company access; Employee via direct assignment+Company access), with NO hierarchy branch in either direction. `can_access_task` itself is completely unchanged and continues providing hierarchy READ visibility exactly as Phase 10 intended; every SELECT-only RLS policy (`tasks_select`, `checklist_items_select`, `task_assignees_select`, `task_handoffs_select`, `notes_select`) is untouched.
- **Complete `can_access_task` usage audit** (every live function/policy referencing it): `create_subtask` (mutation — **fixed**, now gated on direct access), `notes_insert` RLS policy (mutation — **fixed**), `create_task_handoff`'s caller-side check (mutation — **fixed**; the recipient-eligibility check, `can_user_access_task`, was already pure direct-access semantics and untouched), `list_handoff_candidates`'s caller-side check (mutation-adjacent — **fixed**, since it directly feeds handoff creation), `get_task_time_rollup` (sensitive aggregate — **fixed**, a child-only assignee must not receive a parent's sibling-Subtask aggregate merely from hierarchy read), `notify_task_assignment_changed` (mutation, directly callable by authenticated end users — **fixed**; this closes a real, independently-discovered notification-spoofing gap beyond the originally reported scenario). Left unchanged with reasoning: `notify_task_created` (internal-only, no `authenticated` EXECUTE grant at all — unreachable except via `create_task`/`create_subtask`'s own already-gated calls); `resolve_profile_directory` (a low-sensitivity read aggregate — name/role/supervisor-id only — consistent with hierarchy read's own purpose); `start_timer`/`resume_timer`/`create_manual_time_entry` (their real, authoritative gate is the unchanged, assignment-only `can_log_time_on_task`, checked immediately after — tightened their leading pre-check to direct access anyway, as proactive defense-in-depth against ever resting on hierarchy-widened logic even incidentally, not because they were independently exploitable).
- **A real, pre-existing, unrelated bug was found while building this**: `can_access_task_directly` was first implemented as a thin wrapper over the existing `can_user_access_task(candidate_id, task_id)` helper (built for Handoff-recipient validation), but that helper delegates to `can_user_access_company`, which was never updated when Projects/`project_members` were introduced in Phase 8 (unlike `can_access_company`, which `can_access_task` already correctly uses) — reusing it would have made a genuine direct assignee whose only route to a Company is Project membership fail the new "direct access" check entirely, a functional regression caught live during hosted probing before this migration was accepted. Fixed live at the time by making `can_access_task_directly` self-contained instead of delegating — but the real root cause (`can_user_access_company`/`can_user_access_task` never being updated for Projects) was left uncorrected in migration history, and the live hotfix was never reflected in this migration's own committed SQL. **Both were corrected in the migration-canonicalization pass immediately below — `can_user_access_task`/`can_user_access_company` are no longer "untouched," they're the actual forward fix.**
- **Mock parity**: new `canAccessTaskDirectly` in `permissions.ts` (pure pre-Phase-10 `canAccessTask` body, no hierarchy field); `canCreateHandoff`/`usersWhoCanReceiveHandoff` repointed to it; `mock-tasks-provider.ts`'s `createSubtask`/`getTaskTimeRollup` repointed via a new `requireDirectAccess` helper; `mock-notes-provider.ts`'s `createTaskNote` repointed (its own `listNotesForTask` READ stays on hierarchy-inclusive `canAccessTask`); `mock-task-handoffs-provider.ts`'s `listHandoffCandidates` caller check repointed. An additional real mock-only leak was found and fixed in the same pass: `listTimeEntriesForTask`/`requireTaskAccess` (`mock-time-entries-provider.ts`) returned RAW Time Entry rows (with user identity) gated only on hierarchy-widened access — on the hosted side this was never at risk, since `time_entries_select` RLS (`user_id = auth.uid() or manages_user(user_id)`) never depended on Task access at all; the mock path did, and is now tightened to match.
- **UI**: `TaskSubtasksSection`'s "+ Add Subtask" button is now hidden for a viewer who can only see the parent through hierarchy (approximated client-side from the already-loaded parent's own assignees — a UI hint only, the server RPC remains authoritative). `TaskFormDialog` was found, on inspection, to still offer fully editable Client/Project/Service/Activity selectors when editing a Subtask or a top-level Task with existing children — fixed: both cases now show a fixed, read-only "Client — Project — Service — Activity" summary with explanatory copy ("Inherited from the parent Task…" / "This Task has Subtasks. Service and Activity cannot be changed.") instead of interactive pickers, while every ordinary field (title, description, assignees, status, priority, due date, expected time, checklist) remains fully editable.
- **Verified against the hosted database** (rollback-safe, real seed users — Alicia/Sam/Priya/Jordan, real `authenticated`-role RLS enforcement rather than a privileged bypass connection): all 20 required scenarios (A–T plus a dedicated reporting-arithmetic re-check) passed — a child-only assignee reads the parent via hierarchy but is rejected from creating a sibling Subtask, adding a Note, initiating a Handoff, starting/logging time, and requesting the parent's inclusive roll-up; that same assignee operates normally on their own Subtask (read/status/Note/Handoff-to-an-independently-authorized-recipient/time); the parent assignee reads the child via hierarchy but is equally rejected from time/Notes/Handoffs on a coworker-only child; the parent assignee CAN create further Subtasks and receive the correct inclusive roll-up (30 own + 60 + 120 = 210, with zero raw coworker Time Entry rows exposed); a genuinely unrelated Employee is rejected everywhere; the legitimate Supervisor team path and the Superadmin path both work unrestricted; one-level nesting and context-integrity enforcement were re-confirmed unaffected. The unrelated-Supervisor scenario was not independently fixture-tested (only one real Supervisor exists in this hosted dataset) — disclosed rather than fabricated, reasoned via the unchanged `managesUser` boundary already exercised from the Employee side in every rejection case. Two real bugs were found and fixed live before acceptance: the `can_access_task_directly`/`can_user_access_company` project-access gap above, and a probe-fixture cross-contamination issue (an arithmetic check reused a task a prior sub-scenario's own real start/stop-timer test had left a genuine 1-minute residual entry on) — resolved with a dedicated clean fixture, not a product change. Everything rolled back — zero leftover fixture rows confirmed by direct follow-up queries.
- **Reporting arithmetic re-verified**: same 210m factual total (30+60+120) across three distinct Task rows via `get_client_report_weekly_evidence` (unmodified by this migration), never the inclusive roll-up figure re-added into report math.
- Mock-mode regression: a standalone script reproduced the equivalent 19 scenarios and passed identically.
- `tsc`/`eslint` clean (0 errors, same 2 pre-existing unrelated warnings); all four provider-mode builds clean.
- Files: migration `20260821200000_subtask_hierarchy_authorization_hardening.sql`; `src/lib/data/permissions.ts` (`canAccessTaskDirectly`, `canCreateHandoff`/`usersWhoCanReceiveHandoff` repointed); `src/lib/data/providers/mock/mock-tasks-provider.ts` (`requireDirectAccess`, `createSubtask`/`getTaskTimeRollup`), `mock-notes-provider.ts`, `mock-task-handoffs-provider.ts`, `mock-time-entries-provider.ts`; `src/components/tasks/task-subtasks-section.tsx` (Add Subtask visibility gate), `task-form-dialog.tsx` (inherited/locked-context read-only UX).

#### Phase 10 migration canonicalization — direct-access helpers now reproducible from migration history alone

**Status: COMPLETE and MANUALLY ACCEPTED (folded into Phase 10).**

A narrow follow-up, the exact same category of lesson already learned in Phase 9's own migration-canonicalization pass: migration-VERSION alignment (`migration list` showing Local↔Remote matching through a timestamp) does not by itself prove SQL-CONTENT reproducibility. `20260821200000` had received a live, ad-hoc `create or replace function can_access_task_directly(...)` correction after it was first pushed (see the hierarchy-authorization-hardening write-up above) — the remote database had the corrected body, but the local migration file still read as originally written. Left uncorrected, replaying the migration history from scratch would NOT reproduce the database's actual current state.

- `20260821200000_subtask_hierarchy_authorization_hardening.sql`'s local file was restored to accurately represent the SQL that was actually applied when that migration first ran remotely (`can_access_task_directly` as a thin wrapper over `can_user_access_task(auth.uid(), target_task_id)`, including its original comments) — no already-applied migration is edited to reflect a later fix; migration history now matches remote history exactly. `20260821200000` was NOT pushed again.
- The real, pre-existing root cause lives forward-only in a new migration, `20260821210000_subtask_direct_access_canonical_fix.sql`:
  - **`can_user_access_company(candidate_id, target_company_id)`** — brought to full structural parity with the current, auth.uid()-based `can_access_company`: superadmin, internal Company, `user_companies`, Project owner, and Project member, each with the same "candidate themselves, or candidate is a Supervisor whose direct report holds that row" extension the original `user_companies` branch already had. Company/Project access parity only — no Task hierarchy semantics added.
  - **`can_user_access_task(candidate_id, target_task_id)`** — re-asserted with its existing, already-correct, direct-only body (no logic change needed — it already delegated to `can_user_access_company`, so fixing that dependency alone corrects it); re-created rather than left silently alone so migration history documents it was audited.
  - **`can_access_task_directly(target_task_id)`** — reverted to canonically delegate to `can_user_access_task(auth.uid(), target_task_id)` again, now that the dependency chain is provably correct. One authorization rule lives in one place instead of two independent copies of the same logic.
- **Verified against the hosted database** (rollback-safe, real seed users, real `authenticated`-role RLS): a Handoff-recipient-eligibility probe proved the fix's actual purpose — an Employee (Sam) whose only route to a Company is Project membership (not `user_companies`) now correctly appears via `list_handoff_candidates` and can legitimately receive a Handoff via `create_task_handoff`; the same Employee, visible to an unrelated Parent Task only through hierarchy read (assigned to one of its Subtasks), correctly does NOT appear as a candidate for that Parent. A compact 12-scenario re-run of the full direct-access regression (hierarchy read both directions, every mutation/side-effect rejection for a hierarchy-only viewer, normal operation on one's own Task, the Supervisor/Superadmin paths, one-level nesting) and the roll-up/reporting-arithmetic proof (30+60+120=210, three distinct Task rows, never 390) all passed — empirically confirming the now-delegating `can_access_task_directly` is equivalent to the previously-proven self-contained body, so no fallback migration was needed. Everything rolled back — zero leftover fixture rows confirmed by direct follow-up queries.
- Live `pg_get_functiondef` output for `can_user_access_company`/`can_user_access_task`/`can_access_task_directly` confirmed to match `20260821210000`'s canonical SQL exactly, with no ad-hoc-only database state remaining.
- Function grants unchanged in shape: all three revoke `public`/`anon`, grant `authenticated`+`service_role` (required — they're called from RLS policies and from already-authenticated-callable RPCs like `list_handoff_candidates`/`create_task_handoff`).
- No TypeScript/UI source changed in this pass — migration SQL and documentation only; `tsc --noEmit` re-run clean as a lightweight sanity check (full four-build matrix not re-run, per instruction, since no source changed).

#### Phase 10 post-implementation stability fix — TaskDrawer infinite recursion

**Status: COMPLETE and MANUALLY ACCEPTED (folded into Phase 10).**

During manual acceptance, the user reported the dashboard becoming completely unresponsive ("Page isn't responding" in both Chrome and Edge) immediately after sign-in, with no console error and no network involvement — an initial investigation into the Supabase Auth client (lock/deadlock audit, `onAuthStateChange` audit, isolated endpoint testing outside the app) correctly ruled out the entire auth layer as the cause before this was found. Root cause: `TaskDrawer` (`src/components/tasks/task-drawer.tsx`), added by Phase 10's Subtask-drawer-stacking feature ("a Subtask opened from this drawer's own Subtasks section stacks a second TaskDrawer on top"), unconditionally rendered a nested `<TaskDrawer>` of **itself** at the bottom of its own JSX with no base case — mirroring the safe "always-mounted, controlled by an open/id prop" pattern already used for the non-recursive `TaskFormDialog` "Edit" stack, which is safe only because that component isn't self-referential. Since `TaskDrawer` is mounted unconditionally on the Employee/Supervisor dashboards regardless of whether a task is selected, every render spawned an unbounded synchronous chain of nested `TaskDrawer` instances during React's render phase, before any commit — pegging the main thread with no thrown error.

**Fix (the only source change in this pass)**: the nested Subtask `<TaskDrawer>` is now lazily mounted, gated on `subtaskDrawerId != null`, instead of always rendered. A top-level Task's drawer now mounts zero nested drawers until a Subtask is actually opened; the nested instance showing that Subtask can never itself open a further-nested drawer, since `TaskDetailContent` only renders the `TaskSubtasksSection` (the sole caller of `onOpenSubtask`) when `!task.parentTaskId` — so recursion is architecturally capped at exactly two levels, matching the existing one-level Subtask data model. No auth code, RLS, or migration was touched — the bug never reached the database layer. `tsc`/`eslint` (0 errors, same 2 pre-existing warnings) and all four provider-mode builds re-run clean after this fix.

**Locked rule going forward**: any component that stacks a further instance of itself (a "drawer opens a drawer of the same type" pattern) must gate that nested render on an explicit, narrowing piece of state (e.g. `nestedId != null`) — never mount it unconditionally, even if a controlled `open`/`id` prop makes it visually safe. That pattern is only safe for non-recursive children.

## Phase 11 — Workflow Simplification & Product Polish

**Status: COMPLETE.** All four subphases (11A–11D) are MANUALLY ACCEPTED, including final validation: the "JWT issued at future" runtime error was investigated and root-caused to transient clock skew on Supabase's own hosted GoTrue/PostgREST infrastructure (not application code, not the local machine — see the JWT investigation write-up under Phase 11D below); the user confirmed by direct reload that `/dashboard/tasks` and other authenticated Supabase pages load normally with no JWT error. `tsc`/`eslint`/all four provider-mode builds passed. Checkpointed and pushed to GitHub — see "Latest verified checkpoint" below for the exact commit hash.

Four planned subphases, employee-daily-usability-first, Supervisor/Superadmin/Employee three-role model unchanged (no Account Manager role yet):

- **11A — Task Experience Redesign & Time Simplification.** Status: **MANUALLY ACCEPTED.** The user's final manual verification pass confirmed Expected/Estimated Time removal, the full Task/Subtask redesign, the segmented status rail, compact assignee UX, top-right Time Tracking, Log Time relocation, compact Time Activity, the latest-3/View-all/View-fewer history behavior, and the empty-UUID runtime fix. One small visual-only follow-up (the Edit action's placement relative to Time Tracking) was folded into the 11C implementation pass below and awaits only visual confirmation — no functional behavior changed. See below.
- **11B — Task Navigation & Quick View.** Status: **MANUALLY ACCEPTED.** Dashboard Quick View, Tasks direct full-page navigation, My Day direct navigation, Planner direct navigation, Planner Today behavior, Planner Open My Day, one-level Subtask compatibility, and general stability all passed final manual verification. See below.
- **11C — Reporting Simplification & Ownership.** Status: **MANUALLY ACCEPTED.** Audit/design and the implementation built against it both passed final manual verification. A further simplification (Review Queue/Schedules UI removal) was folded into 11D below. See below.
- **11D — Role Surface Cleanup & Cross-App Consistency.** Status: **MANUALLY ACCEPTED.** Final Phase 11 pass — Review Queue and Client Report Schedules UI removed from the normal product (backend fully preserved), Client Visits made temporarily Superadmin-only, legacy Internal Report generation retired from the UI (historical archive preserved), and a repo-wide cross-app consistency sweep. See below.

### Phase 11A — Task Experience Redesign & Time Simplification

**Status: MANUALLY ACCEPTED.**

**Task/Subtask Expected/Estimated Time is deprecated from the entire product UI.** The `tasks.expected_minutes` database column and the `TaskWithRelations.expectedMinutes`/`TaskInput.expectedMinutes`/`SubtaskInput.expectedMinutes` TypeScript fields are all **intentionally untouched** — no migration was created or is expected for this. `TaskFormDialog` (Edit mode only) and provider write paths continue to pass an existing Task's `expectedMinutes` through unchanged on save, so historical data already in storage is never silently wiped; no UI anywhere lets a person set, edit, or view it anymore for a Task or Subtask. Removed from: Add Task, Edit Task, Add Subtask (form inputs), the full Task/Subtask page and Task Drawer body (`TaskDetailContent`'s old "Expected time" field and the "Estimate vs. actual" budget bar in `TaskTimeTracking`), `TaskSummaryItem` (Planner/Dashboard KPI rows), the Supervisor/Superadmin `TeamWorkloadCard` dashboard widget's "· X est." suffix, Team Time's per-entry "Task total: X · Est. Y · Z%" review-signal line (a documented Phase 3.36 behavior, explicitly superseded by this instruction), and the Apply Template / Generate Next Occurrence preview dialogs' per-task and total "expected" display lines. **Explicitly NOT touched** (different features, out of this instruction's scope): Workstream's own "Time vs. Budget" roll-up (`time-budget.ts`, `budget-bar.tsx`, `expected-time-input.tsx` — still a real, separate Workstream-level feature that sums Task `expectedMinutes` under the hood for its own budget bar), Templates' own per-task `expectedMinutes` data field (still stored/cloned, just no longer displayed), and Daily Update's **Scheduled Time** (`daily-update-entries.tsx`/`add-manual-entry-dialog.tsx` — a completely different, user-owned/persistent field, confirmed untouched by inspection).

**Full Task/Subtask page redesign** (`TaskDetailContent`, shared verbatim by the full `/dashboard/tasks/[id]` route and the Task Drawer, so both benefit — see the Drawer note below): the old two-card "Details" + "Assignees" grid is replaced by one compact metadata rail directly under a new `TaskStatusRail` — priority, due date, activity, "Checklist X/Y" progress, and a compact assignee summary (avatar stack + name(s), or nothing at all when the sole assignee is the viewer themselves, since ownership is then obvious from context) — followed by the description and a compact created-by/status-changed-by footer line. Time Tracking is promoted to lead the body (right after the metadata rail) instead of being buried after Details/Assignees/Checklist; the full Checklist keeps its own card further down. The full page's own header (`dashboard/tasks/[id]/page.tsx`) gained the Company name at the front of its Project → Service → Activity breadcrumb (previously missing) and dropped its now-redundant Priority badge (moved into the metadata rail).

**New `TaskStatusRail`** (`src/components/tasks/task-status-rail.tsx`): a segmented five-way control — Todo | In progress | Blocked | Waiting on client | Done — reusing the exact same `STATUS_META`/`statusChipStyle` tokens every other status-colored element already uses. Deliberately NOT a percentage/progress bar: Blocked and Waiting on client are not points on a completion scale, so all five segments are equally weighted and individually clickable, never implying a false 20/40/60/80/100% semantics. Read-only (no `onChange`) when the viewer can't progress the Task.

**Time Tracking redesign — superseded by the final timer-placement correction below.** The `expectedMinutes` prop and the "Estimate vs. actual" budget bar are gone entirely from `TaskTimeTracking` — always a plain factual total now. An initial 11A pass led the body with a clock-style pill instead of the header; the user accepted the redesign but explicitly requested the timer actually live in the page header's top-right, which the combined 11A-final/11B pass below implements.

**Time entry presentation**: `TaskTimeTracking`'s entry list no longer repeats a contributor's name on every row when every entry in the list belongs to the same single person (the common case — a normal Employee viewing their own directly-authorized entries) — the avatar and name only reappear once the list actually contains more than one distinct contributor (multi-assignee Task, or a Supervisor/Superadmin viewing team context). This is a display-only change; the underlying `time_entries` query/RLS/RPC authorization is completely untouched — no coworker's raw entry becomes visible to anyone who couldn't already see it.

### Phase 11B — Task Navigation & Quick View (combined with the 11A timer-placement correction)

**Status: MANUALLY ACCEPTED.**

**Locked navigation rule** — a Task/Subtask's primary click behavior now depends on which surface it's clicked from:

| Surface | Primary click behavior |
|---|---|
| Dashboard/Home (role dashboards, KPI drill-downs) | Opens the (redesigned) Quick View Drawer |
| Tasks module — List, Grid, Board | Navigates directly to `/dashboard/tasks/[id]` |
| My Day | Navigates directly to `/dashboard/tasks/[id]` (already did — `TaskGridCard`'s own no-`onOpen` default) |
| Planner — Day/Week/Month/Group/Unscheduled | Navigates directly to `/dashboard/tasks/[id]` |
| Planner "Today" | Unchanged — jumps the calendar to today's date |
| Planner "Open My Day" (new) | Navigates to `/dashboard/my-day`, placed next to the view tabs |

**Timer architecture — one authoritative controller per rendered Task.** New `useTaskTimer(taskId, assigneeIds)` hook (`src/lib/data/hooks/use-task-timer.ts`) is the single source of truth for a Task's live timer state (entries, running/paused timer, elapsed seconds, Start/Pause/Resume/Stop handlers) — extracted from what used to be `TaskTimeTracking`'s own internal hook calls. The full Task page (`dashboard/tasks/[id]/page.tsx`) calls it exactly once and passes the same object to two presentational consumers: `TaskTimerControl` (new, the full top-right "Time Tracking" cluster — label, clock, Start/Pause/Resume/Stop, and Log time — see the final-polish write-up below) and `TaskTimeTracking` (now purely historical — see below), which now takes the shared `timer` object as a required prop instead of creating its own. Neither component polls independently, so they can never race or briefly disagree on elapsed time. Quick View uses its own separate `useTaskTimer` instance — safe because Quick View and the full page for the same Task are never mounted in the same tree simultaneously.

#### Phase 11A/11B final fix — empty-UUID crash + Time Tracking cluster relocation

**Status: MANUALLY ACCEPTED.**

**Empty-UUID runtime bug, root cause and fix.** The user reproduced `invalid input syntax for type uuid: ""` from `supabase-time-entries-provider.ts`'s `listTimeEntriesForTask`, via `useTaskTimeEntries` inside the new `useTaskTimer` hook. Root cause: both `TaskDrawer` and the full Task page called `useTaskTimer(task?.id ?? "", ...)` **unconditionally at the top of the component**, before `task` was guaranteed loaded — a pattern that's normally safe in this codebase (`useTask(taskId ?? "")` already does this, but `getTask` has its own established `if (!id) return null` guard, confirmed identical to `getCompany`/`getWorkstream`). `listTimeEntriesForTask` has no such guard — it queries `.eq("task_id", taskId)` unconditionally — so passing `""` fired a real query with an invalid UUID on every mount while the Task was still loading. **Fix**: both `TaskDrawer` and `dashboard/tasks/[id]/page.tsx` were split into an outer loading/not-found wrapper plus a `LoadedTaskQuickView`/`LoadedTaskDetailPage` child component that only ever mounts once `task` is a real, loaded, non-null object — so `useTaskTimer` (and everything inside it) is never called with an empty id, while still satisfying the Rules of Hooks (the child component's own hooks are always called consistently, since it's never rendered without a real `task`). A repo-wide audit of every `?? ""` pattern confirmed these were the only two call sites feeding a UUID-filtered query without an existing guard — everything else either already has a `getX`-style empty-id guard (pre-existing, unrelated to Phase 11) or is a form-state/display default, never a live query.

**Time Tracking cluster relocated to the header, in full** (`TaskTimerControl`, rewritten): now the single top-right "Time Tracking" cluster containing the label, the clock/readout (`H:MM:SS` while running, plain total otherwise), Start/Pause/Resume/Stop, **and Log time** (moved here from the body — `ManualTimeEntryDialog` is now owned entirely by this component, so its `onSaved` calls the shared `timer.refreshAll`, refreshing the header total, the Quick View clock, and the Time Activity list from one save with no page reload). The cross-task notices ("Running on X — starting here will pause it" / "X is paused — go resume it") also moved here, compact and right-aligned, out of the historical Time Activity section entirely. A `variant` prop (`"header"` default, `"compact"` for Quick View) controls how much renders: Quick View gets just the clock + Start/Pause/Resume/Stop (still reading the same one-instance-per-render architecture, just its own separate `useTaskTimer` call since Quick View and the full page never coexist for the same Task) — no label, no Log time, no cross-task notices, no history of any kind.

**Time Activity is now purely historical.** `TaskTimeTracking` no longer renders any action button at all (Start/Pause/Resume/Stop/Log time all live in the header) — just the "Total X" / billable-non-billable summary line and the entries list, unchanged from the collapsed-by-default (latest 3, "View all N entries"/"View fewer" inline expand/collapse) design. `TimeEntryCorrectionInfo` (a compact "Corrected" badge that expands its detail on demand) was already exactly this pattern and needed no changes.

**Files changed** (this fix): `src/lib/data/hooks/use-task-timer.ts` (unchanged internally, callers fixed), `src/components/tasks/task-drawer.tsx` (split into `TaskDrawer` + `LoadedTaskQuickView`), `src/app/dashboard/tasks/[id]/page.tsx` (split into `TaskDetailPage` + `LoadedTaskDetailPage`), `src/components/tasks/task-timer-control.tsx` (rewritten — full cluster + `variant` prop + owns `ManualTimeEntryDialog`), `src/components/tasks/task-time-tracking.tsx` (action buttons and cross-task notices removed, purely historical), `src/components/tasks/task-detail-content.tsx` (simplified `TaskTimeTracking` call).

- `tsc`/`eslint` clean (0 errors, same 2 pre-existing unrelated warnings); all four provider-mode builds re-run clean.
- No migration created or needed — this was a pure client-side hook-ordering fix and UI relocation, no SQL/RLS touched.

**Final visual polish (folded into the Phase 11C implementation pass)**: the Edit action on the full Task page was visually stacked directly beneath the Time Tracking cluster, reading as if it were part of the timer. Moved to sit in the title row instead (next to the Company/Project/Service/Activity breadcrumb), with the Time Tracking cluster as its own separate right-aligned block below — Edit is now unambiguously a page-level action, never underneath Log Time. `canEditTask` gating unchanged.

**Quick View Drawer redesign** (`src/components/tasks/task-drawer.tsx`, same external prop interface — `taskId`/`onOpenChange`/`onChanged`/`onTimerChanged` — so every Dashboard caller needed zero wiring changes): now genuinely lightweight — title, SUBTASK badge + parent link, compact Company→Project→Service→Activity context, status/priority/due date badges, compact assignee summary (hidden for a self-only assignee, same rule as the full page), a two-line-clamped description, "Checklist X/Y" and "Subtasks X/Y" (a count only — no per-Subtask row list), the compact `TaskTimerControl`, and a footer with "Edit" (gated on real `canEditTask` authorization, never shown merely because hierarchy-read visibility let the viewer see the Task) and "Open full task" (`Link` to the full page). Deliberately does NOT reproduce: the full Checklist editor, Notes history, Handoff history, full Time Entry history, or a per-Subtask list — those stay full-page-only. `TaskDetailContent` is no longer shared with the Drawer at all (it's full-page-only now); the two intentionally diverge.

**Subtask navigation, Phase 10 recursion safety**: clicking a Subtask row anywhere it's listed (the full page's `TaskSubtasksSection`) now always uses `TaskRow`'s default `Link`-to-full-page behavior (the `onOpenSubtask`/Sheet-on-Sheet nested-Drawer stacking mechanism from Phase 10 was removed entirely, since Quick View no longer lists individual Subtasks to click). Quick View shows only a Subtask **count**, never individual rows, so there is no click target inside Quick View that could open another Drawer — Phase 10's self-recursion bug is now structurally impossible here, not merely guarded against. (The historical guard itself, `{subtaskDrawerId != null && <TaskDrawer .../>}`, was superseded by this rewrite — the old nested-Drawer code path no longer exists at all.)

**Board click-vs-drag**: unchanged, already correct — `TaskBoard` already defaulted to `router.push` navigation when no `onOpenTask` override was passed, and `dnd-kit`'s `PointerSensor` `activationConstraint: { distance: 8 }` already ensures a short click registers as a click (navigate) rather than a drag, so removing the Tasks-module drawer override required no new drag/click disambiguation logic.

**Repo-wide navigation audit** (Section 25): every `TaskDrawer`/`onOpen`/`router.push` Task click site was inventoried. Dashboard/Home (`employee-dashboard.tsx`, `supervisor-dashboard.tsx`, `superadmin-dashboard.tsx`, `task-kpi-detail.tsx`, `task-status-focus-content.tsx`) — Quick View, unchanged wiring. Tasks module (`dashboard/tasks/page.tsx`) — the old `?task=` query-param Drawer mechanism (`openDrawer`/`closeDrawer`/`pushedDrawerEntry`) removed entirely; List row, Grid, and Board now navigate directly. Planner (`dashboard/planner/page.tsx`) — `drawerTaskId` state and the `<TaskDrawer>` instance removed; all five child views (Day/Week/Month/Group/Unscheduled) now navigate directly via a shared `openTask` function. My Day (`bucket-task-grid.tsx`) — already correct, no changes needed.

**Files changed** (11A-final + 11B): new `src/lib/data/hooks/use-task-timer.ts`, `src/components/tasks/task-timer-control.tsx`; rewritten `src/components/tasks/task-drawer.tsx` (Quick View), `src/components/tasks/task-time-tracking.tsx` (history-only, shared-timer prop); `src/components/tasks/task-detail-content.tsx` (takes `timer` prop, no longer shared with the Drawer); `src/app/dashboard/tasks/[id]/page.tsx` (owns the one `useTaskTimer`, renders `TaskTimerControl` top-right); `src/app/dashboard/tasks/page.tsx` and `src/app/dashboard/planner/page.tsx` (navigation rewired, drawer mechanisms removed; Planner gained "Open My Day"); `src/components/tasks/task-subtasks-section.tsx` (`onOpenSubtask` prop removed); doc-comment accuracy fixes in `task-row.tsx`/`task-grid-card.tsx`/`task-board.tsx`.

- `tsc`/`eslint` clean (0 errors, same 2 pre-existing unrelated warnings); all four provider-mode builds (`supabase`, `supabase-core`, `supabase-auth`, `mock`) re-run clean since this touched React/UI source.
- No migration created or needed — no SQL/RLS was touched, navigation/timer-placement is a pure client-side change.

## Phase 11C — Reporting Simplification & Ownership

**Status: MANUALLY ACCEPTED.** The audit below (produced by tracing the actual live RPC/provider code, not docs) remains accurate; the implementation section further down records what was built against it. A further simplification (removing the Review Queue tab and Client Report Schedules UI entirely) was requested after acceptance and implemented as part of Phase 11D below — see that section for the final Reports UX. No reporting migration, RLS policy, or RPC was touched at any point.

**Two separate concepts exist today and are visually conflated**: "Internal Reports" (aka "Accomplishments Reports" — a fully-attributed, per-person checklist document) and "Client Reports" (client-facing, Draft/Finalized lifecycle, anonymized). Both share one `ReportTypeTabs` toggle at `/dashboard/reports` and `/dashboard/reports/client`, and Internal Reports currently have an unconditional, ungated sidebar entry ("Reports") visible to every role, plus surface into the Supervisor dashboard's "Reports Awaiting Review" card and both Employee/Superadmin My Day "Needs attention" strips.

**Confirmed independent — Internal Reports can be hidden safely.** `get_client_report_weekly_evidence`/`generate_client_report` (the real Client Report generation path, both RPC and TypeScript) read only `projects`/`tasks`/`workstreams`/`time_entries`/`daily_updates` — zero references to `accomplishments_reports` anywhere in either the SQL or the TypeScript provider/aggregation code (`client-report-weekly.ts`'s `computeWeeklyReportSections`). Hiding Internal Reports from normal Employee/Supervisor UI cannot break Client Report generation.

**Confirmed — Client Reports already aggregate multiple Employees into one report, already anonymized.** `get_client_report_weekly_evidence`'s evidence query is `group by te.task_id, work_date` with `sum(te.duration_minutes)` — no `user_id` in the grouping or the selected columns at all, so two different Employees' logged time on the same (or different) Tasks under one Project/period already lands in ONE generated report, summed correctly, with zero possibility of the browser or the export learning who logged what. `ClientReportLineItem` (the stored/exported shape) carries no contributor field; CSV/PDF export code (`client-report-export.ts`, `window.print()`) explicitly never reads the row's own `generated_by_name`/`comments`/`history` (which DO store staff identity, but only for internal audit — the generator/reviewer, never the underlying contributors). **The problem is confirmed to be UI/mental-model only, not a data or aggregation gap** — the underlying architecture already does what the boss wants; the presentation just doesn't make that legible today (a stat-card-dominated list rather than a clear client/project → period → generate → review → export flow).

**Finalized immutability is real and multiply enforced**: both `update_client_report_draft` and `update_client_report_draft_wording` RPCs reject any row where `status <> 'draft'`; `reopen_client_report`'s EXECUTE privilege was explicitly revoked from `authenticated` in a dedicated migration; `canReopenClientReport()` unconditionally returns `false` in TypeScript. No code path exists back to Draft once Finalized. (Internal/Accomplishments Reports remain a separate, still-reopenable lifecycle — unaffected, and fine to leave that way since they're not the client-facing artifact.)

**Current authorization** (all capability-based, not role-based — no 4th role needed or proposed): generating a Client Report requires only Project access (`canGenerateClientReport` = `canAccessProject`, open to any Employee/Supervisor/Superadmin who can see the Project) — already matches the desired "Employees contribute, report belongs to the Project" model. Finalizing requires `reporting_review_access` (Superadmin always has it; any role can be explicitly granted it via a Superadmin-only `set_reporting_review_access` RPC) — and once granted, it is **organization-wide**, not scoped to "their own team" (a Supervisor without the flag can never finalize even a direct report's own draft; this was hardened across several historical hotfixes into its current, correct, capability-only form). Export has no separate permission gate — anyone who can view the report can export it. Trash/restore is owner-or-Superadmin; permanent delete is Superadmin-only and requires the row already be trashed.

**Trash is soft-delete only, not actually purged.** Both trash pages show a client-side "auto-deletes after 30 days" label, but no cron/scheduled purge job exists anywhere in the migrations — permanent deletion only ever happens via the explicit "Delete forever" button. This is a pre-existing, harmless discrepancy (nothing is silently destroyed) worth knowing but not urgent to fix as part of 11C.

### Phase 11C implementation (built against the audit above)

**Locked decisions, now implemented**: the normal Reports route is Client Reports; Internal/Accomplishments Reports are removed from everyday Employee/Supervisor workflow (historical backend fully preserved); no fourth role was introduced; `reporting_review_access` remains an orthogonal capability, never conflated with legacy Internal Report access; a report is presented as Client/Project/Period, not by who generated it; multi-Employee aggregation is completely unchanged; the normal Reports page no longer shows Total/Draft/Finalized KPI cards; generation is now centered around Client + Project + Period; finalized reports remain immutable; no migration was created; the false "auto-deletes after 30 days" Trash copy was corrected to truthful wording; Client Report exports remain contributor-name-free.

**Reports navigation**: the sidebar's "Reports" item (`app-sidebar.tsx`) now points to `/dashboard/reports/client` — the normal product no longer lands on Internal Reports. Label stays "Reports" for sidebar consistency.

**Internal/Accomplishments Reports removed from normal UX**: `/dashboard/reports`, `/dashboard/reports/[id]`, and `/dashboard/reports/trash` each gained a Superadmin-only gate (placed after all hooks, so Rules of Hooks holds) — any other role hitting these routes directly now sees a short "Internal Reports have moved — use Client Reports" message with a link to `/dashboard/reports/client`, instead of the real page content. The `accomplishments_reports` table, its rows, RLS, RPCs, and provider interfaces are all completely untouched — this is a UI-exposure gate only, not a backend change. `ReportTypeTabs` was removed from the normal Client Reports page (`/dashboard/reports/client`) but kept on the now-Superadmin-only Internal Reports page, so Superadmin can still navigate between the two legacy/normal areas — the component is not orphaned, still genuinely in use by one route.

**Dashboard/My Day surfaces**: `ReportsAwaitingReviewCard` (Supervisor dashboard) was removed — it was the only caller of `useAccomplishmentsReports`/`isAccomplishmentsReportOwner` in that file, and the component itself became genuinely unused everywhere, so it was deleted outright rather than left orphaned. `NeedsAttentionStrip`'s "reports to review" chip category (fed by `teamReports`) was removed from the shared component and both call sites (`supervisor-my-day.tsx`, `superadmin-my-day.tsx`) — Client Report reviewers now use the Review Queue on `/dashboard/reports/client` instead. No new Client Report widget was added in its place (the simpler, explicitly-preferred option).

**Client Reports page redesign** (`/dashboard/reports/client/page.tsx`, full rewrite): the Total/Draft/Finalized `StatCard` row and the Internal/Client `ReportTypeTabs` toggle are both gone. A prominent "Generate a report" card now leads the page, reusing `GenerateClientReportDialog` verbatim (no generation logic duplicated or rewritten) via a large "Generate Report" button — generation is now the page's organizing action, not a small header button. Below it: My/Others/Review Queue/Schedules tabs (unchanged authorization — `canViewOthersClientReports`/`hasReportingReviewAccess`), then a "Recent Reports" heading and table. Trash moved to a smaller secondary button in the page header.

**`ClientReportsTable` redesign**: added a **Project** column (resolved client-side from `useProjects()` — the same already-access-scoped hook the generate dialog uses, no new data access, no provider change) and renamed **Range → Period**, so a row now reads Client → Project → Period → Status → Generated by → Generated, with Status kept as a small badge (never the organizing identity) and a trailing chevron as the row's "Open" affordance (the whole row remains clickable, matching every other table in the app). "Generated by" is kept — this is an internal, staff-only list view that never leaves the app (confirmed by the audit's export-code read), so it remains legitimate audit context, never the report's headline identity.

**Client Report detail page**: read in full and left unchanged — its heading is already `report.companyLabel` (Client) as the H1, with Brand/Project/Period/Generated-by as a smaller metadata line below, not the headline. This already satisfied "Client/Project/Period, not generator ownership" with no edit needed (confirmed rather than assumed).

**Trash copy correction** (Section 20, both `/dashboard/reports/trash` and `/dashboard/reports/client/trash`): the false "Deleted reports stay here for 30 days before they're permanently removed" claim — untrue, since no purge cron/job exists anywhere in the migrations — was replaced with "Deleted reports stay here until restored or permanently deleted." The "Auto-deletes" column (a computed future date implying automated deletion) was replaced with a truthful "Deleted" column showing the actual `deletedAt` timestamp. Soft-delete mechanics, Restore/Delete-forever permissions (owner-or-Superadmin trash/restore, Superadmin-only permanent delete), and all underlying RLS/RPCs are completely unchanged.

**Explicitly unchanged, verified by re-reading rather than assumed**: `canGenerateClientReport`/`generate_client_report` (Project-access generation rule), `get_client_report_weekly_evidence`'s grouping/anti-double-counting contract, `finalize_client_report`/`update_client_report_draft`/`update_client_report_draft_wording`'s immutability guards, `reopen_client_report`'s revoked EXECUTE privilege, `canReopenClientReport()`'s hardcoded `false`, `client-report-export.ts`'s CSV columns and `window.print()` PDF path (no `generatedByName` added anywhere in export), and Trash/Restore/Permanent-delete authorization (`canTrashClientReport`/`canRestoreClientReport`/`canPermanentlyDeleteClientReport`).

**Files changed**: `src/components/dashboard/app-sidebar.tsx` (Reports nav destination); `src/app/dashboard/reports/page.tsx`, `src/app/dashboard/reports/[id]/page.tsx`, `src/app/dashboard/reports/trash/page.tsx` (Superadmin-only gates; trash copy fix); `src/app/dashboard/reports/client/page.tsx` (full redesign); `src/app/dashboard/reports/client/trash/page.tsx` (trash copy fix); `src/components/client-reports/client-reports-table.tsx` (Project column, Period rename, Open affordance); `src/components/dashboard/supervisor-dashboard.tsx` and `src/components/my-day/{needs-attention-strip,supervisor-my-day,superadmin-my-day}.tsx` (Internal Report attention removed); deleted `src/components/dashboard/reports-awaiting-review-card.tsx` (confirmed genuinely unused after removal).

**Migrations**: none. **Provider abstraction**: untouched — mock/`supabase-auth`/`supabase-core`/`supabase` parity preserved, since no provider method was added, removed, or changed.

- `tsc`/`eslint` clean (0 errors, same 2 pre-existing unrelated warnings); all four provider-mode builds re-run clean.

**Explicit 11C answers**: (1) Yes, multi-Employee aggregation into one Project/period report is already real, proven via `get_client_report_weekly_evidence`'s `group by task_id, work_date` with no `user_id` — unchanged by this implementation. (2) Yes, Employee names are excluded from all client-facing output — unchanged. (3) No, Client Report generation has zero dependency on `accomplishments_reports`. (4) Yes, Internal Reports are now Superadmin-only via a UI gate, historical rows/RLS/RPCs fully preserved. (5) Yes, the stat cards are gone from the normal page; the backend Draft/Finalized column is untouched. (6) Yes, finalized immutability was re-verified unchanged, not touched. (7) Generate: any Employee/Supervisor/Superadmin with Project access (unchanged). (8) Review: anyone with `reporting_review_access` (Superadmin always) — org-wide, unchanged. (9) Finalize: same `reporting_review_access` gate (unchanged). (10) Export: anyone who can already view the report (unchanged). (11) Yes, `reporting_review_access` remains an orthogonal capability flag, never a role, and was never used as a reason to expose legacy Internal Reports. (12) No migration was created. (13) See "Files changed" above. (14) Draft/Finalized backend semantics, all RLS/RPC authorization, both report schemas, export logic, soft-delete mechanics — none changed. (15) Yes, implemented and ready for the user's manual verification pass.

## Phase 11D — Role Surface Cleanup & Cross-App Consistency

**Status: MANUALLY ACCEPTED.** The final Phase 11 pass — no reporting/Visit migration, RLS policy, or RPC was touched.

**Review Queue and ownership tabs removed.** `useClientReports()` (`listReports` + the `can_view_client_report` RLS policy) already returns exactly the set the current viewer is authorized to see — an Employee's own, a Supervisor's team + own, a `reporting_review_access`/Superadmin viewer's org-wide set. The Client Reports page (`/dashboard/reports/client`) now renders that authorized result directly with no client-side tab partitioning at all: "My Reports"/"Team's Client Reports"/"All Client Reports"/"Review Queue" are all gone. A reporting reviewer locates Drafts the same way every viewer filters — the existing compact Status dropdown (All/Draft/Finalized) — then opens the report and reviews/comments/finalizes on its own detail page, which remains completely unchanged as the review workspace. `reporting_review_access` itself, `canViewClientReport`/`canFinalizeClientReport`/`canEditClientReportWording`/`canCommentOnClientReport`, and the underlying RLS were not touched — only the page's client-side presentation of an already-correctly-scoped list changed.

**"Generated by" removed from the normal Recent Reports table.** `ClientReportsTable` no longer has a "Generated by" column — a report's identity is Client/Project/Period, not who generated it. That identity is still stored (`generatedByName` untouched on the type/row) and still shown on the report detail page's own metadata line (internal audit context, confirmed unchanged); it's simply not a column on the shared list everyone sees. Table columns are now Client/Project/Period/Generated/Status/Open(chevron).

**Client Report Schedules UI removed from the normal product.** The "Schedules" tab and its `ClientReportSchedulesPanel` link were removed from `/dashboard/reports/client` along with the ownership tabs above. The entire scheduling backend — `client_report_schedules` table, `create_client_report_schedule`/`update_client_report_schedule`/`delete_client_report_schedule`/`run_client_report_schedule_now`/`run_due_client_report_schedules` RPCs, the pg_cron job, the DST-safe next-occurrence calculation, the previous-7-completed-days rule, Draft-only automatic generation, and `canManageClientReportSchedules` — is completely untouched. `ClientReportSchedulesPanel`/`schedule-form-dialog.tsx`/`use-client-report-schedules.ts`/the schedules provider files were deliberately NOT deleted, even though they now have zero importers — this is a complete, working feature that's UI-hidden by product decision, not dead code from a refactor, and keeping it intact makes re-linking it (e.g. into a future Superadmin settings area) trivial.

**Active-schedule safety check — could not be safely determined.** Two read-only attempts were made: `npx supabase migration list` succeeded (confirming the CLI can reach the linked hosted project), but `npx supabase db dump` (the only available read path to actual row data) requires a local Docker daemon, which is not running in this environment, and failed immediately with a Docker-connection error before touching the remote database. No query against `client_report_schedules` ever executed, no scratch file retained any data (the empty output file was deleted). **The exact count of currently-active schedule records is unknown** — this is reported honestly rather than invented. Regardless of the count, nothing was deleted, paused, or otherwise mutated; `run_due_client_report_schedules`'s pg_cron job continues running exactly as before, unaffected by a UI change on an unrelated page.

**Client Visits made temporarily Superadmin-only.** `DailyVisitHoursCard` (the only Visit UI surface anywhere in the app — there was never a dedicated Visit route to gate) was removed from `EmployeeMyDay`/`SupervisorMyDay` and added to `SuperadminMyDay` instead, reusing the exact same component/dialogs (`AddVisitDialog`/`RecordVisitHoursDialog`) with zero logic changes — Superadmin keeps a fully working, testable Plan → Complete Visit workflow rather than an orphaned backend. **This is a UI-only product boundary, not a new server-side restriction** — `canEditVisitEntry`/`canViewVisitEntriesForUser`/`canDeleteVisitEntry` in `permissions.ts` were read in full and are unchanged: Visit create/edit has always been self-service for any role by original Phase 9F design ("Section 23's own explicit instruction" that a Supervisor's Visit workflow is not manager-only), viewing is self/managed-report/Superadmin (mirroring `canViewTimeForUser`), and delete is owner-or-Superadmin. There is no existing "Superadmin-only" concept at the RLS/RPC layer for Visits, and none was added — an Employee/Supervisor calling `createVisitEntry`/`completeVisitEntry` directly (bypassing the UI) would still succeed server-side today. Achieving genuine server-side Superadmin-only enforcement would require a new RLS policy change on `visit_entries`' INSERT/UPDATE, which was correctly NOT made without explicit approval, per instruction. No dedicated Visit route existed to gate — the only surface was the My Day card, now fully absent from Employee/Supervisor's rendered component tree.

**Visit backend, evidence, and integrity fully preserved.** Visit tables, providers, RPCs, planned→actual workflow, Record Start/End, completion semantics, the overlap-protection rule (against the same user's completed Task time, running timer, and other Completed Visits), and Client Report Visit evidence (`dailyVisitMinutes`, already-aggregated, contributor-free, no double-counting) were all confirmed untouched by direct inspection — none of this was in scope to change, and none of it was touched.

**Legacy Internal Report generation retired from the UI (final decision).** Following the user's final review, `/dashboard/reports` (Superadmin-only since 11C) was repositioned from "Internal Reports" to **"Legacy Internal Reports"**, with copy stating plainly that it is a historical archive and that no new reports are generated there. The **"Generate report" button, its `Plus` import, and the `GenerateReportDialog` mount were removed from the page** — there is no longer any UI path, for any role including Superadmin, to create a new Internal/Accomplishments Report. Viewing, searching, filtering, editing while still Draft, Finalizing, exporting (CSV/print), and Trash (view/restore/permanent-delete) are all unchanged — this is archive access, not a frozen read-only view. **Nothing was deleted**: the `accomplishments_reports` table, its rows, RLS policies, the generation RPC, the `GenerateReportDialog` component/`useAccomplishmentsReports` hook (still used by the list/detail/trash pages for viewing), and all historical comments/history remain fully intact — `GenerateReportDialog` simply has zero importers now, left in place deliberately (same "preserve backend, remove UI entry point only" pattern already used for Client Report Schedules in this same phase) so re-linking it is trivial if ever needed. A repo-wide grep confirmed no other entry point (dashboard, sidebar, command palette, keyboard shortcuts) ever offered legacy report generation, so no other file needed a change for this. No migration.

**Cross-app consistency sweep — two genuine stale links found and fixed** (everything else re-verified clean, not merely assumed): (1) the Ctrl+K command palette's "reports" search category was still built from `useAccomplishmentsReports()` (legacy Internal Reports) and linked to `/dashboard/reports/${id}` — for a normal Employee/Supervisor, any of their own past Internal Reports would surface in search results linking to a route now gated away from them. Swapped to `useClientReports()`/`/dashboard/reports/client/${id}`, matching the locked "Reports = Client Reports" model. (2) `NAV_SHORTCUTS`' "r" keyboard shortcut and its `NAV_ICONS` palette-actions entry still pointed at `/dashboard/reports` (its own doc comment even says it "mirrors AppSidebar's own nav items exactly" — it had drifted from the sidebar's own Phase 11C update). Both now point to `/dashboard/reports/client`. Re-verified, found already correct, no change needed: Task/Subtask Expected Time absence (repo-wide re-grep, only the two intentionally-preserved historical-data form-state fields remain), Task navigation rules (Dashboard→Quick View, Tasks/My Day/Planner→full page, Planner Today/Open My Day), the full Task page header layout, one-level Subtask behavior, `notification-links.ts`'s report-comment routing (a notification for an Internal Report correctly hits the same Superadmin-only gate any other direct link would — consistent, not broken), and the Settings→Workspace `reporting_review_access` toggle (unrelated admin control, untouched).

**Role consistency**: exactly three roles remain (Employee/Supervisor/Superadmin); no Account Manager, Reviewer, or Visit Manager role was introduced; `reporting_review_access` remains a capability flag on any of the three roles, never used to expose either legacy Internal Reports or Client Report Schedules.

**Files changed**: `src/app/dashboard/reports/client/page.tsx` (ownership tabs/Review Queue/Schedules removed, single authorized list); `src/components/client-reports/client-reports-table.tsx` ("Generated by" column removed); `src/components/my-day/employee-my-day.tsx`, `src/components/my-day/supervisor-my-day.tsx` (`DailyVisitHoursCard` removed); `src/components/my-day/superadmin-my-day.tsx` (`DailyVisitHoursCard` added); `src/components/my-day/daily-visit-hours-card.tsx` (doc comment only); `src/components/my-day/needs-attention-strip.tsx` (stale comment fix); `src/components/dashboard/command-palette.tsx`, `src/lib/keyboard-shortcuts.ts` (stale Internal-Report/legacy-route links fixed); `src/app/dashboard/reports/page.tsx` (repositioned as "Legacy Internal Reports," generate action removed).

**Migrations**: none. **Provider abstraction**: untouched. **Deleted**: nothing this pass — `ClientReportSchedulesPanel` and its supporting files are intact but currently unlinked, by design.

- `tsc`/`eslint` clean (0 errors, same 2 pre-existing unrelated warnings); all four provider-mode builds re-run clean.

### JWT "issued at future" runtime error — investigation and root cause

A `listTasks()` Supabase request (`supabase-tasks-provider.ts`, a plain `select("*")`, no custom auth logic) surfaced a "JWT issued at future" runtime error immediately before this phase's intended checkpoint. Investigated per a strict do-not-hack protocol (no swallow/retry/anon-fallback/hardcoded-token/bypass allowed):

- **Code review** (`supabase-tasks-provider.ts`, `use-tasks.ts`, `src/lib/supabase/client.ts`/`server.ts`, `src/proxy.ts`, `auth-context.tsx`, `supabase-auth-provider.ts`) found no bug — every file uses standard, Supabase-documented session/SSR APIs; nothing performs custom JWT verification, token minting, or RLS bypass.
- **Windows clock check**: `Get-Date -Format o` compared twice, back-to-back, against a trusted external HTTP `Date` header — matched to the second both times. `w32tm /query /status` showed a healthy NTP sync (source `time.windows.com`). **Local machine clock skew was ruled out** as the cause.
- **Root cause identified**: this is the documented Supabase `PGRST303` condition ("JWT issued at future"), which originates from **clock skew between Supabase's own hosted GoTrue (token-issuing) and PostgREST (token-validating) infrastructure** — not from the client, not from this app's code, and not from the local development machine. GoTrue stamps a fresh token's `iat` using its own server clock; if the PostgREST node validating that token is running even a few seconds behind at that moment, it rejects an otherwise-valid, freshly-issued token. This is a known, transient, self-resolving condition on Supabase's managed infrastructure (confirmed against the public Supabase discussion describing the identical error code/message).
- **No code change was made or needed** — there was nothing to fix in this repository. RLS was not touched, no auth/session workaround was added, no token was hardcoded or logged.

## Next roadmap

**Phase 11 in its entirety (11A–11D) is COMPLETE and MANUALLY ACCEPTED** — see the phase's own sections above, including 11D's final decisions (Client Visit restriction is intentionally UI-only, no Visit RLS migration; legacy Internal Reports are Superadmin historical-archive-only with new generation removed from the UI; Review Queue and Client Report Schedules UI removed, both backends fully preserved; exactly three roles) and the JWT "issued at future" investigation (root-caused to transient Supabase hosted-infrastructure clock skew, confirmed resolved by the user reloading the authenticated app). Checkpointed and pushed to GitHub — see "Latest verified checkpoint" for the exact hash. **Phase 12 has not been discussed or scoped.**

## Known deferred / do not build now

- No configurable roles/permission matrix (three fixed roles only).
- No deep/infinite sub-task nesting (one level, when built).
- No employee surveillance, screenshots, or idle tracking.
- No geolocation/attendance/clock-in.
- No AI auto-generated reports, estimates, or summaries.
- No duplicate manual Workstream expected-time field (it stays Task-derived).

## Do not regress

- Do not flatten Client → Workstream → Task back into Client → Task.
- Do not put Task-shaped fields (expected time, due date, checklist) back onto Workstream.
- Do not restore a manual `Workstream.expectedMinutes` — Workstream time budget stays Task-derived.
- Do not broaden Supervisor to organization-wide visibility.
- Do not make personal time-logging manager-based again — it is assignee-only, for every role.
- Do not let a Supervisor/Superadmin start/pause/resume/stop another user's live timer.
- Do not silently overwrite a corrected time value without preserving audit history.
- Do not compare one individual TimeEntry against a whole Task's `expectedMinutes` — always cumulative Task actual vs. Task estimate.
- Do not introduce multiple Workstream/Activity IDs on a single Task (`workstreamIds[]`/`activityIds[]`).
- Do not create a second Supabase organization or project — reuse the **Corebridgex** organization's **corebridgex-crm** project.
- Do not expose `role`/`supervisor_id`/`active` as ordinary client-editable profile columns (in RLS or in the UI) — they're authorization-sensitive and, in the schema, reachable only through superadmin-gated RPCs (`admin_set_user_role`/`admin_set_supervisor`/`admin_set_active`).
- Do not treat `src/proxy.ts` (Next.js 16's renamed `middleware.ts`) as an authorization layer — it only refreshes the session cookie; RLS is the real access control.
- Do not add per-provider environment variables (`NEXT_PUBLIC_AUTH_PROVIDER`, etc.) — there is exactly one source of truth, `NEXT_PUBLIC_DATA_PROVIDER`, with exactly four valid values (`mock`/`supabase-auth`/`supabase-core`/`supabase`) centralized in `src/lib/data/provider-mode.ts`. `supabase-core` is itself a deliberate exception approved for this exact transitional purpose — do not add a second one. All 14 providers are now real under `supabase`, so `supabase-core` could be retired, but do not remove it without explicit instruction — its semantics must stay exactly as they are (Auth + the original 6 core providers real, everything else mock) until the user says otherwise.
- Do not derive a real user's role/active/supervisorId/assignedCompanyIds from JWT claims or `user_metadata` — always from `public.profiles`/`public.user_companies`, read through the signed-in user's own session (never a service-role client).
- Do not let `public.profiles.email` diverge from the real Supabase Auth sign-in email — an email-change request must be rejected until a real confirmation flow exists, never applied to one side only.

## Standard development workflow

1. Read `current-project-state.md` → `product-brief.md` → `route-map.md`.
2. Inspect the current code before assuming anything about existing behavior.
3. Audit first for risky/cross-cutting changes (permissions, data model) before writing code.
4. Implement one small, scoped slice at a time.
5. Run `npx tsc --noEmit`, `npx eslint src`, `npm run build` — no new errors/warnings.
6. Report the exact diff and what was verified, in a structured report.
7. Give the user a concrete "try it yourself" manual checklist.
8. The user manually verifies the feature and direction in the running app.
9. Fix any regressions found before proceeding to anything else.
10. Create a Git checkpoint commit **only after** manual acceptance — never before.
11. Update `current-project-state.md` after each meaningful accepted phase.

**Automated verification (`tsc`/`eslint`/`build`) is necessary but is not a substitute for manual UX testing.** Both are required before a checkpoint.

## How to hand off to another AI

Paste this to start a fresh session (ChatGPT, Claude.ai, or Claude Code):

```
First read:
- docs/current-project-state.md
- docs/product-brief.md
- docs/route-map.md

Use the current repository as the implementation source of truth.
Do not regress newer verified behavior.
Read the Current phase and Next roadmap sections before proposing changes.
```

## Last updated

- Date: 2026-08-14
- Date: 2026-08-15
- Date: 2026-08-17
- Date: 2026-08-18
- Date: 2026-08-19
- Date: 2026-08-20
- Date: 2026-08-21
- Date: 2026-08-24
- Date: 2026-08-24 (Phase 11A)
- Date: 2026-08-24 (Phase 11A timer-placement correction + Phase 11B)
- Date: 2026-08-24 (Phase 11A/11B empty-UUID fix + final Time Tracking cluster + Phase 11C audit)
- Date: 2026-08-24 (Phase 11A/11B manually accepted + Edit-button polish + Phase 11C implementation)
- Date: 2026-08-24 (Phase 11A/11B/11C manually accepted + Phase 11D implementation — final Phase 11 pass)
- Date: 2026-08-25 (Phase 11A/11B/11C/11D all manually accepted; legacy Internal Report generation retired from UI; JWT "issued at future" investigated and root-caused to transient Supabase hosted-infrastructure clock skew, not app code)
- **Phase 8 is COMPLETE and pushed** (`a77785c57c3e5424fa63b4221ee9523a688ffae9`). Phase 9A (Reporting Automation audit) is complete and was read-only. **Phase 9B — Reporting Foundation + Client Report Correctness is COMPLETE and manually accepted**: Client Report gained Project scoping (`project_id`, deterministic-only legacy backfill), both report types' generation moved off a raw INSERT onto SECURITY DEFINER RPCs, Employees may now generate Client Report drafts (Project-scoped), generate/finalize are now separate permissions — finalize is never owner-based; **Supervisor finalization is legitimate-team/report-scoped** (reuses `can_view_client_report` + a Project-access defense-in-depth check), **Superadmin remains organization-wide**, a finalized Client Report can no longer be reopened, and the Accomplishments-vs-Client-Report naming confusion that produced the originally-reported RLS error was disambiguated in the UI. Destructive actions (trash/restore/permanently-delete) are separated from view/comment/finalize into their own narrower scope — trash/restore is generator-or-Superadmin-only, permanent delete is Superadmin-only always. A manually-reported Client Report detail-page 404 was root-caused to a **stale local Turbopack dev-server route cache**, not RLS or application code. Checkpointed as `299499948621f0d0d4742529d1e658d91b8eaf11`.
- **Phase 9C — Daily Update Automation is COMPLETE and manually accepted** ("Phase 9C passed") — see its own section above. Checkpointed as `a2f295f94a40e5c446e371a4b76cac9fcfb7dcdf`.
- **Phase 9D — Weekly Client Report Generation is COMPLETE and MANUALLY ACCEPTED** — see its own section above. Checkpointed as `39614fa660529c72d891c37ec573758599462559`.
- **Phase 9E — Sparing Efficiency Review + Finalization is COMPLETE and MANUALLY ACCEPTED.** **Phase 9F — Daily Visit Hours + Recurring Client Report Schedules is COMPLETE and MANUALLY ACCEPTED**, including its final integrity hotfix, migration canonicalization, manual-acceptance UI fixes, the locked Plan → Complete Visit workflow business-semantics correction, and final Visit UX polish — see their own sections above.
- **The user confirmed "Phase 9 passed." Phase 9 in its entirety (9A–9F) is COMPLETE and MANUALLY ACCEPTED**, folded into one final checkpoint commit — "checkpoint: complete phase 9 reporting automation" — pushed to GitHub (see git log for the exact hash; also recorded in "Latest verified checkpoint" above).
- **Phase 10 — One-Level Subtasks is COMPLETE and MANUALLY ACCEPTED** — base slice, hierarchy-authorization hardening, migration canonicalization, and the post-implementation TaskDrawer infinite-recursion fix, all manually accepted against the complete acceptance checklist. Migrations through `20260821210000_subtask_direct_access_canonical_fix.sql` are canonical and reproducible from migration history alone. Checkpointed as "checkpoint: complete phase 10 one-level subtasks" and pushed to GitHub (see git log for the exact hash; also recorded in "Latest verified checkpoint" above).
- **Phase 11 — Workflow Simplification & Product Polish is now formally defined**, with four planned subphases (11A Task Experience Redesign & Time Simplification, 11B Task Navigation & Quick View, 11C Reporting Simplification & Ownership, 11D Role Surface Cleanup & Cross-App Consistency). **Phase 11A is IMPLEMENTED, uncommitted, MANUAL ACCEPTANCE PENDING** — Task/Subtask Expected Time removed product-wide from the UI (DB field/type preserved, no migration), the full Task/Subtask page redesigned around a new compact metadata rail and `TaskStatusRail` segmented status control, Assignee presentation made compact, de-duplicated contributor names in Time Tracking history.
- **The user accepted the Phase 11A redesign with one correction**: the primary timer must live in the full Task page header's top-right, not lead the body. Combined with Phase 11B (Task Navigation & Quick View) in one pass: new `useTaskTimer` hook is the single authoritative timer controller per rendered Task, shared by the new header `TaskTimerControl` and the (now history-only) `TaskTimeTracking` body section; the Task Drawer was rewritten into a genuinely lightweight Quick View (Dashboard/Home only); a locked navigation rule now sends every dedicated work surface (Tasks List/Grid/Board, My Day, Planner) straight to the full Task page instead of opening a drawer; Planner gained an "Open My Day" action next to its view tabs, with "Today" keeping its normal calendar meaning.
- **An empty-UUID runtime crash** (`invalid input syntax for type uuid: ""`, from `listTimeEntriesForTask` via the new `useTaskTimer` hook) was found and fixed: `TaskDrawer` and the full Task page both split into an outer loading wrapper + a child component that only mounts `useTaskTimer` once a real Task exists, so no empty-id Time Entry query can ever fire. In the same pass, the user's final locked requirement — ALL primary time actions (Start/Pause/Resume/Stop, Log time) live in the top-right "Time Tracking" header cluster, the lower "Time Activity" section is history-only — was implemented; `reporting_review_access`-adjacent Phase 11C was also fully audited/designed (no implementation). **Phase 11A and 11B are IMPLEMENTED, uncommitted, FINAL MANUAL RECHECK PENDING. Phase 11C is AUDIT/DESIGN COMPLETE, implementation not started.** See their own sections above for the complete write-up. HEAD remains `67f8972d862e9a8ea57c280219a2f19cc5b45284` (Phase 10) until 11A/11B are manually accepted.
- **The user's final manual verification pass confirmed Phase 11A and 11B in full — both are now MANUALLY ACCEPTED.** One small Edit-button layout polish (visually separating it from the Time Tracking cluster) was folded into this pass. **Phase 11C — Reporting Simplification & Ownership was then implemented** against its complete audit/design: sidebar Reports now routes to Client Reports; Internal/Accomplishments Reports are gated Superadmin-only everywhere (routes, dashboard/My Day attention surfaces), historical backend fully preserved; the Client Reports page dropped its Total/Draft/Finalized stat cards and Internal/Client toggle in favor of a prominent Client+Project+Period generation panel and a "Recent Reports" table (now including a Project column); the false 30-day auto-delete Trash copy was corrected on both report types; multi-Employee aggregation, finalized immutability, generation/review/finalize authorization, and CSV/PDF export were all re-verified unchanged. **Phase 11C is IMPLEMENTED, uncommitted, MANUAL ACCEPTANCE PENDING.** No migration was created. HEAD remains `67f8972d862e9a8ea57c280219a2f19cc5b45284` (Phase 10) until 11C is manually accepted.
- **The user's final manual verification pass confirmed Phase 11C in full — Phase 11A, 11B, and 11C are all now MANUALLY ACCEPTED.** **Phase 11D — Role Surface Cleanup & Cross-App Consistency (the final Phase 11 pass) was then implemented**: the Client Reports page's Review Queue tab, ownership tabs ("My Reports"/"Team's/"All Client Reports"), and Client Report Schedules tab were all removed — `useClientReports()`'s already-RLS-scoped result now renders directly, a reviewer filters Status: Draft instead; "Generated by" was dropped from the normal Recent Reports table (still shown on the report detail page); the entire scheduling backend (table/RPCs/pg_cron) was left completely untouched, just unlinked from the UI. A read-only check for active schedule records was attempted (`supabase migration list` succeeded, `supabase db dump` failed — no local Docker daemon) — the exact count could not be safely determined and none were touched regardless. Client Visits (the `DailyVisitHoursCard` My Day widget — the only Visit UI surface in the app) moved from Employee/Supervisor to Superadmin-only, as an explicit UI-only product boundary — the underlying `permissions.ts` Visit functions remain self-service-by-design for any role, and no RLS/migration was created to harden this further, per instruction. A cross-app consistency sweep found and fixed two genuinely stale links: the command palette's report search (was indexing legacy Internal Reports) and the "r" keyboard shortcut (still pointed at the legacy `/dashboard/reports` route) — both now correctly point to Client Reports. **Phase 11D is IMPLEMENTED, uncommitted, MANUAL ACCEPTANCE PENDING.** No migration was created. HEAD remains `67f8972d862e9a8ea57c280219a2f19cc5b45284` (Phase 10) until 11D — and with it, all of Phase 11 — is manually accepted.
